# 인수인계 — MovieFit Studio (진하기 슬라이더 · 크기 축소 · 색 결함 수정)

> **v1.3** | 최종 업데이트: **2026-08-15 20:35** | 작성자: Claude Code

## 변경 이력

| 버전 | 일시 | 주요 변경 |
|------|------|---------|
| v1.3 | 2026-08-15 20:35 | **설치본(8765)에 반영하고 검증했다.** 6절 신설. 남은 것은 설치 파일(exe) 재빌드와 공개 배포뿐이다 |
| v1.2 | 2026-08-15 19:55 | **사용자가 GitHub 에 push 했다.** 1·3·5절을 push 뒤 상태로 고쳤다 |
| v1.1 | 2026-08-15 19:20 | 사용자가 **커밋을 승인하고 "비눈도 동일하게"** 를 지시. 앞 작업을 커밋(`b77e939`)하고, **비·눈도 알파 합성으로** 바꿨다. 옛 `blend` 경로를 통째로 걷어 냈다 |
| v1.0 | 2026-08-15 17:40 | 초기 작성. 사용자 지시 4건을 마치고, 데모에서 **색 결함 하나를 더 잡아 고친** 시점 |

> 앞 문서 `handover_20260815_1310.md` 는 **효과 16종과 가림 상한**까지를 다룬다.
> 그 문서 8절의 4건을 이 세션에서 전부 마쳤다.

---

## 0. 한 문단 요약

**사용자 지시 4건을 전부 마쳤다.** 화면을 가리는 효과 9종에 **진하기(투명 정도)
슬라이더**를 붙였고, 물방울·비눗방울·폭죽을 지시대로 줄였다. 그리고 **그 데모를
눈으로 보다가 결함을 하나 더 잡았다** — 주변 어둡게가 화면을 보라색으로 물들이고
있었는데, 진하기 200%뿐 아니라 **기본값에서도** 그랬다. 이번 작업이 만든 것이 아니라
**원래부터 있던 것**이고 점검 804개가 전부 통과하고 있었다. 밝기 면만 곱하던 것을
**알파 합성**으로 바꿔 고쳤다(색의 진하기 150% → 101%).

이어서 사용자가 커밋을 승인하고 *"비눈도 동일하게"* 를 지시해, **비·눈도 알파
합성으로** 바꿨다(4절). 그러면서 옛 `blend` 경로를 쓰는 곳이 하나도 안 남아
통째로 걷어 냈다. **점검 813개가 전부 통과한다.**

---

## 1. 지금 컴퓨터 상태

| 항목 | 상태 |
|---|---|
| 설치본 (8765) | **반영 완료·검증됨** (2026-08-15 20:0x 재기동, 6절) |
| 개발 서버 (8766) | 새 코드가 올라가 있다 |
| 마지막 커밋 | `dbe3c76` — 비·눈 알파 합성 |
| 커밋 안 된 변경 | **없다** (이 문서의 v1.2 수정만 남는다) |
| GitHub push | **완료.** `origin/main` = `dbe3c76` (2026-08-15 19:5x, 사용자가 직접) |

### 서버를 다시 띄우는 법 (파이썬을 고쳤으면 **반드시**)

```powershell
Get-NetTCPConnection -LocalPort 8766 -State Listen -ErrorAction SilentlyContinue |
  ForEach-Object { Stop-Process -Id $_.OwningProcess -Force }
```
```
python -m app --port 8766
```

### 점검 돌리는 법

```
MOVIEFIT_TEST_URL=http://127.0.0.1:8766 python tests/looks_test.py
```
`MOVIEFIT_TEST_PROJECTS` 는 **지운다**. `PYTHONIOENCODING=utf-8` 을 함께 주면
한글이 안 깨진다.

---

## 2. 이번 세션에 한 일 (넷)

### 2-1. 진하기(투명 정도) 슬라이더 — 9종

14절에서 값을 낮춘 것 전부에 붙였다: 주변 어둡게 · 가장자리 어둡게 · 비 · 눈 ·
따뜻하게 · 차갑게 · 선명하게 · 빈티지 · 흑백. **기본 100%**(=14절의 순한 값),
범위 20~200%.

**막힌 곳**: 색감 계열은 등록표에 필터 문자열을 글자 그대로 적어 두어 **진하기를
곱할 수가 없었다.** 필터 이름과 값만 적는 형태로 바꾸고, 값마다 '아무 일도 안 하는
값(중립값)'을 알면 한 줄로 계산된다는 것을 이용했다:

    새 값 = 중립값 + (적어 둔 값 - 중립값) × 진하기

중립값은 `eq` 의 채도·대비가 1, `colorbalance` 가 0, `hue` 의 s 가 1,
`colorchannelmixer` 가 단위행렬이다. 이 한 규칙이 6종을 다 덮고, 곱셈이라
**세기 3단계 사이의 비율이 저절로 보존된다.**

`app/static/app.js` 는 **한 줄도 안 고쳤다** — 서버가 준 설명서만 보고 슬라이더를
그리기 때문이다(13-1절의 설계가 실제로 값을 했다).

### 2-2. 물방울 · 비눗방울 · 폭죽을 줄였다

| 효과 | 무엇을 | **함께** 고친 것 |
|---|---|---|
| 물방울 | 지름 `min/17 → min/21` | 개수 `9·19·34 → 12·25·44` |
| 비눗방울 | 지름 `min/42 → min/50` | 개수 그대로 ("조금만"이라고 했다) |
| 폭죽 | 칸 크기 기본 `38 → 22` | 알갱이 굵기 `칸/28 → 칸/20` |

**작아지면 덮는 면적이 줄어 세기 사다리가 통째로 내려앉는다.** 그래서 개수·굵기를
함께 올려야 한다. 폭죽은 칸만 줄이면 알갱이가 다시 점으로 사라진다(15-5절에서 이미
한 번 겪은 실수다).

### 2-3. ★ 데모를 눈으로 보다가 잡은 결함 — 주변 어둡게의 색

**점검 804개가 전부 초록불인 상태에서 눈으로 잡았다.** 자세한 원인·해법·실측은
`docs/DESIGN_effects_and_zoom.md` 16-4절에 있다. 요지만:

- 회색 그림을 **밝기(Y) 면에만 곱하고** 색(U·V)은 그대로 두었다
- `R - B = 1.402(V-128) + 1.772(U-128)` 은 **밝기와 무관**하다. 그래서 밝기를 반으로
  줄이면 '밝기에 견준 색'이 두 배가 된다 → 화면이 보라색이 된다
- **고침**: 검은 그림을 **알파로** 얹는다(`geq` 로 알파만 그리고 `overlay`).
  알파 합성은 색차도 함께 데려간다 — `U_새 = 128 + (U-128)×(1-a)`
- 색의 진하기 150% → **101%**. 구간 밖 완전 일치는 그대로(0개)
- dim 값도 다시 잡았다: `0.84/0.72/0.56 → 0.86/0.73/0.60`

### 2-4. 새 잣대를 점검에 넣었다

- **밝기에 견준 색의 진하기** `(최대채널-최소채널)/최대채널` 의 평균. 어둡게 하는
  효과에 상한 115%. **기본값과 진하기 200% 양쪽에서** 잰다
- 진하기가 실제로 화면을 바꾸는지 (50/100/200%가 1.3배씩 벌어지는지) 9종 전부
- 진하기의 **기본값이 100%인지** — 기본값을 올리면 14절의 가림 상한이 무력해진다

---

## 2-5. 점검 현황 — 813개 (21개 스위트) · 전부 통과

| 스위트 | 개수 | 이번에 늘어난 것 |
|---|---:|---|
| `tests/looks_test.py` | **126** | 진하기 9개 · 기본값 1개 · 슬라이더 등록 1개 · 색이 튀는지 4개 · 빗줄기 색 2개 · blend 제거 1개 |
| `tests/artfx_test.py` | 55 | (크기만 바뀌어 개수는 그대로) |
| `tests/effects_ui_test.py` | 39 | 슬라이더 목록이 늘어난 것 반영 |
| `tests/weather_test.py` | **73** | 알파 합성으로 바뀐 것 반영 |
| 나머지 17개 | 520 | — |

> 늘어난 점검은 **전부 새 잣대**다. 예전 잣대로는 이번에 잡은 두 결함이 안 잡혔다.

따로 도는 것: `longvideo_test.py`(17개, 10~40분) — **이번에 돌리지 않았다.**

> ⚠ 일괄 실행 중에 `effects_ui_test.py` 가 1초 만에 죽은 적이 있다. 원인은 코드가
> 아니라 **윈도우 소켓 자원 고갈**(`ERR_NO_BUFFER_SPACE`)이었다 — 그때 다른 창에서
> FFmpeg 을 여럿 돌리고 있었다. 단독으로 돌리면 39개가 전부 통과한다.
> **점검과 무거운 렌더를 동시에 돌리지 말 것.**

---

## 3. 커밋 현황 — 둘 다 GitHub 에 올라가 있다

```
dbe3c76  비·눈도 알파로 얹어 빗줄기가 배경색을 머금던 것을 고친다
b77e939  효과를 16종으로 늘리고, 진하기를 조절하게 하고, 어두워질 때 색이 튀던 것을 고친다
```

`b77e939` — 효과 16종 · 가림 상한 · 진하기 슬라이더 · 크기 축소 · 주변 어둡게의
색 수정 · 새 점검 · 교훈 셋 · 이 문서.
`dbe3c76` — 비·눈 알파 합성 · 옛 `blend` 경로 제거 · 눈 진하기 상향 · 빗줄기 색 점검.

> 데모 `효과_크기비교_데모.mp4` · `효과_진하기_데모.mp4` · `효과_비눈색_데모.mp4` 는
> `.gitignore` 의 `/*.mp4` 가 막고 있어 커밋에 안 들어간다. **일부러 그렇게 두었다.**

---

## 4. 비·눈도 같은 방식으로 고쳤다 (사용자 지시 "비눈도 동일하게")

`screen` 으로 밝기만 올리면 **밝아진 화소가 원래 색을 그대로 지닌다.** 그래서
풀밭 위의 빗줄기가 초록빛이었다 — 흰 비가 아니라 '밝아진 풀'이다.

흰 그림에 `alphamerge` 로 알파를 붙여 `overlay` 로 얹는 방식으로 바꿨다.
`alphamerge` 는 둘째 입력의 밝기를 첫째 입력의 알파로 삼으므로 **쓰던 회색 그림을
한 글자도 안 고치고** 그대로 알파로 쓸 수 있었다.

밝아진 자리만 골라 잰 색(낮을수록 흰색): 비 20.9% → **15.4%**, 눈 18.6% → **13.3%**.

**값은 비만 그대로 두고 눈만 올렸다.** 알파로 바꾸면 평균 밀림은 줄지만
(비 6.05 → 3.54) **남은 결은 거의 그대로다**(136% → 132%) — 화면이 어수선해지는
정도는 안 변했다는 뜻이라 비는 올릴 이유가 없다(올리면 결이 상한 140%를 넘는다).
눈은 '약하게'가 바닥(0.3)에 딱 걸려 진하기를 `0.46/0.60/0.74 → 0.62/0.78/0.94` 로
올렸다.

**옛 `blend` 경로를 통째로 걷어 냈다.** 덧씌우는 효과 셋이 전부 알파를 쓰게 되어
쓰는 곳이 하나도 남지 않았다. 안 쓰는 함수를 "이 파일에서 가장 중요한 한 줄"이라는
설명과 함께 남겨 두면 다음 사람을 오도한다.

---

## 5. 다음에 할 일

1. **설치 파일(exe)을 다시 만들어 공개 배포** ← 다음은 여기다. 6-3절에 순서가 있다.
   **아직 안 했고, 되돌리기 어려운 공개 작업이라 사용자 확인을 받아야 한다**
2. 사용자가 데모 세 편을 본다 (필요하면)
   - `효과_크기비교_데모.mp4` (18초) — 왼쪽 예전 크기 / 오른쪽 지금 크기
   - `효과_진하기_데모.mp4` (27초) — 진하기 20% / 100% / 200%
   - `효과_비눈색_데모.mp4` (12초) — 왼쪽 예전(밝기만) / 오른쪽 지금(알파)
5. 남은 것: 4K·30분 영상 시험 · ElevenLabs 승인 대기 · 소재 칸 통합 ·
   마이크 녹음(F-45) · 내 목소리 나레이션(F-46) · EXIF 회전·`.heic` · 강제정렬 정확도

---

## 6. 데모를 다시 만드는 법

스크립트는 저장소에 없다(이번 세션 scratchpad 에 있었다). 다시 만들 때 지킬 것:

1. **두 쪽이 화면의 같은 자리를 보여 줘야 한다.** 왼쪽 반·오른쪽 반을 각각 잘라
   붙이면 바탕이 달라 비교가 안 된다. 두 영상에서 **같은 가운데 640픽셀**을 잘라
   나란히 놓는다
2. **자리를 정하는 효과(폭죽)는 화면 한가운데**에 둔다. 안 그러면 한쪽 반쪽에만
   나와 비교가 아예 안 된다
3. 바탕은 **결·중간 밝기·색을 갖춘 화면**이어야 한다. 색막대 위에서는 결도 색도
   못 잰다
4. 글자는 `drawtext` 대신 **Pillow 로 그린 PNG 를 `movie=` 로** 얹는다. 윈도우 글꼴
   경로의 콜론이 필터그래프를 깨뜨린다
5. 예전 크기를 재현하려면 `app/core/fxart.py` 의 나누는 수를 옛 값으로 바꾼
   **사본 모듈**을 만들어 `effects.fxart` 에 잠시 끼워 넣는다

---

## 7. 새 창에서 쓸 시작 프롬프트

```
capcut-studio-kit 프로젝트를 이어서 진행합니다.

먼저 다음을 읽어 주세요:
1) CLAUDE.md
2) memory/ 폴더 전체
3) handover_20260815_1740.md   ← 이 문서
4) docs/DESIGN_effects_and_zoom.md   (16절이 최신 현황)

지금 상태: 화면 효과 16종에 진하기 슬라이더까지 붙어 개발 서버(8766)에 올라가
있고, 점검 813개가 전부 통과합니다. **커밋과 push 를 마쳤습니다**(`dbe3c76`).
다음 할 일은 **설치본(8765) 반영과 배포**입니다.

점검을 돌릴 때는 MOVIEFIT_TEST_URL=http://127.0.0.1:8766 을 주고
MOVIEFIT_TEST_PROJECTS 는 지워 주세요.
```

---

## 6. 설치본(8765) 반영 — 완료·검증됨 (2026-08-15 20:0x)

### 6-1. 무엇을 했나

설치 위치: `C:\Users\INTEL\AppData\Local\Programs\MovieFit Studio`

1. **백업**: `app` · `tools` · `assets` · `requirements.txt` 를
   `_backup_20260815_2010\` 에 통째로 넣어 두었다. 문제가 없으면 지워도 된다
2. **`pillow` 를 새로 넣었다.** 설치본에는 pip 이 없다 —
   `tools/build_installer.py` 가 payload 를 만든 뒤 pip·setuptools·wheel 을
   지우기 때문이다(159행). 그래서 **개발 파이썬의 pip 으로 설치본 폴더에 직접** 넣었다:

       python -m pip install --target "<설치폴더>\python\Lib\site-packages" pillow

   두 파이썬이 **3.11.9 로 정확히 같아서** 가능했다. 다르면 이 방법을 쓰면 안 된다.
   `numpy` 는 음성인식 부품이 이미 함께 깔아 두어 손댈 필요가 없었다(2.4.6).
3. `app` · `tools` · `assets` · `requirements.txt` 를 저장소에서 덮어썼다
   (`projects/` 는 **건드리지 않았다** — 사용자 자료다)
4. `__pycache__` 5개를 지웠다 (옛 바이트코드가 남지 않게)
5. 8765 를 끄고 `MovieFitStudio.bat` 으로 다시 켰다

### 6-2. **새 코드가 실제로 도는지** 확인한 것 (파일 복사 ≠ 반영)

| 확인한 것 | 결과 |
|---|---|
| 효과 종류 개수 | **16종** |
| 비의 값 목록 | `speed`, **`opacity`** |
| 폭죽 '터지는 크기' 기본값 | **22** (옛 코드는 38 — 새 값이 확실히 올라왔다) |
| 주변 어둡게 진하기 | 기본 100 · 범위 20~200 |
| 설치본 파이썬으로 그림 그리기 | **5장 그려짐** (`pillow` 가 그 환경에서 실제로 돈다) |
| 필터에 알파 합성이 쓰이는가 | `overlay=0:0` · `alphamerge` 있음, `blend=` **없음** |
| 실제 브라우저로 눌러 보기 | `effects_ui_test.py` **39개 전부 통과** (8765 겨냥) |

> 폭죽 기본값 22 를 고른 이유: **옛 코드와 새 코드가 다른 값을 주는 자리**라서
> "정말 새 코드가 도는가"를 한 번에 가른다. 개수·이름만 보면 옛 코드도 통과한다
> (memory/installed-app-needs-restart.md 4번).

> 사용자에게는 브라우저에서 **Ctrl+F5** 를 눌러 달라고 안내해야 한다.
> 브라우저가 옛 화면 파일을 들고 있을 수 있다.

### 6-3. 아직 안 한 것 — 설치 파일(exe) 재빌드와 공개 배포

지금은 **이 컴퓨터의 설치본만** 새 코드다. 남들이 내려받는 설치 파일은 옛것이다.

    1) app/__init__.py 의 __version__ 을 올린다 (지금 0.5.0)
    2) docs/release_notes_vX.Y.Z.md 를 쓴다 (v0.3.0 것을 본으로)
    3) python tools/build_installer.py            → build/dist 에 exe 가 나온다
       · Inno Setup(ISCC) 필요: winget install JRSoftware.InnoSetup
       · requirements.txt 를 읽어 pillow 를 payload 에 자동으로 넣는다
    4) 빈 환경에서 **처음부터 설치해 본다** (memory/fresh-install-must-be-tested.md)
    5) GitHub 릴리스로 exe 를 올린다
    6) 소개 사이트는 **git push 로 자동 배포되지 않는다.** Vercel 명령줄로 직접 올린다
       (memory/vercel-landing-is-cli-deployed.md)

**5·6번은 되돌리기 어려운 공개 작업이라 사용자 확인을 받고 해야 한다.**
