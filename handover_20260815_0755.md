# 인수인계 — MovieFit Studio (화면 확대 · 화면 효과 띠 완성)

> **v1.0** | 최종 업데이트: **2026-08-15 07:55** | 작성자: Claude Code

## 변경 이력

| 버전 | 일시 | 주요 변경 |
|------|------|---------|
| v1.0 | 2026-08-15 07:55 | 초기 작성. 화면 확대와 화면 효과 띠 뼈대를 만들어 검증까지 마친 시점 |

> 앞 문서 `handover_20260814_1607.md`(v1.1) 는 **목소리 버그 수정과 화면 효과 조사까지**를
> 다룬다. 그 뒤의 일은 이 문서가 맞다. 그 문서의 4-A절(사용자 지시 원문)은 **여전히 유효**하다.

---

## 0. 한 문단 요약

**사용자가 지시한 두 기능 중 "화면 확대"를 완성했고, "화면 강조(=화면 효과)"의 뼈대와
첫 효과인 줌 강조를 완성했다.** 둘 다 실제로 영상을 만들어 픽셀로 검증했고, 화면은
브라우저를 몰아서 클릭까지 확인했다. **점검 541개가 전부 통과한다.**
남은 것은 비·눈·비눗방울·작은 폭죽·색보정·나머지 강조 4종을 **등록표에 붙이는 일**이다.
**커밋하지 않았다.**

---

## 1. 사용자가 정정해 준 것 (가장 중요 — 새 창은 이것부터 읽을 것)

앞 문서 4-A절이 "화면 **강조**"의 뜻을 확인 못 했다고 적어 두었다. **확정되었다.**

> **"화면 강조" = 비 · 눈 · 거품 버블 · 요소요소 작게 터지는 폭죽 같은 화면 효과 자체다.**
> (효과의 세기만 뜻하는 것이 아니었다)

사용자가 못박은 원칙 세 가지:

1. **"효과 기능만 설계하고 사용자가 적용할 수 있도록 하는 거야."**
   → 어떤 효과도 "몇 초에 자동으로 들어간다"고 정하지 않는다. 기본값은 **빈 목록**이다.
2. **"효과를 20초 넣고 이러는 것 아니야."** → 길이를 못 박지 않는다. 사용자가 정한다.
3. **화면 확대는 그냥 배율이 아니다.** "16:9 를 9:16 으로 만들 때 **9:16 사각형을 원본의
   어느 위치에 맞출지**도 정하는 것"이다.

비눗방울(거품 버블) 사양도 확정됐다: **천천히 · 이리저리 떠다니며 · 작게**
(크게 만들면 화면을 가린다고 사용자가 직접 지적했다).

---

## 2. 지금 컴퓨터 상태

| 항목 | 상태 |
|---|---|
| 설치본 (8765) | **꺼져 있음** (이번 작업은 설치본에 반영하지 않았다) |
| 개발 서버 (8766) | **켜져 있음**. 새 코드가 올라가 있다 |
| 작업 트리 | **커밋 안 됨.** 마지막 커밋 `71a3e12` |

### 커밋하지 않은 변경 27건 — **두 세션 분량이 섞여 있다**

마지막 커밋은 `71a3e12`. 커밋할 때 **어느 것이 무슨 작업인지 갈라서** 다루어야 한다.

**(가) 지난 세션 — 목소리 버그 수정 (이미 검증 끝남)**

```
 M app/api/tts.py            미리듣기가 목소리 언어에 맞는 견본을 쓰게
 M app/core/tts/base.py      SAMPLE_TEXTS(20개 언어) + 언어 판별
 M app/core/tts/edge.py      오류 문구를 원인별로 갈라 씀
 M app/static/app.js         (이번 작업과 **같은 파일**이다 — 주의)
 M .gitignore
?? tests/voice_language_test.py             점검 26개
?? tools/check_elevenlabs.py                아직 안 돌려 봄
?? memory/tts-voice-must-match-text-language.md
?? memory/ffmpeg-effect-layer-traps.md
?? docs/RESEARCH_screen_effects.md
?? handover_20260814_1607.md
```

**(나) 이번 세션 — 화면 확대 · 화면 효과 띠**

```
 M app/core/framing.py       zoom · sharpness · _pad_tail
 M app/core/ffmpeg.py        video_fps · 효과를 필터에 끼움
 M app/core/projects.py      effects 저장 구조
 M app/api/render.py         효과를 렌더링에 넘김
 M app/api/system.py         effect-kinds API · framing 이 zoom 을 받음
 M app/static/app.js         resolveFraming(zoom) · 효과 패널 · 효과 띠
 M app/static/index.html     확대/위치 막대 · 효과 패널 · 효과 띠
 M app/static/style.css      효과 띠 · 타임라인 높이 수정
 M tests/ui_test.py          막대 이름 바뀐 곳 한 줄
 M memory/verify-ui-by-actually-clicking.md   함정 한 가지 추가
?? app/core/effects.py                       새 모듈
?? tests/zoom_test.py                        점검 25개
?? tests/effects_test.py                     점검 39개
?? tests/effects_ui_test.py                  점검 21개
?? docs/DESIGN_effects_and_zoom.md
?? docs/RESEARCH_capcut_effects_filters.md
?? handover_20260815_0755.md
```

> `app/static/app.js` 는 **두 작업이 같이 들어 있다.** 나눠 커밋하려면 조각 단위로
> 골라야 하고, 그럴 필요가 없다면 한 번에 커밋하되 메시지에 둘 다 적는 편이 낫다.

> `목소리_들어보기/` 와 `화면효과_데모.mp4` 는 **저장소에 넣지 말 것.**
> (`.gitignore` 가 막고 있어 위 목록에 안 보인다 — 커밋 전에 다시 확인할 것)

> **서버를 다시 띄울 때 주의**: 윈도우에서 `pkill` 로는 안 죽는다. PowerShell 로
> `Get-NetTCPConnection -LocalPort 8766 -State Listen | ForEach-Object { Stop-Process -Id $_.OwningProcess -Force }`
> 를 써야 한다. 안 죽이면 **옛 코드가 계속 돌아 새 API 가 404 로 나온다** (실제로 겪었다).

> **점검 환경변수는 짝으로**: 개발 서버(8766)를 겨냥할 때는
> `MOVIEFIT_TEST_URL=http://127.0.0.1:8766` 을 주고 `MOVIEFIT_TEST_PROJECTS` 는 **지운다.**

---

## 3. 만든 것 ① — 화면 확대·위치 ✅ 완료·검증됨

**무엇인가**: 가로 영상을 세로로 다시 만들 때 잘라낼 **세로 사각형의 크기와 위치**를
사용자가 정한다.

**핵심 정의**: *출력 영상 크기는 확대와 무관하게 그대로 두고, 그 틀에 원본의 얼마를 담을지만
바꾼다.* 크기를 고정하는 이유 — 자막 글자 크기가 출력 세로 화소 기준이라, 안 그러면
**확대할 때마다 자막이 같이 출렁인다.**

| 고친 곳 | 무엇을 |
|---|---|
| `app/core/framing.py` | `zoom`(0.5~2.0) · `sharpness` 화질 지표 · `_pad_tail()` |
| `app/static/app.js` | `resolveFraming()` 에 **똑같은** 계산 · 위치 막대 **가로·세로 2개** · 확대 막대 |
| `app/static/index.html` | 확대 막대 · 가로/세로 위치 막대 · 화질 경고 |
| `app/api/system.py` | `/api/system/framing` 이 `zoom` 을 받음 |
| `tests/zoom_test.py` | **새 점검 25개** |

**검증**: 화면과 서버 계산을 **800가지에서 대조 — 전부 일치.** 일부러 2픽셀 어긋나게 만들어
**228건을 잡는 것**까지 확인했다. 실물로는 출력 크기가 확대와 무관하게 404×720 으로 같고,
2.0배 결과가 "1.0배의 가운데를 두 배로 키운 것"과 **49.5 : 14.1** 로 일치했다.

**미리 막은 함정**: 확대하면 사각형이 세로로도 작아져 두 축 모두 움직일 수 있게 되는데,
기존 위치 막대는 **한 개뿐**이라 한쪽 축이 잠겼을 것이다. 두 개로 나눴다.

---

## 4. 만든 것 ② — 화면 효과 띠 뼈대 + 줌 강조 ✅ 완료·검증됨

### 이 뼈대의 값어치

**효과를 하나 더 붙이는 일이 `app/core/effects.py` 의 `KINDS` 등록표에 항목 하나를 더하는
것으로 끝난다.** 저장 구조·API·화면·렌더링을 다시 손댈 필요가 없다. 앞으로 효과 열 몇 개를
붙일 것이므로 이것이 1단계의 핵심이었다.

등록표 항목 하나에 넣는 것: `label`(한국어 이름) · `hint`(설명) · `order`(필터 순서) ·
`params`(효과별 값의 기본값) · `strengths`(세기 3단계) · `build`(필터 만드는 함수).

### 막대 하나의 생김새

```json
{"id": "fx-1", "kind": "zoom_punch", "start": 12.0, "end": 14.5,
 "strength": "medium", "params": {}}
```

**여러 개 놓을 수 있고 겹쳐도 된다.** 이것이 "요소요소 작은 폭죽"을 만드는 방법이다.

### 고친 곳

| 파일 | 무엇을 |
|---|---|
| `app/core/effects.py` | **새 파일** — 등록표 · `normalize()` · `build_filter()` · 줌 강조 |
| `app/core/ffmpeg.py` | `video_fps()` 새로 만듦 · 효과를 받아 필터에 끼움 |
| `app/core/projects.py` | 저장 구조에 `effects: []` · 저장할 때 다듬기 |
| `app/api/render.py` | 프로젝트의 효과를 렌더링에 넘김 |
| `app/api/system.py` | `/api/system/effect-kinds` |
| `app/static/*` | 효과 패널 · 타임라인 효과 띠 · 끌기/길이조절/세기/지우기 |
| `tests/effects_test.py` | **새 점검 39개** |
| `tests/effects_ui_test.py` | **새 점검 21개** (브라우저 실제 클릭) |

### 필터 순서 (바꾸지 말 것)

```
[화면비 자르기 + 확대] → [화면 효과] → [자막 새기기]
```

자막이 **맨 마지막**이어야 한다. 효과가 자막 위에 오면 글자가 가려진다.
효과가 **하나도 없으면 필터를 만들지 않는다** — 기존 경로가 그대로 돌아 느려지지 않는다.

### 검증 결과

**구간이 정확히 지켜진다** (효과 없는 영상과 대조):

| 시각 | 닮음 | 뜻 |
|---|---|---|
| 1.0초 (구간 앞) | **inf** | 픽셀 하나도 다르지 않다 |
| 3.0초 (구간 안) | **4.6** | 확실히 다르다 |
| 5.0초 (구간 뒤) | **45.9** | 사실상 같다 |

길이·프레임 수·프레임률·화면 크기 모두 효과 없는 영상과 동일하다.

**브라우저 클릭 검증**: 단추 → 재생 위치에 막대 생성 → 끌면 2.0→2.6초 이동 →
세기 단추가 값 변경 → 저장 후 재접속해도 남아 있음 → 지우면 사라짐. JS 오류 0건.

---

## 5. zoompan 실측 — 반드시 알아야 할 두 함정

시간에 따라 배율을 바꾸는 FFmpeg 내장 수단은 `zoompan` 이 **사실상 유일하다**
(`crop`·`scale` 은 가로·세로에 시간식을 못 쓴다). `sample_10s.mp4`(1280×720, 30fps)로 쟀다.

| 함정 | 실측 결과 | 막는 법 |
|---|---|---|
| 기본 `fps=25` | **10초 영상이 12초로 늘어난다.** 프레임 수는 300 그대로인데 길이가 변한다 → **소리와 어긋난다** | `fps=<원본 fps>` 를 반드시 준다 (`ffmpeg.video_fps()`) |
| `d`(머무는 프레임) | `d=1` 이면 되풀이 없음 — 1~3초 60프레임 중 **59개가 서로 다름**(그냥 재인코딩한 대조군은 30개) | `d=1` 을 못 박는다 |

---

## 6. 잡은 결함 하나 — 오류 없이 끌기만 죽었다

효과 띠를 넣으면서 `.tl-track` 높이를 34px 줄였는데, **같은 것을 정하는 규칙이 하나 더
있었다**: `.tl-inner.has-film .tl-track { height: calc(100% - 58px) }` (영상 띠가 켜졌을 때).

이게 이겨서 효과 띠가 **타임라인 상자 바깥으로 밀려나** 아래 단추 위에 놓였고,
**단추가 마우스를 가로채 막대를 끌 수 없었다.** 오류도 콘솔 경고도 없었고 서버 점검
39개는 전부 통과했다.

찾은 방법: 브라우저에서 `document.elementFromPoint(x, y)` 로 **그 좌표에 실제로 무엇이
있는지** 물었더니 `"btn btn-tiny"` 가 나왔다.

점검에 못 박았다 — ① 효과 띠 아래끝이 타임라인 상자 안에 있는가 ② 막대 한가운데의
`elementFromPoint` 가 막대 자신인가. 교훈은 `memory/verify-ui-by-actually-clicking.md` 에 있다.

---

## 7. 점검 현황 — 541개 (18개 스위트) · 전부 통과

기존 456 + `zoom_test` 25 + `effects_test` 39 + `effects_ui_test` 21.
따로 도는 것: `longvideo_test.py`(17개, 10~40분).

```
python tests/zoom_test.py         화면 확대 (서버 없으면 계산만, 있으면 실물까지)
python tests/effects_test.py      화면 효과 (같음)
python tests/effects_ui_test.py   효과 패널 — 브라우저를 실제로 몰아서 (서버 필수)
```

---

## 8. 다음에 할 일 (승인된 순서)

`docs/RESEARCH_capcut_effects_filters.md` 의 선별안 12개 중 **줌 강조 1개만 만들었다.**

| 단계 | 무엇 | 근거 |
|---|---|---|
| **2** | **비 · 눈** — 등록표에 붙인다 | `RESEARCH_screen_effects.md` 에 필터 조리법이 **실측까지** 끝나 있다 |
| **3** | **필터** — 밝기·대비·채도 슬라이더 + 색감 프리셋 5종 | `eq` 계열이라 속도 부담 없음 |
| **4** | **강조 4종** — 주변 어둡게 · 부분 흐림 · 비네트 · 네모 테두리 | 위치 지정 화면이 필요해 손이 더 간다 |
| **5** | **물방울 맺힘 · 비눗방울 · 작은 폭죽** | 파이썬이 그림을 그려야 하고 다듬는 데 시간이 가장 많이 든다 |

**2단계를 시작하는 법**: `app/core/effects.py` 의 `KINDS` 에 `"rain"` 항목을 더하고
`_build_rain()` 을 쓴다. 필터 조리법과 넘어진 함정 4가지는
`memory/ffmpeg-effect-layer-traps.md` 와 `docs/RESEARCH_screen_effects.md` 5절에 있다.
**특히 `format=gbrp` 를 빼면 화면 전체가 뿌예진다.**

점검은 `tests/effects_test.py` 의 구조를 그대로 쓴다 — 구간 밖은 `inf`(완전 일치),
구간 안은 확실히 다름.

---

## 9. 아직 안 된 것 (정직하게)

- **효과 11개 미착수** (줌 강조만 있다)
- **줌 강조의 세기 3단계가 눈으로 구별되는지 렌더해서 재지 않았다.**
  배율 간격은 2.33배·1.71배로 기준(1.3배)을 넘지만, **실제 화면 변화량은 안 쟀다**
- **세로 영상(9:16)·4K 에서 효과를 시험하지 않았다.** 720p 가로에서만 확인했다
- **긴 영상(30분)에서 효과를 돌려 보지 않았다.** 5.7배 느려진다는 것은 30초 실측 환산값이다
- **설치본에 반영하지 않았다.** 개발 서버(8766)에만 있다
- **커밋하지 않았다**
- **사용자가 `화면효과_데모.mp4` 를 아직 안 봤다** — 비·눈·물방울·폭죽이 **보기 좋은지**는
  사람만 판정할 수 있다. 2단계 전에 확인받는 것이 좋다
- 앞 문서에서 넘어온 것: **ElevenLabs 승인 대기** · 소재 칸 통합 · 마이크 녹음(F-45) ·
  내 목소리 나레이션(F-46) · EXIF 회전·`.heic` · 강제정렬 정확도

---

## 10. 새 창에서 쓸 시작 프롬프트

```
capcut-studio-kit 프로젝트를 이어서 진행합니다.

먼저 다음을 읽어 주세요:
1) CLAUDE.md
2) memory/ 폴더 전체
3) handover_20260815_0755.md   ← 이 문서
4) docs/DESIGN_effects_and_zoom.md      (설계와 구현 현황)
5) docs/RESEARCH_capcut_effects_filters.md  (선별안 12개)
6) docs/RESEARCH_screen_effects.md      (비·눈·물방울·폭죽 조리법)

지금 상태: 화면 확대와 화면 효과 띠 뼈대가 완성되어 개발 서버(8766)에 올라가 있고,
점검 541개가 전부 통과합니다. 아직 커밋하지 않았습니다.
효과는 줌 강조 1개만 등록되어 있습니다.

점검을 돌릴 때는 MOVIEFIT_TEST_URL=http://127.0.0.1:8766 을 주고
MOVIEFIT_TEST_PROJECTS 는 지워 주세요.

할 일: (여기에 적어 주세요)

  예시 1) 2단계 — 비·눈 효과를 등록표에 붙이기
  예시 2) 지금까지 작업 커밋하기
  예시 3) 설치본(8765)에 반영하기
  예시 4) 화면효과_데모.mp4 를 보고 어떤 효과가 쓸 만한지 정하기
```
