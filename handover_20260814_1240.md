# 인수인계 — MovieFit Studio (Phase 7 완료 · v0.5.0)

> **v1.0** | 최종 업데이트: **2026-08-14 12:40** | 작성자: Claude Code

## 변경 이력

| 버전 | 일시 | 주요 변경 |
|------|------|---------|
| v1.0 | 2026-08-14 12:40 | 초기 작성. Phase 7(영상 띠·배경음악·덕킹 세기·화면 손질)을 마치고 v0.5.0 을 배포한 시점 |

> 앞 문서 `handover_20260813_1930.md` 는 **v0.4.0 까지**를 다룬다. 지금 상태는 이 문서가 맞다.

---

## 0. 한 문단 요약

**Phase 7 을 끝내고 v0.5.0 을 배포했다.** 이번 작업은 계획서가 아니라 **사용자가 실제로
화면을 써 보고 지적한 것**들에서 나왔다. 타임라인에 영상 띠가 생겼고, 배경음악을 깔 수
있게 되었고, 덕킹 세기를 고를 수 있게 되었다. 화면 손질도 여럿 했다.
점검은 333개 → **430개**로 늘었고 전부 통과한다.

---

## 1. 지금 컴퓨터 상태

| 항목 | 상태 |
|---|---|
| 설치본 | 8765 포트 · **v0.5.0 로 갈아 끼웠고 다시 켰다** (PID 는 매번 바뀐다) |
| 개발 서버 | 8766 에 떠 있을 수 있다. 필요 없으면 꺼도 된다 |
| 작업 트리 | 깨끗함. 마지막 커밋 `befae03` |
| 태그·릴리스 | **v0.5.0 태그를 GitHub 에 올렸다.** 릴리스는 아래 3절 확인 |
| 소개 사이트 | **운영 배포 완료** — `배경음악 깔기`·`영상 띠` 가 실제 주소에서 확인됨 |

> **점검을 돌릴 때 환경변수를 짝으로 정한다.** 주소만 바꾸면 디스크를 직접 보는 검사가
> 엉뚱한 폴더를 본다 (`memory/test-must-target-the-right-server.md`).
>
> ```powershell
> # 설치본(8765)을 겨냥할 때 — 둘 다 설정
> $env:MOVIEFIT_TEST_URL="http://127.0.0.1:8765"
> $env:MOVIEFIT_TEST_PROJECTS="$env:LOCALAPPDATA\Programs\MovieFit Studio\projects"
> # 개발 서버(8766)를 겨냥할 때 — PROJECTS 는 지운다 (기본값이 저장소 폴더다)
> ```

---

## 2. Phase 7 에서 만든 것

### 타임라인 영상 띠 (필름스트립)

- 영상의 화면들을 시간 순서로 늘어놓는다. `영상 띠` 체크상자로 켜고 끈다
- 낱장이 아니라 **가로로 이어붙인 그림 한 장** — FFmpeg 한 번, 요청 한 번
- 프로젝트 `cache/` 에 저장해 두 번째부터는 즉시 나온다
- 핵심 파일: `app/core/filmstrip.py` (신규), `/media/project/{id}/filmstrip`

### 배경음악 (없던 기능)

- 왼쪽 `배경음악` 칸 — 영상이 있는 프로젝트에서만 보인다
- 짧으면 되풀이, 길면 자름, 끝 2초는 서서히 줄임
- **자막을 새긴 뒤 두 번째 단계로** 얹는다. 자막은 `-vf`, 소리 섞기는 `-filter_complex`
  가 필요한데 FFmpeg 이 둘을 함께 쓰는 것을 거부하기 때문이다
- 핵심 파일: `audio_mix.add_background_music()`, `render._with_background_music()`
- 프로젝트 칸: `bgm_path` · `bgm_volume` (주인공 음원 `audio_path` 와 역할이 반대라 분리)

### 덕킹 세기 3단계

| 단계 | 말할 때 원본 크기 (실측) |
|---|---|
| 약하게 | 약 70% |
| 보통 | 약 37% (예전 값 그대로) |
| 많이 | 약 22% |

- `audio_mix.DUCK_LEVELS` 에 threshold·ratio·attack·release 가 함께 들어 있다
- **`release` 가 가장 크게 작용한다.** 이걸 안 바꾸면 '많이'가 '보통'보다 5%p 밖에 안 낮다

### 화면 손질

`?` → `사용설명서` · `영상 모드/대본 모드` → `자막 작업/나레이션 작업` ·
색 고르개 기본이 하늘색 · 글자 약 15% 키움 · 화면비 단추 높이 통일 ·
`시간 붙이기` 입력칸 통일 · `Phase 1에서 연결됩니다` 삭제 ·
설명서 스크롤 버그 수정 + `목차`·`↑ 맨 위로` 고리

---

## 3. 배포 상태 — **반드시 확인할 것**

| 단계 | 상태 |
|---|---|
| git push (main) | ✅ `befae03` |
| 태그 v0.5.0 | ✅ push 완료 |
| 소개 사이트 (Vercel) | ✅ 운영 배포 확인 |
| 설치 파일 만들기 | 🔄 이 문서를 쓰는 시점에 **압축 중** |
| GitHub 릴리스 | ⬜ **아직 안 했다** — 설치 파일이 나온 뒤에 해야 한다 |

**릴리스를 만드는 방법** (설치 파일이 `build/dist/MovieFitStudio-Setup.exe` 에 생긴 뒤):

```powershell
gh release create v0.5.0 "build/dist/MovieFitStudio-Setup.exe" `
  --title "v0.5.0 — 영상 띠 · 배경음악 · 덕킹 세기" --notes-file <노트파일>
```

확인: `gh api repos/parkjikoon-hub/moviefit-studio/releases/latest --jq .tag_name` 가
`v0.5.0` 이어야 한다. 소개 사이트의 내려받기 단추는 `releases/latest` 를 가리키므로
릴리스만 만들면 자동으로 새것을 가리킨다.

---

## 4. 점검 현황 — 430개 (14개 스위트)

| 점검 | 개수 | 무엇을 보나 |
|---|---:|---|
| `guide_test.py` | 32 | 설명서 세 곳이 어긋나지 않는가 |
| `smoke_test.py` | 46 | 서버 기본 기능 전반 |
| `errors_test.py` | 18 | 잘못된 입력에 **첫 글자가 한글인** 안내가 나오는가 |
| `filmstrip_test.py` | 14 | 영상 띠의 자리가 실제 시각과 맞는가 |
| `bgm_test.py` | 18 | 배경음악이 실제로 깔리는가 |
| `ducking_test.py` | 26 | 세기 3단계가 실제로 다른가 |
| `phase1_test.py` | 26 | 영상 → 자막 |
| `phase2_test.py` | 33 | 나레이션 (인터넷 필요) |
| `phase3_test.py` | 18 | 편집 완성도 |
| `phase4_test.py` | 46 | 화면비 |
| `phase6_test.py` | 47 | 사진·음원·강제정렬 |
| `shortcuts_test.py` | 21 | 단축키를 실제로 눌러 확인 |
| `ui_test.py` | 47 | 화면을 실제로 눌러 확인 |
| `phase6_ui_test.py` | 38 | 사진·음원 화면 |

따로 도는 것: `longvideo_test.py` (17개, **10~40분 걸림**)

시험용 파일은 저장소에 넣지 않는다. 없으면 만든다:

```
python tools/make_sample.py              색막대 영상 + 나레이션 견본
python tools/make_sample_images.py --count 30
python tools/make_sample_speech.py       말소리 영상 (인터넷 필요)
python tools/make_sample_long.py         30분 영상
```

`filmstrip_test` 와 `bgm_test` 는 시험 파일을 **스스로 만든다**.

---

## 5. 아직 안 된 것 (정직하게)

- **화면 효과 삽입** (비·눈·물방울·폭죽 등) — 사용자가 요청했으나 **착수하지 않았다.**
  범위가 커서 따로 조사와 설계가 필요하다. 아래 6절 참고
- **소재 칸 통합** — 영상·사진·음원을 한 칸에서 관리하자는 설계는 세웠으나,
  배경음악만 별도 칸으로 넣고 통합은 하지 않았다
- **Phase 4 의 마이크 녹음(F-45)·내 목소리 나레이션(F-46)** — 여전히 미착수.
  화면에 칸은 있으나 누르면 "엔진 검토를 마치는 대로 연결됩니다" 안내만 나온다
- **EXIF 회전·`.heic`** — 여전히 미시험/미지원
- **강제정렬 정확도** — 인공 음성 표본 하나뿐. 실제 강의 영상에서는 미지수
- **덕킹 세기를 귀로 확인하지 않았다.** 70/37/22% 는 **숫자로만** 쟀다
- **영상 띠·배경음악을 실제 사용자 파일로 써 보지 않았다.** 시험용 파일로만 확인했다
- `USER_GUIDE.md` 만 보고 처음 쓰는 사람이 끝까지 갈 수 있는지 **시험하지 못했다**

---

## 6. 다음에 할 만한 일

1. **사용자가 실제 영상으로 써 보기** — 지금 가장 값진 확인이다.
   특히 ① 덕킹 `약하게`/`많이` 가 귀에 다른가 ② 영상 띠가 장면 찾기에 쓸 만한가
2. **화면 효과** — 요청받은 기능. FFmpeg 만으로 비·눈을 만드는 것은 가능하지만
   (`noise`·`geq` 필터), 물방울이 터지거나 폭죽이 터지는 것은 **투명 배경 영상 소재**를
   겹치는 방식이 현실적이다. 소재를 어디서 구할지(라이선스 포함)가 첫 관문이다
3. **Phase 4 — 마이크 녹음과 내 목소리 나레이션**
   (`docs/RESEARCH_voice_cloning.md` 에 엔진 선정이 끝나 있다)
4. **소재 칸 통합**

---

## 7. 새 창에서 쓸 시작 프롬프트

```
capcut-studio-kit 프로젝트를 이어서 진행합니다.

먼저 다음을 읽어 주세요:
1) CLAUDE.md
2) memory/ 폴더 전체
3) handover_20260814_1240.md  ← 이 문서
4) docs/ROADMAP.md 의 Phase 7

지금 상태: Phase 7 까지 끝났고 v0.5.0 입니다.
설치본(8765)이 v0.5.0 으로 갱신되어 있습니다.
점검을 돌릴 때는 MOVIEFIT_TEST_URL 과 MOVIEFIT_TEST_PROJECTS 를 짝으로 맞춰 주세요.

할 일: (여기에 하고 싶은 것을 적어 주세요)
```
