설치본에 코드를 반영할 때는 **파일을 바꿔 넣는 것으로 끝나지 않는다. 반드시 다시 켜야 한다.** 화면 파일(html·css·js)은 요청할 때마다 디스크에서 읽으므로 새로고침만으로 반영되지만, 파이썬은 프로그램이 켜질 때 한 번만 읽는다.

무슨 일이 있었나 (2026-08-14, v0.5.0 작업):

**① 사용자가 "고쳤다는데 안 된다"고 했다.**
설명서 스크롤 버그를 고치고 점검까지 통과했는데, 사용자는 "지금도 스크롤링이 되지 않음"이라고
했다. 확인해 보니 사용자는 **설치본(8765)** 을 보고 있었고 내 수정은 저장소에만 있었다.
설치본은 설치 폴더 안의 **화면 파일 사본**을 쓴다.

    curl http://127.0.0.1:8765/guide.css | grep overflow   → 옛 내용
    curl http://127.0.0.1:8766/guide.css | grep overflow   → 고쳐진 내용

즉 **"어느 서버를 보고 있는가"를 먼저 물어야 했다.** 내 점검은 8766 을 겨냥했으니 통과가
맞았고, 사용자 화면은 8765 였으니 안 되는 것도 맞았다. 둘 다 옳았다.

**② 파일을 바꿔 넣었더니 이번에는 조용히 틀렸다.**
파이썬 파일(덕킹 세기)까지 복사한 뒤 브라우저를 열어 드리려 했는데, 프로세스 시작 시각을
보니 **어제 18:35 에 켜진 채**였고 새 파일은 오늘 10:48 에 넣은 것이었다.

    Get-NetTCPConnection -LocalPort 8765 -State Listen | ... | Get-Process | Select StartTime

그대로 열었으면 **새 화면(세기 단추 3개)이 옛 API 에 `duck_level` 을 보내고, 옛 코드는
모르는 값이라 조용히 버려서 세 단계가 전부 똑같이 나왔을 것이다.** 오류도 안 난다.
FastAPI 는 모르는 필드를 기본적으로 무시한다.

## 반영하는 순서

    1) 파일 복사 (원본은 백업해 둘 것)
    2) 설치 폴더의 __pycache__ 전부 삭제   ← 옛 바이트코드가 남지 않게
    3) 8765 프로세스 종료 후 MovieFitStudio.bat 으로 다시 켜기
    4) **새 코드가 올라왔는지 실제로 확인** — 새 코드에만 있는 동작을 불러 본다
       예) 잘못된 duck_level 을 보내 400 이 오는지 (옛 코드는 200 을 준다)
    5) 사용자에게 브라우저 Ctrl+F5 를 안내 (브라우저도 옛 파일을 들고 있다)

## 왜 중요한가

**둘 다 오류가 나지 않는다.** ①은 "고쳤는데 안 된다"는 소모적인 대화를 만들고,
②는 **새 기능이 있는 척하면서 아무 일도 하지 않는다** — 가장 나쁜 종류다.

4번을 건너뛰지 말 것. "파일을 복사했다"는 "새 코드가 돌고 있다"가 아니다.
[[test-must-target-the-right-server]] 와 같은 뿌리다 — 그쪽은 검사할 서버를 착각하는 것,
이쪽은 **이미 돌고 있는 프로그램이 무엇을 기억하고 있는지**를 착각하는 것이다.
