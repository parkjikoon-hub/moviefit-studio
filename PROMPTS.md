# PROMPTS — 복사해서 붙여넣는 프롬프트 모음

각 프롬프트는 코드 블록 안의 내용을 **통째로 복사**해서 Claude Code 입력창에 붙여넣으면 됩니다.
Fable 5 프롬프팅 가이드의 패턴(이유 제공, 경계 명시, 근거 있는 진행 보고, 자체 검증, 메모리, 조기 중단 방지)이 반영되어 있습니다.

---

## 프롬프트 0 — 킥오프 (맨 처음 1회)

```
나는 유튜브·홍보용 영상 제작에 쓰기 위해, CapCut의 자막 생성/수정 기능과 나레이션(TTS) 기능만을
전문으로 하는 나만의 프로그램 "SubCap Studio"를 만들고 있어. 완성되면 내가 매일 실무에 쓸 도구야.
나는 비개발자이고, 너는 이 프로젝트의 개발 전체를 맡는다.

먼저 이 프로젝트 폴더의 문서를 이 순서로 읽어라:
CLAUDE.md → docs/PRD.md → docs/TECH_SPEC.md → docs/UI_SPEC.md → docs/ROADMAP.md → memory/README.md
CLAUDE.md의 규칙(비개발자 대상 설명, 자체 검증, 메모리 시스템, 진행 보고 원칙)은 이 프로젝트 내내 유효하다.

읽은 뒤 곧바로 ROADMAP의 Phase 0을 시작해라. 행동 지침:
- 충분한 정보가 있으면 즉시 실행해라. 문서에 이미 확정된 결정을 다시 논의하지 말고,
  추구하지 않을 선택지를 내게 나열하지 마라. 선택이 필요하면 추천안 하나를 제시해라.
- 작업에 필요한 범위를 넘는 기능 추가, 리팩토링, 추상화를 하지 마라.
  잘 작동하는 가장 단순한 것을 만들어라.
- 내게 멈춰서 물어도 되는 경우는 세 가지뿐이다: 되돌릴 수 없는 작업, 실질적 범위 변경,
  나만 줄 수 있는 입력. 그 외 되돌릴 수 있는 작업은 묻지 말고 진행해라.
- 진행 상황을 보고하기 전에 각 주장을 이 세션의 실제 도구 결과와 대조해라.
  증거를 가리킬 수 있는 작업만 완료로 보고하고, 검증 안 된 것은 그렇다고 명시해라.
- 턴을 끝내기 전 마지막 문단을 점검해라. 그것이 계획이나 "이제 ~하겠습니다" 같은 약속이면,
  지금 그 작업을 실행해라. Phase 0 수용 기준이 통과되었거나 내 입력에 막혔을 때만 턴을 끝내라.
- 최종 보고는 결과부터 한 문장으로 말하고, 비개발자인 내가 따라할 실행 순서를 번호로 제시해라.
```

---

## 프롬프트 V — 단계 완료 검증 (각 Phase 끝날 때마다)

```
방금 끝낸 Phase의 수용 기준을 검증해라. 방법:
새로운 컨텍스트의 검증 서브에이전트를 띄워, docs/ROADMAP.md의 해당 Phase 수용 기준을
항목별로 "직접 실행해서" 확인하게 해라. 구현 설명을 믿지 말고 서버 기동, 실제 파일 입력,
결과물 열어보기까지 수행해야 한다. 결과는 항목별 통과/실패 표로 보고하고,
실패 항목은 수정한 뒤 같은 방식으로 재검증해라. 전 항목 통과 전에는 다음 Phase를 시작하지 마라.
이번 Phase에서 얻은 교훈(막혔던 지점, 확인된 접근법)이 있으면 memory/ 규칙에 따라 기록해라.
```

---

## 프롬프트 1 — Phase 1 시작

```
Phase 0 수용 기준이 전부 통과되었다. docs/ROADMAP.md의 Phase 1(영상→자막 MVP)을 시작해라.
음성인식 테스트에는 실제 사람 음성이 담긴 영상이 필요하니, 준비가 되면 나에게
"영상 파일을 프로젝트 폴더의 tests/sample/ 에 넣어달라"고 요청하고 그동안 다른 작업을 계속해라.
독립적인 하위 작업(백엔드 API와 프론트엔드 화면 등)은 서브에이전트에 위임하고 병렬로 진행해도 좋다.
서브에이전트가 궤도를 벗어나면 개입해라.
```

## 프롬프트 2 — Phase 2 시작

```
Phase 1 검증이 통과되었다. docs/ROADMAP.md의 Phase 2(나레이션과 대본 모드)를 시작해라.
이 단계의 핵심은 PRD의 차별점 D1이다: 자막 타이밍은 추정하지 말고 각 문장 오디오의
ffprobe 실측 길이로 계산해야 한다. edge-tts는 TECH_SPEC의 어댑터 구조(R1)를 지켜 구현해라.
```

## 프롬프트 3 — Phase 3 시작

```
Phase 2 검증이 통과되었다. docs/ROADMAP.md의 Phase 3(편집 완성도)을 시작해라.
스타일은 app/core/style_map.py 를 단일 출처로 삼아 플레이어 오버레이와 번인 결과가
일치하도록 해라 (TECH_SPEC R5).
```

## 프롬프트 4 — Phase 4 시작

```
Phase 3 검증이 통과되었다. docs/ROADMAP.md의 Phase 4(마감 품질)를 시작해라.
마지막에 비개발자용 사용 설명서 USER_GUIDE.md 를 작성하되, 스크린샷 없이도
따라할 수 있게 화면 위치를 말로 정확히 묘사해라.
```

---

## 보조 프롬프트

### 이어서 진행 (말만 하고 멈췄을 때)

```
계속. 끝까지 진행해.
```

### 오류 해결 (오류가 났을 때 — 괄호 안을 채워서)

```
아래 오류가 발생했다. 원인을 진단하고 수정한 뒤, 같은 조작을 다시 실행해서
오류가 사라졌는지 실제로 확인하고 결과를 보고해라. 수정 과정에서 얻은 교훈은 memory/에 기록해라.

[상황: 무엇을 하다가 났는지 — 예: 자막 자동 생성 버튼을 눌렀을 때]
[오류 메시지 전체 붙여넣기]
```

### 기능 수정/추가 요청 (괄호 안을 채워서)

```
다음 변경을 요청한다: [원하는 것].
이유: [왜 필요한지 — 예: 실제로 써보니 ~가 불편해서].
먼저 이 변경이 docs/PRD.md 범위 안인지 판단하고, 범위 밖이면 PRD 반영안을 먼저 제안해라.
범위 안이면 바로 구현하고, 관련 수용 기준을 실제 실행으로 재확인한 뒤 보고해라.
```

### 새 세션 인수인계 (대화가 너무 길어졌을 때, 기존 세션에서 실행)

```
이 세션이 길어졌다. 새 세션의 다른 클로드가 이어받을 수 있도록:
1) memory/ 를 최신 교훈으로 갱신하고,
2) HANDOVER.md 파일을 만들어 다음을 기록해라: 현재 완료된 Phase와 수용 기준 상태,
   진행 중이던 작업과 정확히 어디까지 했는지(파일·함수 단위), 다음에 할 일 목록,
   주의할 점. 파일 작성 후 나에게 "새 세션에서 HANDOVER.md를 읽고 이어가 달라고
   붙여넣을 문장"을 알려줘라.
```

### 새 세션 시작 (새 세션에서 실행)

```
이 프로젝트를 이어받아라. CLAUDE.md → memory/ 전체 → HANDOVER.md 순서로 읽고,
HANDOVER.md의 "다음에 할 일"부터 즉시 진행해라. 이미 완료·검증된 작업을 다시 하지 마라.
```
