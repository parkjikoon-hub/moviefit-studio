# 인수인계 — MovieFit Studio (Phase 6 완료 · v0.4.0 배포 완료)

> **v1.0** | 최종 업데이트: **2026-08-13 19:30** | 작성자: Claude Code

## 변경 이력

| 버전 | 일시 | 주요 변경 |
|------|------|---------|
| v1.0 | 2026-08-13 19:30 | 초기 작성. Phase 6(사진 영상·노래 영상·강제정렬) 7단계를 모두 만들고 v0.4.0 을 배포한 시점 |

> 앞 문서 `handover_20260813_1620.md` (v3.0) 은 **v0.3.0 배포까지**를 다룬다.
> 지금 상태는 이 문서가 맞다.

---

## 0. 한 문단 요약

**Phase 6 을 끝내고 v0.4.0 을 배포했다. 남은 큰 일은 Phase 4(내 목소리 나레이션)뿐이다.**
사진 여러 장으로 영상을 만들고, 노래에 가사와 사진을 얹고, 내가 가진 대본에 시간을
자동으로 붙이는 세 기능이 들어갔다. 시작 화면이 카드 4개로 바뀌었다.
점검은 248개 → **333개**로 늘었고, 설치본을 겨냥해 전부 다시 돌려 통과를 확인했다.

---

## 1. 지금 컴퓨터 상태 (새 창에서 제일 먼저 볼 것)

| 항목 | 상태 |
|---|---|
| 설치본 | **8765 포트에 떠 있음 · v0.4.0 · 최신 코드** (해시로 50개 파일 전부 대조 확인) |
| 개발 서버 | **꺼져 있음** (설치할 때 껐다). 필요하면 `python -m app --port 8766` |
| 설치 파일 | `build/dist/MovieFitStudio-Setup.exe` (120.6MB, v0.4.0) |
| 작업 트리 | 깨끗함. 마지막 커밋 `aa93b8b` |
| 태그·릴리스 | **v0.4.0 배포 완료** (GitHub 최신 릴리스 = v0.4.0, Vercel 운영 반영 확인) |

> **점검을 돌릴 때 반드시 두 가지를 짝으로 정한다.** 주소만 바꾸면 디스크를 직접
> 들여다보는 검사가 엉뚱한 폴더를 본다 (`memory/test-must-target-the-right-server.md`).
>
> ```powershell
> # 설치본을 겨냥할 때
> $env:MOVIEFIT_TEST_URL="http://127.0.0.1:8765"
> $env:MOVIEFIT_TEST_PROJECTS="$env:LOCALAPPDATA\Programs\MovieFit Studio\projects"
> # 개발 서버를 겨냥할 때는 둘 다 지운다 (기본값이 저장소 폴더다)
> ```

---

## 2. Phase 6 에서 만든 것

### 사진으로 영상 만들기 (단계 1~4)

- 시작 화면 카드 `[사진으로 시작]` → 파일 창에서 **여러 장을 한 번에** 고른다
- 왼쪽 `[사진]` 칸 — 미리보기·시작 시각·표시 시간·순서 바꾸기(▲▼)·빼기(✕)
- 화면비를 고르는 것이 곧 영상 크기를 정하는 것이다 (9:16 → 1080×1920)
- 크기가 제각각인 사진을 **먼저 같은 크기로 맞춘 뒤** 이어붙인다
- 자막은 영상일 때와 똑같이 들어간다

핵심 파일: `app/core/slideshow.py` (신규), `app/core/framing.py` 의 `fit_filter()`

### 노래(음원) 영상 (단계 5)

- 시작 화면 카드 `[음원으로 시작]` → mp3
- `[가사 줄에 맞추기]` 로 사진↔가사를 순서대로 짝짓는다
- 영상 길이 = 음원 길이 (실측 12.000초 = 12.000초)
- 사진 수와 가사 줄 수가 다르면 **내보내기 전에** 무슨 일이 벌어지는지 알려 준다

### 내 대본에 시간 붙이기 (단계 6)

- 왼쪽 `[내 대본에 시간 붙이기]` 칸 + `[시간 붙이기]` 단추
- 글자는 내 대본, 시각만 음성인식에서 가져온다
- 짝을 못 찾은 줄은 **노란 띠와 ⚠** 로 표시하고 그 줄만 고치게 한다

핵심 파일: `app/core/align.py` (신규), `app/api/render.py` 의 `/align`

### 자막→대본 마무리 (단계 7)

- 옮기기 전에 "나레이션을 만들면 지금 자막 시각이 새로 계산된다"고 알린다
- 원본 소리 볼륨을 30% → **0%** 로 내린다 (두 목소리가 겹치지 않도록)

---

## 3. 반드시 알아야 할 함정 두 개 (이번에 직접 밟았다)

`memory/fps-filter-eats-the-first-images.md` 에 자세히 적었다. 요약하면:

| 무엇을 하면 | 무슨 일이 벌어지나 |
|---|---|
| 사진을 이어붙일 때 `fps` 필터를 **쓰면** | 사진 순서가 망가진다 (t=1·4·7초가 전부 3번째 사진) |
| 사진을 이어붙일 때 `fps` 필터를 **안 쓰면** | 자막이 **한 글자도** 안 들어간다 (사진 한 장은 프레임이 하나뿐이라 필터가 볼 순간이 없다) |

**둘 다 오류가 나지 않는다.** 그래서 두 번에 나눠 만든다 — ① 사진만으로 영상을 만들고
② 그 영상에 이미 검증된 `burn_subtitles()` 를 쓴다. 한 번 더 인코딩하는 값을 치른다.

이걸 잡아낸 것은 **결과 영상의 특정 시각 화면 색을 재어 몇 번째 사진인지 대조하는 검사**와
**자막 글만 비운 영상을 하나 더 만들어 픽셀 차이를 재는 검사**였다.
장마다 색이 다른 시험용 사진(`tools/make_sample_images.py`)이 없었으면 몰랐을 것이다.

---

## 4. 점검 현황 — 333개

| 점검 | 개수 | 무엇을 보나 |
|---|---|---|
| `tests/guide_test.py` | 32 | 설명서 세 곳이 어긋나지 않는가, 설명서가 실제 화면과 맞는가 |
| `tests/ui_test.py` | 47 | 브라우저로 실제로 눌러 확인 (화면비·화살표·저장·설명서) |
| `tests/smoke_test.py` | 46 | 서버 기본 기능 전반 |
| `tests/phase1_test.py` | 26 | 영상 → 자막 |
| `tests/phase2_test.py` | 33 | 나레이션 (인터넷 필요) |
| `tests/phase3_test.py` | 18 | 편집 완성도 |
| `tests/phase4_test.py` | 46 | 화면비 |
| **`tests/phase6_test.py`** | **47** | 사진 영상·음원 영상·강제정렬 (서버 쪽) |
| **`tests/phase6_ui_test.py`** | **38** | 사진·음원 화면을 브라우저로 실제로 눌러 확인 |

시험용 파일은 저장소에 넣지 않는다. 없으면 만든다:

```
python tools/make_sample_images.py --count 30   # 크기·형식·색이 제각각인 사진 30장
python tools/make_sample_speech.py              # 말소리가 든 영상 (인터넷 필요)
python tools/make_sample.py                     # 색막대 영상 + 나레이션 견본
```

> `sample_10s.mp4` 는 색 막대 + 사인파라 **말소리가 없다.** 강제정렬 점검에는
> `sample_speech.mp4` 를 쓴다.

---

## 5. 아직 안 된 것 (정직하게)

- **휴대폰 사진의 회전 정보(EXIF)를 시험하지 못했다.** 세로로 찍은 사진이 눕는지
  실제 휴대폰 사진으로 확인해야 한다. 릴리스 노트에도 적어 두었다
- **아이폰 `.heic` 는 못 읽는다.** 설명서 9-1 에 "jpg 로 바꿔 주세요"라고 안내했다
- **강제정렬 정확도는 가장 좋은 경우만 재 보았다.** 대본과 말소리가 정확히 같은
  표본에서 오차 중앙값 0.000초. 실제 영상(잡음·사투리·빠른 말)에서는 미지수다
- **사진 전환 효과(스르륵 바뀜)는 범위에서 뺐다.** 딱딱 바뀐다. `xfade` 는 입력을
  동시에 여러 개 열어야 해서 수십 장에서 메모리 부족으로 죽을 위험이 크다 (조사 2.5절)
- **「내 목소리로 나레이션」·마이크 녹음은 화면에만 있고 동작하지 않는다** (Phase 4 미착수)
- **독립 검증 서브에이전트를 띄우지 못했다.** 이번 세션은 서브에이전트 사용이 막혀
  있어서, 수용 기준을 전부 **실행 가능한 점검으로 바꿔** 직접 돌리는 것으로 대신했다.
  다음 세션에서 별도 컨텍스트의 검증자를 한 번 돌리면 좋다 — 지난번에 실제로
  결함 2건을 찾아냈다
- `USER_GUIDE.md` 만 보고 처음 쓰는 사람이 끝까지 갈 수 있는지 **시험하지 못했다**

---

## 6. 다음에 할 만한 일

1. **사용자가 실제 사진·노래로 한 번 만들어 보기** — 지금 가장 값진 확인이다.
   특히 휴대폰 사진(세로로 찍은 것)을 넣어 눕지 않는지 볼 것
2. **Phase 4 — 내 목소리 나레이션** (`docs/ROADMAP.md` Phase 4). 마이크 녹음이
   전제이고, TTS 엔진은 어댑터 뒤에 선택 설치로 붙이기로 되어 있다
3. **사진 전환 효과** — 하려면 따로 조사가 필요하다 (조사 2.5절이 방향을 적어 두었다)
4. **강제정렬 정확도 실측** — 실제 강의 영상 하나로 재 보고, 쓸 만하지 않으면
   "완전 자동"이 아니라 "두드려 맞추기 보조"로 자리를 낮추는 것을 다시 논의

---

## 7. 새 창에서 쓸 시작 프롬프트

```
capcut-studio-kit 프로젝트를 이어서 진행합니다.

먼저 다음을 읽어 주세요:
1) CLAUDE.md
2) memory/ 폴더 전체
3) handover_20260813_1930.md  ← 이 문서
4) docs/ROADMAP.md

지금 상태: Phase 6 까지 끝났고 v0.4.0 이 배포되어 있습니다.
설치본(8765)이 떠 있고 개발 서버는 꺼져 있습니다.
점검을 돌릴 때는 MOVIEFIT_TEST_URL 과 MOVIEFIT_TEST_PROJECTS 를
짝으로 맞춰 주세요.

할 일: (여기에 하고 싶은 것을 적어 주세요)
```
