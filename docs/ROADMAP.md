# ROADMAP — 개발 단계와 수용 기준

> **v2.1** | 최종 업데이트: **2026-08-13 12:15** | 작성자: Claude Code

## 변경 이력

| 버전 | 일시 | 주요 변경 |
|------|------|---------|
| v1.0 | 2026-08-11 22:46 | 초기 작성 (Phase 0~4) |
| v2.1 | 2026-08-13 12:15 | 사용자 요청 기능 4건(F-A 화면비 전환 / F-B 위치 화살표 / F-C 롱폼·숏폼 / F-D 저장됨 클릭) 완료와 검증 결과 기록. 브라우저 자동화 검증 도입 |
| v2.0 | 2026-08-12 17:40 | 제품명 MovieFit Studio 확정. 사용자 요청으로 범위 확대 — 미뤄 두었던 항목(F-12/22/23/43/44/04/53)과 제외했던 항목(F-45 마이크 녹음, F-13 단어 타임스탬프, 자동 덕킹)을 모두 정식 범위로 편입. 신규 기능 추가: 내 목소리 나레이션(F-46), 자막 자유 위치 배치(F-33), 자막 타임라인(F-25), 읽기 속도 검사(F-26). Phase 0 완료 표시 및 실제 검증 결과 기록. |

각 Phase는 **앞 Phase의 수용 기준이 전부 통과된 뒤에만** 시작한다. 수용 기준 검증은 검증 서브에이전트가 직접 실행해서 확인한다 (CLAUDE.md의 자체 검증 규칙).

---

## Phase 0 — 환경 준비와 뼈대 ✅ 완료 (2026-08-12 검증)

**목표**: 사용자 PC에서 "run.bat 더블클릭 → 브라우저에 화면"까지.

수용 기준 — 검증 결과:
- [x] `python tools/check_env.py` 전 항목 정상 출력 — 파이썬 3.11.9, FFmpeg 8.1, 패키지 6종, 폰트 2종, 샘플 영상 모두 확인
- [x] `run.bat` 실행으로 브라우저가 열리고 시작 화면이 보임 — 서버 로그에 브라우저의 실제 요청 기록 확인
- [x] 프로젝트를 만들고 서버를 껐다 켜도 목록에 남아 있음 — 재시작 실험으로 확인
- [x] `tests/sample/`에 재생 가능한 샘플 영상 존재 — 10초 mp4 생성, 스트리밍 확인
- [x] `python tests/smoke_test.py` 33개 항목 통과

Phase 0에서 함께 완료한 것 (원래 계획보다 앞당김):
- PWA 아이콘 8종 + manifest + 서비스 워커 (브라우저에서 앱처럼 설치 가능)
- 자막 스타일 단일 출처 `app/core/style_map.py` — 프리셋 5종, CSS↔ASS 매핑, 자유 위치
- 목소리 목록 API — edge-tts 322개 중 **한국어 가능 14개** 선별, 미리듣기 실제 동작 확인
- 우측 패널 전체 (글꼴·크기·자간·줄간격·정렬·굵게·기울임·색·외곽선·그림자·배경·위치·줄바꿈 규칙·프리셋 저장)
- 하단 자막 타임라인 띠 + 목록 편집기 병행 구조

---

## Phase 1 — 영상→자막 MVP (시나리오 A) ✅ 완료 (2026-08-12 독립 검증)

**목표**: 영상을 넣고 자막을 자동 생성해 목록에서 고친 뒤 SRT로 내보낸다.

작업: F-01, F-02, F-03, F-10, F-11, F-20, F-21, F-24, F-25, F-26, F-50, F-54, 진행률(jobs)

수용 기준 — 검증 결과 (별도 컨텍스트의 검증 에이전트가 직접 실행):
- [x] 사람 음성이 든 실제 영상에서 자막이 자동 생성되고 진행률이 표시된다 — 한국어 음성을 정확히 인식, 진행률 1→3→8→100 관측
- [x] 세그먼트 텍스트를 고치면 2초 내 자동 저장되고 새로고침해도 유지된다 — API 응답과 디스크의 project.json으로 확인
- [x] 병합/분할/삭제/±0.1초 넛지가 동작하고 오버레이에 즉시 반영된다 — **코드 검토로 판정** (브라우저 자동화 없음). 검증에서 발견된 넛지 반올림 결함은 수정 완료
- [x] 타임라인 막대를 끌어 시간을 조절하면 목록과 오버레이가 함께 갱신된다 — **코드 검토로 판정**
- [x] 읽기 속도가 빠른 자막이 노란색/빨간색으로 표시된다 — **코드 검토로 판정**. 타임라인과 목록의 색 기준이 어긋나던 것 수정 완료
- [x] SRT 파일이 올바른 형식으로 열리고 유튜브와 호환된다 — 검증에서 **데이터 손실 결함 발견 후 수정**, 재검증 통과
- [x] 자막 번인 mp4에서 한글이 깨지지 않는다 — 영상 프레임을 뽑아 **눈으로 확인**. 값어치·꽃잎·읽다·뚫었다 모두 정상, 네모 0개
- [x] 서버 콘솔에 처리되지 않은 예외가 없다 — 로그 248줄에 HTTP 500 0건

검증에서 발견해 고친 결함:
1. **SRT/VTT 내보내기가 중괄호 `{}` 안의 글자를 소리 없이 삭제** — pysubs2가 ASS 서식 명령으로 오해. 직접 쓰기로 변경 (읽기는 계속 pysubs2 사용)
2. **사용자가 직접 넣은 줄바꿈이 SRT에서 사라짐** — `text.split()`이 줄바꿈을 먹었다. 줄 단위로 먼저 나눈 뒤 각각 줄바꿈하도록 수정
3. **화면·SRT·번인의 줄바꿈 규칙이 제각각** — 세 곳 모두 같은 `wrap_text` 규칙을 쓰도록 통일. 화면이 넘치는 줄을 잘라내 감추던 것도 제거
4. **넛지 ±0.1초가 실제로는 0.1초 격자에 스냅** — 1.54초에서 ▶를 누르면 1.6초가 되던 것을 1.64초로
5. **타임라인은 노랑, 목록은 빨강** — 같은 기준에 다른 색이던 것 통일

남은 한계: 마우스로 하는 동작 3건은 브라우저 자동화가 없어 코드 검토로만 판정했다.
실사용 중 이상이 발견되면 이 항목부터 의심할 것.

---

## Phase 2 — 나레이션과 대본 모드 (시나리오 B, 킬러 기능 D1) ✅ 기능 완성 (2026-08-12, 독립 검증 대기)

**목표**: 대본 → 나레이션 오디오 + 완벽 동기화 자막.

작업: F-40, F-41, F-42, F-43, F-44, F-51, F-52 (+ 자동 덕킹을 앞당겨 구현)

수용 기준 — `tests/phase2_test.py` 27개 항목 전부 통과:
- [x] 대본을 붙여넣고 나레이션을 생성하면 문장별 mp3와 이어붙인 전체 오디오가 생성된다
- [x] 자막 타이밍이 각 문장 오디오의 **실측 길이**로 산출되어 소리와 어긋나지 않는다
      — 자막 길이와 음성 길이의 차이 **최대 0.0ms**, 이어 붙이기 오차 **0.0ms**
      (120문장 스트레스 시험에서도 누적 오차 1.0ms)
- [x] 음성 미리듣기가 3초 내 재생된다 *(Phase 0에서 선행 구현)*
- [x] 한 문장만 고쳐 재생성하면 그 문장만 다시 만들어지고 이후 타이밍이 자동 재계산된다
      — 문장 하나를 길게 바꾸자 전체 끝이 18.48초 → 22.25초로 자동 이동
- [x] 자막을 대본으로 옮기는 버튼이 동작한다 (F-44)
- [x] 영상에 나레이션을 입힐 때 원본 소리 볼륨 조절이 반영된다 — 30% 설정이 결과에 반영됨
- [x] 나레이션 오디오 단독(mp3) 내보내기가 된다
- [x] edge-tts 호출 실패 시 프로그램이 죽지 않고 한국어 안내가 뜬다

설계상 중요한 결정:
- 자막 시각을 **계산값이 아니라 이어 붙인 결과 파일에서 각 문장이 실제로 시작하는 위치**로 확정한다.
  두 번 계산하지 않으므로 어긋날 여지가 없다.
- 문장을 이어 붙일 때 `concat` 방식 대신 **절대 위치 배치(adelay+amix)** 를 쓴다.
  mp3 인코더가 문장마다 넣는 미세한 여백이 뒤로 갈수록 누적되는 것을 막는다.
- `amix`에 `normalize=0` 을 준다. 이게 없으면 "원본 30%"가 실제로는 15%가 된다.
- 나레이션이 영상보다 길면 뒷부분이 잘리므로, 렌더링 전에 몇 초가 잘리는지 미리 경고한다.

독립 검증 결과 (별도 컨텍스트, 순환 논리를 깨는 방식으로 확인):
- 만들어진 나레이션 오디오를 **음성인식에 다시 통과시켜** 실제 발화 시각을 프로그램 바깥에서 구한 뒤
  저장된 자막 시각과 대조 — **누적 드리프트 없음** 확인 (첫 문장과 마지막 문장의 어긋남이 같음)
- 문장 재생성은 파일 수정시각·해시로 확인 — 해당 문장 파일만 바뀌고 나머지는 그대로
- 원본 볼륨 0%/30%/100%를 실제 음량으로 측정 — −38.1 / −32.9 / −23.9 dB (이론값과 일치)

검증에서 발견해 고친 결함:
1. **문장 사이 쉼이 설정값과 1.1초 어긋남** — edge-tts가 문장 앞에 0.2초, 뒤에 0.9초씩
   무음을 붙여 주는데 그것까지 '말하는 길이'로 세고 있었다. 음성 생성 직후 앞뒤 무음을
   잘라 내도록 수정. 실제 정적 1.42초 → **0.42초**, 자막과 소리의 어긋남 0.19초 → **0.06초**,
   같은 대본의 전체 길이 18.5초 → 13.5초 (죽은 공백 제거)
2. **잘림 경고가 내보내기 직전에 안 뜸** — 나레이션을 만든 직후에만 알려서, 나중에 다시 열어
   내보내면 잘리는 줄 모르고 진행됐다. 내보내기 대화상자를 열 때 다시 확인하고,
   영상으로 내보낼 때는 한 번 더 물어보도록 수정
3. **자체 테스트의 D1 검증이 동어반복이었음** — "자막 길이 == 음성 길이"는 자막 길이를
   음성 길이로 계산했으니 당연히 같다. 실제 오디오 파일에서 소리 나는 구간을 따로 재어
   자막 시각과 대조하도록 교체. F-44와 음성 생성 실패 처리 검사도 추가

재검증: `tests/phase2_test.py` **33개 항목** 통과 (전체 105개 항목 통과)

---

## Phase 3 — 편집 완성도 ✅ 기능 완성 (2026-08-12, 독립 검증 대기)

**목표**: 사전, 찾기/바꾸기, 실행 취소, 스타일 번인 일치.

작업: F-12, F-22, F-23, F-30, F-31, F-32, F-33, F-04, F-53

수용 기준 — `tests/phase3_test.py` 18개 항목 통과:
- [x] 프리셋 5종이 오버레이와 번인 결과 양쪽에 일관되게 적용된다
      — 검은 배경에 새겨 글자 픽셀 수를 측정: 기본 935 / 예능 1526 / 뉴스 517 / 미니멀 448 / 강조 2111.
      다섯 프리셋이 실제로 서로 다른 결과를 만든다
- [x] **마우스로 끌어 놓은 자막 위치가 번인 결과에서도 같은 자리에 나온다 (F-33)**
      — 글자가 찍힌 픽셀의 무게중심을 계산해 설정값과 대조.
      (25%, 30%) 지정 → 실제 (24.8%, 29.8%) / (75%, 60%) 지정 → 실제 (74.8%, 59.8%).
      **오차 0.2%**
- [x] 위치 프리셋(위/가운데/아래)도 의도한 높이에 나온다 — 14.7% / 49.8% / 84.9%
- [x] 사용자 사전에 규칙을 넣고 자막을 다시 만들면 자동 교정이 적용된다 (F-12)
      — "안녕하세요 → 반갑습니다" 규칙이 인식 결과에 실제로 반영됨
- [x] SRT/VTT 가져오기가 동작한다 (F-04) — 내보낸 파일을 지웠다가 다시 읽어 복구 확인.
      화면에 [SRT · VTT 파일 불러오기] 버튼 추가, 파일 선택 창이 자막 파일만 걸러 보여 준다
- [x] 내보내기 진행률이 표시되고 완료 후 [폴더 열기]가 동작한다 (F-53)
- [~] 찾기/바꾸기로 특정 단어를 전체 교체할 수 있다 (F-22) — **코드 검토로만 판정**
- [~] Ctrl+Z로 직전 편집이 되돌려진다 (F-23) — **코드 검토로만 판정**

위치 측정 방법: 검은 배경 영상에 자막을 새긴 뒤 밝은 픽셀(=글자)의 무게중심을 구해
설정값과 비교한다. "코드가 그렇게 되어 있다"가 아니라 "결과물이 실제로 그렇다"를 확인하는 방식이다.

---

## 사용자 요청 기능 4건 ✅ 완료 (2026-08-13, 커밋 b852f2d)

Phase 사이에 사용자가 실사용 중 요청한 것들이다. 화면비(F-A·F-C)가 셋을 하나로 묶는다.

작업: F-A(세로↔가로 전환), F-B(자막 위치 화살표), F-C(롱폼·숏폼), F-D(저장됨 클릭)

수용 기준 — 검증 결과:
- [x] `[가로 16:9] [세로 9:16] [정사각 1:1]`이 **서로 오갈 수 있다** (한 방향이 아니다)
      — 브라우저에서 실제로 눌러 확인. 9:16 → 16:9 → 1:1 → 원본 모두 왕복
- [x] 자막 위치 `↑↓←→`가 1%씩, `Shift`와 함께 5%씩 움직인다
      — 실측 80.0 → 79.0 (1%), 51.0 → 46.0 (5%). 화면의 자막도 함께 움직임을 확인
- [x] 미리보기 틀이 고른 화면비로 바뀌고, 잘릴 부분이 어둡게 보인다
      — 화면에 그려진 틀 404픽셀 = 서버 계산 404픽셀. 틀 바깥 `rgba(0,0,0,.58)` 확인
- [x] 미리보기 틀을 끌어 잘라낼 자리를 정할 수 있다
      — 120픽셀 끌자 50% → 67.29%, 옆의 막대도 같은 값으로 따라옴
- [x] **실제로 잘라 저장된다** — 내보낸 mp4를 ffprobe로 실측:
      1280×720 → 9:16 잘라내기 **404×720** / 1:1 **720×720** / 9:16 여백채우기 **720×1280**
- [x] 영상 내보내기 3종 모두에 적용된다 (자막 영상·10초 미리보기·나레이션 영상)
      — 나레이션 영상도 404×720 으로 나오는 것을 확인
- [x] 화면비를 바꿔도 자막이 잘려 나가지 않는다
      — **자막 글만 비운 같은 영상과 비교**해 늘어난 밝은 픽셀을 셈 (2,522~8,569개)
- [x] "저장됨"을 누르면 즉시 저장된다
      — 눌렀을 때 디스크의 `updated_at`이 실제로 바뀌고 내용도 반영됨을 확인
- [x] 옛 프로젝트(`output` 키가 없는 것)도 그대로 열린다 — 기본값이 '원본 그대로'

설계상 중요한 결정:
- 화면비 계산은 `app/core/framing.py` 가 단일 출처다. 다만 **화면(`app.js`)도 같은 계산을
  갖고 있다** — 미리보기 틀은 마우스를 끄는 동안 즉시 다시 그려야 해서 서버에 물어볼 수
  없기 때문이다. 두 곳이 어긋나는 것을 막으려고 `tests/phase4_test.py`가 **`app.js` 파일을
  그대로 node로 실행해** 240가지 경우를 대조한다.
- 자르기 필터는 자막 필터보다 **먼저** 와야 하고, 자막 파일(ASS)은 **잘라낸 뒤의 크기**로
  만들어야 한다. 원본 크기로 만들면 글자 크기와 위치가 전부 어긋난다.
- 나레이션 영상은 화면비를 바꿀 때만 재인코딩한다. 바꾸지 않으면 지금까지처럼 화면을
  그대로 복사해 빠르다.

**검증 방식이 달라졌다**: 이번부터 브라우저 자동화(Playwright)로 **실제로 눌러서** 확인한다
(`tests/ui_test.py`, 33개 항목). Phase 1·3에 남아 있는 "코드 검토로만 판정" 항목들도
이 방식으로 옮길 수 있다. 자세한 방법은 `memory/verify-ui-by-actually-clicking.md`.

---

## Phase 4 — 내 목소리와 고급 기능

**목표**: 사용자 본인 목소리로 나레이션, 마이크 녹음, 자동 덕킹.

작업: F-13, F-45, F-46, 자동 덕킹(F-51 P2)

- **F-45 마이크 녹음** — 브라우저 MediaRecorder로 녹음해 프로젝트에 저장. 목소리 등록의 전제이자, 직접 나레이션을 녹음하려는 사용자를 위한 기능.
- **F-46 내 목소리 나레이션** — 등록한 목소리 샘플로 나레이션 생성. 엔진 선정은 `docs/RESEARCH_voice_cloning.md`의 결론을 따른다. `TTSEngine` 어댑터 뒤에 붙이며, 용량이 크므로 **선택 설치**로 분리한다.
- **F-13 단어 단위 타임스탬프 재분할** — Whisper의 단어별 시각을 이용해 자막을 더 자연스러운 지점에서 끊는다.
- **자동 덕킹** — 나레이션이 나오는 구간에서만 원본 소리를 자동으로 줄인다 (`sidechaincompress`).

수용 기준:
- [ ] 마이크로 10초 녹음 → 저장 → 재생이 된다
- [ ] 등록한 목소리로 만든 나레이션이 원래 목소리와 알아들을 수 있게 닮았다 (사용자 청취 확인)
- [ ] 목소리 등록 화면에 "본인 목소리만 등록" 안내가 명시되어 있다
- [ ] 선택 설치가 실패해도 기본 나레이션(edge-tts)은 정상 동작한다
- [ ] 덕킹을 켜면 나레이션 구간의 원본 소리가 실제로 줄어든다 (파형 확인)

---

## Phase 5 — 마감 품질

작업: 단축키 전체(UI_SPEC 3절), 오류 메시지 전수 한국어화, 긴 영상(30분+) 성능 점검, `USER_GUIDE.md`

수용 기준:
- [ ] UI_SPEC 3절의 단축키가 모두 동작한다
- [ ] 30분 영상에서 STT·렌더링이 완료되고 진행률이 정확하다
- [ ] 없는 파일·빈 대본·지원 안 되는 형식을 넣어도 죽지 않고 한국어 안내가 뜬다
- [~] `USER_GUIDE.md`만 보고 처음 쓰는 사람이 시나리오 A·B를 완료할 수 있다
      — **문서는 2026-08-12 17:40에 작성했다** (`docs/USER_GUIDE.md`, 소개 사이트의
      `/guide` 페이지에도 같은 내용). 화면에 실제로 있는 항목·단축키만 적었는지는
      `app/static/index.html`에서 뽑아 대조했다. 다만 **처음 쓰는 사람이 이 문서만 보고
      끝까지 갈 수 있는지는 아직 시험하지 못했다.** 그 확인이 끝나야 [x]로 바꾼다.
