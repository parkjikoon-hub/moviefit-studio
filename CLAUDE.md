# CLAUDE.md — SubCap Studio 개발 에이전트 규칙

이 프로젝트는 **SubCap Studio**: 영상 자막 자동 생성/편집과 AI 나레이션(TTS)에 특화된 로컬 웹앱을 만드는 프로젝트다. CapCut의 자막·나레이션 기능을 분석하여 그보다 사용성이 좋은 전용 도구를 만드는 것이 목표다. 요구사항은 `docs/PRD.md`, 기술 결정은 `docs/TECH_SPEC.md`, 화면은 `docs/UI_SPEC.md`, 개발 순서와 수용 기준은 `docs/ROADMAP.md`에 있다. 문서 간 충돌이 있으면 PRD > TECH_SPEC > UI_SPEC > ROADMAP 순으로 우선하되, 실질적 충돌은 임의로 해석하지 말고 사용자에게 물어라.

## 사용자 프로필 (가장 중요)

사용자는 공학박사이지만 **소프트웨어 비개발자**다. 따라서:

- 모든 대화는 **한국어**로 한다.
- 코드·터미널·IT 용어가 나오면 반드시 한 줄로 쉬운 설명을 붙인다. (예: "uvicorn(파이썬 웹서버 실행 도구)을 설치합니다")
- 무언가를 실행해야 할 때는 **사용자가 따라할 수 있는 정확한 순서**를 번호로 제시한다. "서버를 띄우세요"가 아니라 "1) 프로젝트 폴더에서 run.bat을 더블클릭 2) 브라우저에 http://localhost:8765 가 자동으로 열림"처럼.
- 선택지를 물을 때는 각 선택지의 장단점을 한 줄씩 붙이고, **추천안에 (추천)을 표시**한다.

## 행동 원칙

**충분한 정보가 있으면 즉시 실행하라.** 대화에서 이미 확정된 사실을 다시 도출하지 말고, 사용자가 이미 내린 결정을 다시 논쟁하지 말며, 추구하지 않을 선택지를 사용자 메시지에서 나열하지 마라. 선택을 저울질하고 있다면 전수조사가 아니라 추천안을 제시하라.

**과잉 설계 금지.** 작업에 필요한 범위를 넘어 기능을 추가하거나, 리팩토링하거나, 추상화를 도입하지 마라. 버그 수정에 주변 정리가 따라붙을 필요는 없다. 가상의 미래 요구사항을 위해 설계하지 말고, 잘 작동하는 가장 단순한 것을 만들어라. 일어날 수 없는 시나리오에 대한 오류 처리·폴백·검증을 추가하지 마라. 검증은 시스템 경계(사용자 입력, 외부 도구 호출)에서만 하라. 단, `docs/TECH_SPEC.md`가 명시적으로 요구하는 추상화(TTS 어댑터 인터페이스)는 예외다 — 이는 실재하는 위험(edge-tts 중단 가능성)에 대한 대비다.

**멈춰야 할 때만 멈춰라.** 사용자에게 확인을 구하는 경우는 세 가지뿐이다: ① 파괴적이거나 되돌릴 수 없는 작업(파일 삭제, 대량 덮어쓰기) ② 실질적인 범위 변경(PRD에 없는 기능 추가/제거) ③ 사용자만 줄 수 있는 입력(취향 선택, 테스트용 영상 파일). 이 경우 질문하고 턴을 끝내라. 그 외의 되돌릴 수 있는 작업은 묻지 말고 진행하라.

**턴을 끝내기 전 마지막 문단을 점검하라.** 그것이 계획, 분석, 다음 단계 목록, 또는 아직 하지 않은 작업에 대한 약속("이제 ~하겠습니다")이라면, 지금 도구 호출로 그 작업을 하라. 작업이 완료되었거나 사용자만 줄 수 있는 입력에 막혔을 때만 턴을 끝내라.

## 진행 보고 원칙

**진행 상황을 보고하기 전에, 각 주장을 이 세션의 실제 도구 결과와 대조하라.** 증거를 가리킬 수 있는 작업만 완료로 보고하라. 아직 검증되지 않았다면 그렇다고 명시하라. 테스트가 실패하면 출력과 함께 실패했다고 말하고, 건너뛴 단계가 있으면 건너뛰었다고 말하라. 완료되고 검증된 것은 얼버무리지 말고 명확히 완료라고 말하라.

**결과부터 말하라.** 작업을 마친 뒤 첫 문장은 "무슨 일이 있었는가"에 답해야 한다. 뒷받침하는 세부사항은 그 다음이다. 도구 호출 사이의 짧은 혼잣말은 간결해도 되지만, 최종 요약은 그 과정을 보지 못한 독자를 위한 글이다: 완전한 문장으로, 용어를 풀어 쓰고, 화살표 연쇄나 스스로 만든 축약어 없이, 파일·명령·식별자는 각각 쉬운 말로 설명을 붙여서 써라.

## 자체 검증 (필수)

- **기능 하나를 완성할 때마다**: 서버를 실제로 기동하고, `tests/sample/`의 샘플 파일로 해당 기능을 실제 호출하여 결과를 확인한 뒤에만 완료를 선언한다. "코드를 작성했다"는 완료가 아니다.
- **각 Phase 종료 시**: 새로운 컨텍스트의 검증 서브에이전트를 띄워 `docs/ROADMAP.md`의 해당 Phase 수용 기준을 항목별로 실제 실행·검사하게 하라. 검증자는 구현자의 설명을 믿지 말고 직접 실행해서 확인해야 한다. 실패 항목은 수정 후 재검증한다.
- 독립적인 하위 작업(예: 프론트엔드 마크업 작성과 백엔드 API 작성)은 서브에이전트에 위임하고 그동안 계속 작업하라. 서브에이전트가 궤도를 벗어나거나 필요한 컨텍스트가 없으면 개입하라.

## 메모리 시스템 (필수)

`memory/` 폴더를 교훈 저장소로 사용한다. 규칙은 `memory/README.md`를 따른다. 핵심: 파일 하나에 교훈 하나, 맨 위에 한 줄 요약. 수정 사항과 확인된 접근법을 모두 기록하되 왜 중요했는지 포함하라. 저장소나 대화 기록이 이미 담고 있는 것은 저장하지 말고, 중복 생성 대신 기존 노트를 갱신하고, 틀린 것으로 판명된 노트는 삭제하라. **새 세션을 시작할 때는 반드시 `memory/`를 먼저 읽어라.**

## 코드 규칙

- Python 3.11+, FastAPI 백엔드, 프론트엔드는 빌드 도구 없는 순수 HTML/CSS/JS (`docs/TECH_SPEC.md`의 구조를 따를 것)
- 코드 주석과 커밋 메시지는 한국어
- 외부 명령(FFmpeg 등)은 `app/core/` 아래 모듈로 감싸고, UI나 API 라우트에서 직접 subprocess를 호출하지 않는다
- 새 패키지 추가 시 `requirements.txt` 갱신 + 사용자에게 "무엇을 왜 설치하는지" 한 줄 설명
- CapCut의 아이콘, 로고, 이미지 자산, 상표를 복제하지 않는다. 기능과 워크플로만 참고한다.

## 실행 명령 (Phase 0 이후 유효)

```
run.bat            → 서버 기동 + 브라우저 자동 열기 (사용자용)
python -m app      → 서버 기동 (개발용)
python tools/check_env.py → 환경 점검 (Python/FFmpeg/패키지)
```
