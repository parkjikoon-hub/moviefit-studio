# 인수인계 — MovieFit Studio (목소리 버그 수정 · 화면 효과 조사 완료)

> **v1.1** | 최종 업데이트: **2026-08-14 16:20** | 작성자: Claude Code

## 변경 이력

| 버전 | 일시 | 주요 변경 |
|------|------|---------|
| v1.1 | 2026-08-14 16:20 | **4-A절 추가** — 사용자가 말로 지시한 두 기능(화면 강조 세기 · 영상 확대)의 정확한 내용. 새 창이 이 지시를 모른다는 지적에 따라 명시함 |
| v1.0 | 2026-08-14 16:07 | 초기 작성. 목소리 버그를 고쳐 설치본에 반영하고, 화면 효과 조사를 마친 시점 |

> 앞 문서 `handover_20260814_1240.md` 는 **v0.5.0 배포까지**를 다룬다. 그 뒤의 일은 이 문서가 맞다.

---

## 0. 한 문단 요약

**목소리가 대부분 안 들리던 버그를 고쳐 설치본까지 반영했고, 화면 효과(비·눈·물방울·폭죽)
조사를 마쳤다.** 화면 효과는 넷 다 만들 수 있고 바깥 소재가 필요 없다는 것이 실측으로
확인됐다. 목소리 품질(앵커·친근함 같은 말투)은 조사만 끝났고 **아직 아무것도 만들지 않았다.**
ElevenLabs 를 붙이는 설계는 **사용자 승인 대기 중**이다. **커밋하지 않았다.**

---

## 1. 지금 컴퓨터 상태

| 항목 | 상태 |
|---|---|
| 설치본 (8765) | **켜져 있고, 목소리 수정이 반영되어 있다** (PID 는 매번 바뀜, 16:07 기준 29384) |
| 개발 서버 (8766) | **꺼 두었다** |
| 작업 트리 | **커밋 안 된 변경 9건** (아래 2절). 마지막 커밋 `71a3e12` |
| 설치본 백업 | `%TEMP%\moviefit_backup_20260814_1514` (덮어쓰기 전 원본 4개) |

> **점검을 돌릴 때 환경변수를 짝으로 정한다.**
> ```powershell
> # 설치본(8765) — 둘 다 설정
> $env:MOVIEFIT_TEST_URL="http://127.0.0.1:8765"
> $env:MOVIEFIT_TEST_PROJECTS="$env:LOCALAPPDATA\Programs\MovieFit Studio\projects"
> # 개발 서버(8766) — PROJECTS 는 지운다
> ```

---

## 2. 커밋하지 않은 변경 (9건)

```
 M app/api/tts.py                             미리듣기가 목소리 언어에 맞는 견본을 쓰게
 M app/core/tts/base.py                       SAMPLE_TEXTS(20개 언어) + 언어 판별 함수들
 M app/core/tts/edge.py                       오류 문구를 원인별로 갈라 씀
 M app/static/app.js                          견본 문장 사본 제거
?? tests/voice_language_test.py               새 점검 26개
?? docs/RESEARCH_screen_effects.md            화면 효과 조사 보고서
?? memory/ffmpeg-effect-layer-traps.md        FFmpeg 효과 레이어 함정 4가지
?? memory/tts-voice-must-match-text-language.md  목소리-언어 불일치 교훈
?? tools/check_elevenlabs.py                  ElevenLabs 계정 점검 도구 (아직 안 돌려 봄)
?? 목소리_들어보기/                            한국어 가능 목소리 14개 견본 (커밋하지 말 것)
```

> `목소리_들어보기/` 와 `화면효과_데모.mp4` 는 **저장소에 넣지 말 것.** `.gitignore` 가
> `/*.mp3` `/*.mp4` 를 막고 있지만 폴더 안의 mp3 는 안 막힌다. 커밋 전에 확인할 것.

---

## 3. 고친 것 — 목소리가 대부분 안 들리던 버그 ✅ 완료·검증됨

**증상**: 사용자가 목소리 목록에서 듣기를 누르면 대부분 이 오류가 났다.

    나레이션을 만들지 못했습니다. 인터넷 연결을 확인하고 다시 시도해 주세요. (상세: NoAudioReceived)

**원인**: 미리듣기가 어떤 목소리에게든 **한국어 문장을 고정으로** 보내고 있었다.
한국어를 읽을 수 있는 목소리는 322개 중 **14개뿐**(한국어 전용 3 + 다국어 11)이라
나머지 308개는 소리를 하나도 못 받았다. **목소리는 멀쩡했다** — 영어를 주면 전부 성공했다.

원인을 가른 실험: 같은 목소리에 **한국어와 영어를 각각** 보내 본 것. 한 언어만 시험했으면
"목소리 308개가 죽었다"고 오진했을 것이다.

**고친 방법** (기능을 줄이지 않는 쪽):
- 견본 문장을 **그 목소리의 언어로** 고른다 → 이제 **322개를 전부 들을 수 있다**
- 오류 문구를 원인별로 갈랐다. 한글 글 + 한국어 못 하는 목소리일 때만:
  `이 목소리는 프랑스어 전용이라 한국어를 읽지 못합니다…`
- **같은 문장이 `app.js` 에도 복사되어 있었다** — 서버만 고치면 무효였다

**검증**: `tests/voice_language_test.py` **26개 통과**. 일부러 망가뜨려 **5개가 실패**하는 것까지
확인했다. 기존 점검도 그대로다 (`smoke_test` 46, `phase2_test` 33).
설치본에 반영 후 다시 켜고 **새 코드가 도는 것을 실제 호출로 확인**했다.

> 사용자에게 **브라우저 Ctrl+F5** 를 안내했는지 확인할 것 (브라우저가 옛 화면을 들고 있다).

---

## 4. 조사만 끝난 것 — 화면 효과 ✅ 조사 완료 / ❌ 구현 미착수

전문은 `docs/RESEARCH_screen_effects.md`. 요점만:

**넷 다 만들 수 있고 바깥 소재를 사 오거나 받아 올 필요가 없다.**

| 효과 | 방법 | 실측 |
|---|---|---|
| 비 | FFmpeg 필터만 | 빗줄기 세로 15.4px × 가로 1.6px |
| 눈 | FFmpeg 필터만 | 송이 9.7 × 9.6px (둥근 정도 1.00) |
| 물방울 | 파이썬이 굴절 지도를 그리고 `displace` | 안쪽 62% 변화 / 바깥 1.3% |
| 폭죽 | 파이썬이 입자를 그림 | 시각 정확, 색 있음. **입자가 얇아 더 다듬어야** |

- **세기 3단계가 실제로 구별된다**: 화면의 3.2% / 13.3% / 31.2% (4.1배·2.4배 차이)
- **자막과 한 번에 처리된다** — 배경음악처럼 두 번 나눌 필요가 없다
- **속도**: 1080p 30초에 17초 (자막만은 3초). 30분 영상이면 약 17분 → 화면에 미리 알릴 것
- **미리보기는 브라우저에서 흉내 내지 말 것.** 이미 있는 10초 미리보기를 쓴다
  (5초 조각이 1~2초면 나온다). 흉내 내면 결과물과 반드시 어긋난다

**추천 순서**: ① 비·눈 + 세기 3단계 → ② 물방울 → ③ 폭죽

**넘어진 함정 4가지**는 `memory/ffmpeg-effect-layer-traps.md` 에 있다. 전부 **오류 없이 틀린
결과**라 반드시 읽고 시작할 것. 특히 `format=gbrp` 를 빼면 화면 전체가 뿌예진다.

**사용자가 아직 안 본 것**: `화면효과_데모.mp4` (20초, 원본→비→눈→물방울→폭죽).
**보기 좋은지는 사용자만 판정할 수 있다.**

---

## 4-A. 사용자가 지시한 두 기능 — **정확한 내용** (새 창에서 반드시 읽을 것)

사용자가 이 세션에서 직접 말로 지시한 것이다. 계획서에는 없으므로 여기에만 남는다.

### ① "화면 강조 기능의 정도를 조절할 수 있도록 하면 어떨까?"

> 사용자 원문 (2026-08-14, 비 효과 실측 결과를 보고 나서 한 말)

**무엇을 만들라는 것인가**: 화면 효과(비·눈·물방울·폭죽)를 **얼마나 세게** 걸지
사용자가 고를 수 있게 하라는 것이다. 효과를 켜고 끄는 것만으로는 부족하다.

**설계**: `약하게 · 보통 · 많이` 3단계. 단계마다 **개수와 진하기를 함께** 움직인다.
값 하나만 움직이면 단계가 안 벌어진다 (덕킹에서 이미 겪었다).

| 단계 | 밝아진 픽셀 | 화면 비율 | 평균 들림 |
|---|---:|---:|---:|
| 약하게 | 29,536 | 3.2% | 1.35 |
| 보통 | 122,210 | 13.3% | 5.98 |
| 많이 | 287,194 | 31.2% | 18.68 |

단계 사이 **4.14배 · 2.35배**. 가짜 선택지 판정 기준(1.3배)을 크게 넘는다 — 실측 완료.

**점검에 반드시 넣을 것**: 순서(약<보통<많이)와 **최소 간격(1.3배)**.
`memory/options-must-actually-differ.md` 를 읽고 시작할 것.

> ⚠️ **뜻을 확인하지 못한 것**: "화면 **강조**"를 위와 같이 "화면 **효과**의 세기"로 읽었다.
> 문맥상(비 효과 결과를 보여 준 직후에 한 말) 이것이 자연스럽지만, 영상 편집에서
> "강조"는 **화면 일부를 도드라지게 하는 것**(한 곳을 확대해 주목시키기, 주변을 어둡게
> 하거나 흐리게 하기, 화살표·테두리 표시)을 뜻하기도 한다. **만들기 전에 사용자에게
> 어느 쪽인지 한 번 물을 것.** 잘못 읽으면 통째로 헛일이 된다.

### ② "영상 확대 조절 기능"

> 사용자 원문: "화면비 조절 기능에서, 영상확대 조절 기능이 있으면 좋겠어.
> 롱폼에서 숏폼으로 재제작시, 실제 영상 화면을 확대 또는 축소 할 수 있는 기능을
> 활용하면 더 좋겠어."

**무엇을 만들라는 것인가**: 가로 영상을 세로로 다시 만들 때, **영상 화면 자체를
크게·작게** 조절하게 하라는 것이다.

**지금 왜 부족한가**: 현재 `app/core/framing.py` 는 **자르기만** 한다.
1280×720 을 9:16 으로 만들면 가운데 405픽셀만 남기는데, 이는 **원본 가로 화소의 32%**
만 쓰는 것이다. 인물이 원본에서 작게 나오면 세로로 잘라도 여전히 작다.

**어떻게 계산하는가**: `resolve()` 에서 자를 크기를 `zoom` 으로 나눈 뒤 출력 크기로 늘린다.

| 확대 | 원본에서 잘라내는 크기 | 가로 화소 사용률 | 선명도 지표 |
|---|---|---:|---:|
| 1.0배 (지금) | 405 × 720 | 32% | 1.13 |
| 1.6배 | 253 × 450 → 405×720 으로 늘림 | 20% | **0.67** |
| 2.5배 | 162 × 288 → 405×720 으로 늘림 | 13% | (미실측) |

즉 **확대할수록 화질이 떨어진다** (1.6배에서 약 41% 하락). 상한을 2배쯤 두고
화면에 경고를 띄울 것.

**축소**는 crop 으로는 뜻이 안 통한다 (이미 다 보이고 있다). 이미 있는
`여백 채우기(pad)` 와 묶어서 "영상을 틀보다 작게 넣고 남는 자리를 채운다"로 다룰 것.

**반드시 짝으로 고칠 곳**: `app/static/app.js` 의 `outputFrameRect()`.
미리보기의 그리는 쪽과 마우스로 끄는 쪽이 **둘 다** 이 함수를 거친다. 한쪽만 고치면
자막이 튄다. 그리고 `tests/phase4_test.py` 가 `app.js` 를 node 로 실행해 서버 계산과
240가지를 대조하므로 그 점검도 함께 손봐야 한다.
`memory/preview-must-use-the-output-frame.md` 를 읽고 시작할 것 — 여기서 이미
"화면에는 보이는데 결과물에는 없는" 사고가 났다.

---

## 5. 조사만 끝난 것 — 목소리 품질 ⚠️ 승인 대기

**사용자 요청**: "앵커, 아나운스, 친밀감, 신뢰감, 힘찬, 정겨운, 낭낭한 목소리 등 다양한
한국어 목소리를 넣고 싶다. 지금 것은 너무 AI 목소리 티가 난다."

### 확인된 사실

- **edge-tts 는 말투 지정을 아예 지원하지 않는다** (실측: SSML 지시문을 소리 내어 그대로
  읽어 파일이 7배로 늘어났다). 앵커 톤 같은 것은 이 엔진으로 **불가능**하다
- 앱은 이미 322개를 다 보여 주고 있다. "목소리를 더 노출한다"는 해법이 **아니다**
- 한국어를 읽을 수 있는 것은 14개. `목소리_들어보기/` 에 견본을 만들어 두었다
  (**사용자가 아직 안 들었다**)
- **Azure 정식 API**: 한국어 목소리 약 10개(edge-tts 는 이 중 3개만 열어 줌),
  `newscast` 같은 말투 지정 지원, **월 50만 자 무료** (10분 대본 약 160편)
- **ElevenLabs**: 사용자가 목소리 19개를 골라 보내 왔다 (아래 6절)

### 아직 확인 못 한 것

- 한국어 목소리 **각각이 어떤 말투를 지원하는지** (문서에서 표를 뽑다가 중단됨)
- **Azure 한국어가 실제로 덜 AI 같은지** — 키가 있어야 들어볼 수 있다

---

## 6. 승인 대기 중인 설계 — ElevenLabs 사용자별 API 키

사용자가 목소리 라이브러리 주소 20개(중복 1개, **고유 19개**)를 보내며 등록을 요청했다.
ID 는 `tools/check_elevenlabs.py` 의 `WANTED` 에 적어 두었다.

### 사용자가 못박은 요구 (아주 중요하다고 했다)

> "이 프로그램을 사용하는 사람(나 아닌)들이 **본인의 elevenlabs api key 를 입력해서**
> 사용하도록 설계해야 할 것 같아."

키를 앱에 심으면 배포본 사용자 전원이 개발자 계정으로 요금을 쓰게 된다. 절대 심지 말 것.

### 걸림돌 (검색으로 확인, 계정으로는 미확인)

1. **사용자는 무료 플랜이다.**
2. **목소리 라이브러리의 목소리는 무료 플랜에서 API 로 쓸 수 없다.** 즉 19개를 쓰려면
   **유료 플랜이 필요하다.** (표기 의무 문제가 아니라 소리가 아예 안 나오는 문제)
3. 라이브러리 목소리는 각자 계정에 **먼저 담아야** 한다 (`POST /v1/voices/add/{주인ID}/{목소리ID}`).
   주인 ID 는 사용자가 보낸 주소에 없다 — 키가 있으면 검색해서 찾을 수 있다.
4. **무료 플랜은 상업적 사용 금지**, 공개 시 제목에 `elevenlabs.io` 표기 의무.
   유료($5부터)는 상업적 사용 가능. → 사용자 질문 "영상에 합성하면 유료냐"의 정답은
   **"합성이 아니라 공개·상업적 사용이 기준"** 이다.

### 다음에 할 일 — 먼저 이것부터

**`python tools/check_elevenlabs.py` 를 사용자가 직접 돌리게 한다.**
키를 화면에 찍지 않고 저장하지도 않는 점검 도구다. 요금제·남은 글자·**19개 목소리로 실제
소리가 나오는지**를 확인한다. 위 걸림돌 2번이 사실인지 **계정으로 직접 확인**하는 것이
목적이다. 남의 블로그 요약이 아니라 증거가 필요하다.

**아직 안 돌려 봤다.** 결과에 따라 갈린다:
- 소리가 나온다 → ElevenLabs 엔진을 붙인다
- 안 나온다 → 사용자에게 (a) 유료 전환 (b) **Azure 로 전환** (c) 지금 유지 중 고르게 한다

### 설계안 (사용자에게 제시했고 아직 승인 못 받음)

- **키 저장 위치**: `%LOCALAPPDATA%\MovieFit Studio\settings.json`
  (설치 폴더도 프로젝트 폴더도 아닌 **사용자 개인 폴더**. 프로젝트를 남에게 줘도 키가
  안 따라가고, 저장소에 커밋될 일도 없다)
- **화면**: `설정`에 키 입력란 + `확인` 단추. 누르면 실제로 물어서 ① 키가 맞는지
  ② **어느 플랜인지** ③ 남은 글자 수를 보여 준다. 저장된 키는 뒤 4자만 보인다
- **안전 규칙**: 키를 기록에 남기지 않음 · 오류 메시지에 넣지 않음 · 프로젝트 파일에
  넣지 않음 · `.gitignore` 에 추가
- **키가 없을 때**: ElevenLabs 목소리는 `키가 필요합니다`로 흐리게. edge-tts 는 그대로 동작
- **요금 보호**: 만들기 전에 "이 대본은 ○○자를 씁니다"를 미리 보여 준다
  (사용자가 **자기 돈**을 쓰는 것이다)

---

## 7. 그 밖에 조사만 해 둔 것 — 화면 확대/축소

사용자 요청: "롱폼에서 숏폼으로 재제작할 때 영상 화면을 확대·축소하고 싶다."

- 계산은 간단하다. `app/core/framing.py` 의 `resolve()` 에서 자를 크기를 `zoom` 으로 나눈 뒤
  출력 크기로 늘리면 된다
- 실측: 1280×720 을 9:16 으로 자르면 지금은 가로 화소의 **32%** 만 쓴다.
  1.6배 확대하면 20%만 쓰고 선명도가 1.13 → 0.67 로 **약 41% 떨어진다**
  → 상한(2배 정도)과 화질 경고가 필요하다
- **함정**: `app.js` 의 미리보기 계산을 **짝으로 같이 고쳐야** 한다.
  안 그러면 미리보기와 결과물이 어긋난다 (`memory/preview-must-use-the-output-frame.md`
  에 이미 한 번 겪은 사고가 적혀 있다). `phase4_test` 가 `app.js` 를 node 로 실행해
  서버 계산과 대조하므로 그 점검도 함께 손봐야 한다

---

## 8. 점검 현황 — 456개 (15개 스위트)

기존 430개 + **`voice_language_test.py` 26개**.
따로 도는 것: `longvideo_test.py` (17개, 10~40분 걸림).

시험용 파일은 저장소에 넣지 않는다. 없으면 만든다:

```
python tools/make_sample.py              색막대 영상 + 나레이션 견본
python tools/make_sample_images.py --count 30
python tools/make_sample_speech.py       말소리 영상 (인터넷 필요)
python tools/make_sample_long.py         30분 영상
```

---

## 9. 아직 안 된 것 (정직하게)

- **화면 효과 구현 미착수.** 조사만 끝났다
- **ElevenLabs 미착수.** 설계안만 냈고 승인 못 받았다. 점검 도구도 안 돌려 봤다
- **화면 확대/축소 미착수**
- **사용자가 `화면효과_데모.mp4` 와 `목소리_들어보기/` 를 아직 확인하지 않았다.**
  둘 다 "보기·듣기 좋은지"는 사람만 판정할 수 있다
- **커밋하지 않았다**
- 이전 문서에서 넘어온 미완료: 소재 칸 통합 · 마이크 녹음(F-45) · 내 목소리 나레이션(F-46)
  · EXIF 회전·`.heic` · 강제정렬 정확도 · 덕킹 세기를 귀로 확인 안 함
  · `USER_GUIDE.md` 만 보고 처음 쓰는 사람이 끝까지 가는지 미시험

---

## 10. 새 창에서 쓸 시작 프롬프트

```
capcut-studio-kit 프로젝트를 이어서 진행합니다.

먼저 다음을 읽어 주세요:
1) CLAUDE.md
2) memory/ 폴더 전체 (23개)
3) handover_20260814_1607.md  ← 이 문서
   **특히 4-A절**: 제가 말로 지시한 두 기능(화면 강조 세기 · 영상 확대)이
   거기에만 적혀 있습니다. 계획서에는 없으니 반드시 읽어 주세요.
4) docs/RESEARCH_screen_effects.md

지금 상태: v0.5.0 이 배포되어 있고, 그 위에 목소리 버그 수정이
설치본(8765)에 반영되어 있습니다. 아직 커밋하지 않았습니다.
개발 서버는 꺼져 있습니다.
점검을 돌릴 때는 MOVIEFIT_TEST_URL 과 MOVIEFIT_TEST_PROJECTS 를 짝으로 맞춰 주세요.

할 일: (여기에 하고 싶은 것을 적어 주세요)

  예시 1) ElevenLabs — 먼저 python tools/check_elevenlabs.py 를 제가 돌려서
          결과를 알려 드릴 테니, 그 결과를 보고 다음을 정합시다
  예시 2) 화면 효과 만들기 — 비·눈부터 (docs/RESEARCH_screen_effects.md 7절 순서)
  예시 3) 지금까지 작업 커밋하기
  예시 4) 화면 확대/축소 기능 만들기
```
