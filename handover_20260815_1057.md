# 인수인계 — MovieFit Studio (화면 효과 13종 완성 · 값 조절 체계)

> **v1.1** | 최종 업데이트: **2026-08-15 11:18** | 작성자: Claude Code

## 변경 이력

| 버전 | 일시 | 주요 변경 |
|------|------|---------|
| v1.0 | 2026-08-15 10:57 | 초기 작성. 2·3·4단계(비·눈 → 색감·강조)를 마치고 효과 13종을 갖춘 시점 |
| v1.1 | 2026-08-15 11:18 | **커밋을 마쳤다**(`018ffa3`). 1·3·7절의 "커밋 안 됨" 기술을 사실에 맞게 고쳤다 |

> 앞 문서 `handover_20260815_0755.md`(v1.0)는 **효과 띠 뼈대와 줌 강조까지**를 다룬다.
> 그 뒤의 일은 이 문서가 맞다. 앞 문서의 1절(사용자가 정정해 준 원칙 세 가지)은
> **여전히 유효**하다.

---

## 0. 한 문단 요약

**화면 효과가 1종에서 13종이 되었고, 효과마다 값을 슬라이더로 조절할 수 있게 되었다.**
승인된 5단계 중 **2·3·4단계를 마쳤고 5단계(물방울·비눗방울·폭죽)만 남았다.**
점검은 **715개가 전부 통과**한다. 만드는 도중 **점검이 통과하고 있던 결함 두 가지**를
잡았다(색이 통째로 사라지던 것, 따뜻하게·차갑게가 아무 일도 안 하던 것).
**이번 작업은 커밋했다** — `018ffa3` (2026-08-15 11:18). 커밋 직전에 점검 715개를
8766 개발 서버를 겨냥해 다시 돌려 전부 통과하는 것을 확인했다.

---

## 1. 지금 컴퓨터 상태

| 항목 | 상태 |
|---|---|
| 설치본 (8765) | **반영 안 됨.** 이번 작업은 개발 서버에만 있다 |
| 개발 서버 (8766) | 켜져 있다면 새 코드가 올라가 있다. 확인은 아래 참조 |
| 마지막 커밋 | `018ffa3` — 이번 작업(효과 13종·값 조절)까지 담겨 있다 |
| 커밋 안 된 변경 | **없다.** 작업 트리가 깨끗하다 |
| 아직 안 밀어 올림(push) | `origin/main` 보다 **2개 앞서 있다** (`b8e1fbf`·`018ffa3`) |

### 서버를 다시 띄우는 법 (윈도우)

파이썬 코드를 고쳤으면 **반드시 다시 띄워야 한다.** 화면 파일(html·css·js)은
새로고침으로 반영되지만 파이썬은 켜질 때 한 번만 읽는다.

```powershell
Get-NetTCPConnection -LocalPort 8766 -State Listen -ErrorAction SilentlyContinue |
  ForEach-Object { Stop-Process -Id $_.OwningProcess -Force }
```

```
python -m app --port 8766
```

`pkill` 로는 안 죽는다. 안 죽이면 **옛 코드가 계속 돌아 새 효과가 목록에 안 나온다.**

### 점검 돌리는 법

```
MOVIEFIT_TEST_URL=http://127.0.0.1:8766 python tests/looks_test.py
```

`MOVIEFIT_TEST_PROJECTS` 는 **지운다**(설치본을 겨냥할 때만 짝으로 쓴다).

---

## 2. 무엇을 만들었나

### 2-1. 효과 13종 (전부 FFmpeg 내장 필터, 바깥 소재 없음)

| 종류 이름 | 화면 이름 | 조절할 수 있는 값 | 만든 시점 |
|---|---|---|---|
| `zoom_punch` | 줌 강조 | — | 1단계 |
| `rain` | 비 | 속도 | 2단계 |
| `snow` | 눈 | 속도 | 2단계 |
| `blur_area` | 부분 흐림 | 자리·너비·높이 | **이번** |
| `spotlight` | 주변 어둡게 | 자리·밝은 부분 크기 | **이번** |
| `vignette` | 가장자리 어둡게 | — | **이번** |
| `color_adjust` | 밝기·대비·채도 | 밝기·대비·채도 | **이번** |
| `warm` `cool` `vivid` `vintage` `mono` | 따뜻하게·차갑게·선명하게·빈티지·흑백 | — | **이번** |
| `box_mark` | 네모 테두리 | 자리·너비·높이 | **이번** |

**13종 전부 효과 구간 바깥이 원본과 한 값도 다르지 않다.**

### 2-2. 값 조절(슬라이더) 체계 — 이번 작업의 핵심 구조

사용자 지시: *"효과 속도를 조절할 수 있도록 설계에 반영해 줘."*

속도 하나만 붙이지 않고 **값 하나를 슬라이더 하나로 그리는 공통 체계**를 만들었다.
등록표에 설명서를 적으면 화면이 그대로 그린다:

```python
"params": {"speed": {"label": "떨어지는 속도", "min": 30, "max": 250,
                     "step": 5, "default": 100, "suffix": "%"}}
```

**화면 코드(`app.js`)는 효과 이름을 하나도 모른다.** 새 효과에 값을 붙여도
화면을 고칠 일이 없다. 브라우저로 눌러 확인했다.

> 함께 고친 것: 예전 `_clean_params` 는 **모든 값을 0~100 으로 잘랐다.**
> 그대로 두었으면 속도를 100% 위로 못 올리고 밝기를 음수로 못 내렸다.

---

## 3. 이 커밋(`018ffa3`)에 담긴 11건 — 전부 이번 세션 것이다

앞 세션들과 달리 **주제가 하나뿐이라 그대로 담았다.** (아래 목록에 이 문서 자신이
빠져 있는 것은 v1.0 을 쓸 때는 아직 커밋 전이었기 때문이다. 실제로는 이 문서도 함께 담겼다.)

```
 M app/core/effects.py                 효과 10종 추가 · 값 체계 · 결함 2건 수정
 M app/static/app.js                   슬라이더를 그리는 renderFxParams()·fxDefaults()
 M app/static/index.html               #fx-params 칸 하나
 M app/static/style.css                .fx-params 모양
 M docs/DESIGN_effects_and_zoom.md     13절 신설 (v1.5)
 M memory/ffmpeg-effect-layer-traps.md 함정 ⑥ 신설 (색이 사라지는 것)
 M memory/options-must-actually-differ.md  세 번째 사례 (색감 프리셋)
 M tests/effects_ui_test.py            슬라이더 화면 점검 7개
 M tests/weather_test.py               색 살아 있는지 검사 4개
?? tests/looks_test.py                 **새 점검 85개**
```

> 데모 영상 `화면효과_모음_데모.mp4`·`화면효과_비눈_데모.mp4` 는 `.gitignore` 의
> `/*.mp4` 가 막고 있어 커밋에 안 들어간다. **일부러 그렇게 두었다.**

---

## 4. 이번에 잡은 결함 둘 — **둘 다 점검이 통과하고 있었다**

이 절이 이 문서에서 가장 중요하다. 두 결함 모두 "오류 없이 틀린 결과"였고,
**기존 점검이 초록불을 켜고 있었다.**

### ① 비·눈·주변 어둡게를 걸면 화면의 색이 통째로 사라졌다

색을 지키려고 넣은 `blend=...:c1_opacity=0:c2_opacity=0` 이 **정반대로** 동작했다.
`blend` 는 첫 입력이 '위', 둘째가 '아래'이고 `opacity=0` 은 '아래를 쓴다'는 뜻이라,
효과 그림의 중립 색값(128)이 원본 색을 덮었다.

    c1_opacity=0 → 색의 진하기 126.1 → 0.0   (흑백)
    옵션을 뺌     → 126.1 → 126.1            (그대로)

**왜 안 잡혔나**: 그때 이미 이 효과들을 검사하는 점검이 서른 개 가까이 있었고
구간 밖 완전 일치·안개 없음·낙하 방향·세기 3단계·자막 생존까지 다 쟀는데,
**하나같이 `convert("L")` 로 흑백을 만들어 밝기만** 봤다.

### ② 따뜻하게·차갑게가 사실상 아무 일도 안 했다

    따뜻하게 보통 → 중립 회색에서 빨강−파랑  +1.0
    차갑게  보통 → 중립 회색에서 빨강−파랑   0.0

`colorbalance` 는 그림자(`rs`)·중간톤(`rm`)·밝은톤(`rh`)을 **따로** 받는데
그림자만 지정해서 밝은 화면에서는 손댈 자리가 없었다. 세 톤을 모두 지정해
`+25 / −25` 로 갈랐다.

**왜 안 잡혔나**: 점검이 "원본과 달라졌다"만 봤다. `eq=saturation` 이 원본의 색을
키운 것만으로도 "달라졌다"가 통과한다.

### 여기서 나온 규칙 두 가지 (앞으로 반드시 지킬 것)

1. **화면을 바꾸는 기능은 밝기와 색을 따로 잰다.**
   색은 한 줄이면 된다: `np.abs(rgb[:,:,0]-rgb[:,:,2]).mean()`
2. **색을 판정할 때는 중립 회색(0x808080)에 걸어 잰다.**
   바탕이 색을 갖고 있으면 채도만 올려도 '색이 옮겨간 것처럼' 보인다.
   실제로 파란 사진에서 따뜻하게가 **더 파래지는** 것으로 잡혔다.

두 결함 모두 **일부러 되돌려** 점검이 잡는 것(각각 6개·1개 실패)까지 확인했다.

---

## 5. 만드는 법 요약 (`app/core/effects.py`)

효과를 새로 붙이는 일은 **등록표(`KINDS`)에 항목 하나 더하는 것**으로 끝난다.
갈래가 둘이다:

| 갈래 | 뜻 | 쓰는 효과 |
|---|---|---|
| `build` | 화면 자체를 주무르는 **사슬 필터** 하나를 돌려준다 | 줌 강조, 색 계열, 비네트, 네모, 부분 흐림 |
| `layer` | 화면 위에 덧씌울 **그림 + 켜는 구간 + 합성 방법** 목록을 돌려준다 | 비, 눈, 주변 어둡게 |

비·눈·주변 어둡게가 쓰는 합성은 **밝기 면만** 건드린다:

```
blend=c0_mode=screen:c1_mode=normal:c2_mode=normal      ← 비·눈 (밝힌다)
blend=c0_mode=multiply:c1_mode=normal:c2_mode=normal    ← 주변 어둡게 (어둡게 한다)
```

**`opacity` 를 절대 붙이지 말 것** (4절 ①).

### 반드시 지키는 것 (전부 실측으로 확인됨)

- 색을 바꾸는 필터는 **`eq` 를 앞에** 둔다. 뒤에 두면 색공간 변환이 끼어 구간 밖
  화소가 ±2 밀린다. 순서만 바꾸면 차이가 정확히 0이 된다
- 효과 그림의 **검정은 0**(16이 아니다). 16이면 온 화면에 안개가 낀다
- 문턱값은 `minval`·`maxval` 로 적는다(숫자로 적으면 자가 어긋나 점이 0개가 된다)
- 잘라내는 창을 **위로** 올려야 그림이 아래로 떨어진다
- 판정용 렌더는 **무손실(`-c:v ffv1`)** 로 굽는다. H.264 는 뒤 프레임을 미리 보고
  압축을 정해서, 뒤쪽에만 효과가 있어도 앞쪽 프레임이 달라진다

---

## 6. 점검 현황 — 715개 (20개 스위트) · 전부 통과

| 스위트 | 개수 | 무엇 |
|---|---:|---|
| `tests/looks_test.py` | **85** | **새로 만듦.** 색감·강조·부분 흐림·값 조절 |
| `tests/weather_test.py` | 71 | 비·눈 (색 검사 4개 추가) |
| `tests/effects_ui_test.py` | 39 | 효과 화면 (슬라이더 점검 7개 추가) |
| `tests/effects_test.py` | 39 | 효과 띠 뼈대 |
| `tests/zoom_test.py` | 25 | 화면 확대 |
| 나머지 15개 | 456 | 기존 |

따로 도는 것: `longvideo_test.py`(17개, 10~40분).

---

## 7. 다음에 할 일

### 7-1. 남은 것은 5단계 하나뿐이다 — 다만 **구조 변경이 따라온다**

| 남은 효과 | 만드는 법 | 왜 앞의 10종과 다른가 |
|---|---|---|
| 물방울 맺힘 | 파이썬이 굴절 지도 2장 + 하이라이트 1장을 그림 → `displace` | **그림 파일을 FFmpeg 입력으로 더 넣어야** 한다 |
| 비눗방울 | 파이썬 입자 (천천히·이리저리·작게 — 사용자 확정 사양) | 같음 |
| 작은 폭죽 | 파이썬 입자 (중력·감쇠·반짝임) | 같음 |

지금 렌더링은 **입력이 영상 하나뿐**이고 `-vf` 한 줄로 끝난다
(`app/core/ffmpeg.py` 의 `burn_subtitles`). 그림을 겹치려면 `-i` 를 더 붙이고
`-filter_complex` 로 옮겨야 하며, 그때 **소리 흐름(`-c:a copy`)과 자막 필터가
살아남는지 다시 재야 한다.** 앞의 10종은 이 구조를 건드리지 않았다.

조사 결과는 `docs/RESEARCH_screen_effects.md` 에 있다(물방울은 안쪽 62% / 바깥 1.3%로
실측 완료, 폭죽은 만들었으나 입자가 얇다고 기록됨).

### 7-2. 그 밖에 남은 것

- **사용자가 데모를 아직 안 봤다.** `화면효과_모음_데모.mp4`(48초, 13종 전부)
- **눈 '많이'는 송이가 뭉쳐 네모지게 보인다** — 값의 문제이지 방법의 문제가 아니다
- **4K 와 30분짜리 긴 영상에서 시험하지 않았다** (720p 가로·세로만 확인)
- **설치본(8765)에 반영하지 않았다**
- ~~커밋하지 않았다~~ → **커밋했다** (`018ffa3`). 다만 **GitHub 에 밀어 올리지는 않았다** —
  `origin/main` 보다 2개 앞서 있다
- 앞 문서에서 넘어온 것: ElevenLabs 승인 대기 · 소재 칸 통합 · 마이크 녹음(F-45) ·
  내 목소리 나레이션(F-46) · EXIF 회전·`.heic` · 강제정렬 정확도

---

## 8. 새 창에서 쓸 시작 프롬프트

```
capcut-studio-kit 프로젝트를 이어서 진행합니다.

먼저 다음을 읽어 주세요:
1) CLAUDE.md
2) memory/ 폴더 전체
3) handover_20260815_1057.md   ← 이 문서
4) docs/DESIGN_effects_and_zoom.md   (12·13절이 최신 현황)
5) docs/RESEARCH_screen_effects.md   (물방울·폭죽 실측 조리법)

지금 상태: 화면 효과 13종과 값 조절 슬라이더가 완성되어 개발 서버(8766)에
올라가 있고, 점검 715개가 전부 통과하며, 커밋(018ffa3)까지 마쳤습니다.
아직 GitHub 에 밀어 올리지는(push) 않았습니다.
승인된 5단계 중 5단계(물방울·비눗방울·작은 폭죽)만 남았습니다.

점검을 돌릴 때는 MOVIEFIT_TEST_URL=http://127.0.0.1:8766 을 주고
MOVIEFIT_TEST_PROJECTS 는 지워 주세요.

할 일: (여기에 적어 주세요)

  예시 1) 5단계 — 물방울·비눗방울·작은 폭죽 만들기
  예시 2) 설치본(8765)에 반영하고 배포하기
  예시 3) 화면효과_모음_데모.mp4 를 보고 값을 손볼 것 정하기
  예시 4) GitHub 에 밀어 올리기(push)
  예시 5) 내 영상으로 데모를 다시 만들어 보기
```
