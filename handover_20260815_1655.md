# 인수인계 — MovieFit Studio v0.6.0 공개 배포 완료

> **v1.0** | 최종 업데이트: **2026-08-15 16:55** | 작성자: Claude Code

## 변경 이력

| 버전 | 일시 | 주요 변경 |
|------|------|---------|
| v1.0 | 2026-08-15 16:55 | 초기 작성. **v0.6.0 을 공개 배포하고, 사용설명서에 빠져 있던 화면 효과 장을 써 넣은** 시점 |

> 앞 문서 `handover_20260815_1740.md` 는 진하기 슬라이더·크기 축소·색 결함 수정까지를
> 다룬다. 그 문서 6-3절의 배포 절차를 이 세션에서 전부 마쳤다.
>
> ⚠ **앞 문서들의 시각 표기는 실제 시계보다 앞서 있다.** 그 문서는 스스로를 "20:35"
> 라고 적었지만 같은 작업의 마지막 커밋 시각은 `15:03` 이다. 이 문서부터 실제 시각을
> 쓴다. 파일 이름의 시각도 마찬가지다.

---

## 0. 한 문단 요약

**v0.6.0 을 공개 배포했다.** 설치 파일을 새로 만들어 GitHub 릴리스에 올렸고, 소개
사이트도 새로 배포했다. 그 과정에서 **배포 절차에 빠져 있던 구멍 하나를 찾아 메웠다** —
화면 효과 16종이 프로그램에는 다 들어와 있는데 **사용설명서에는 한 줄도 없었다.**
사용자 승인을 받아 새 9장을 쓰고 옛 9~19장을 10~20장으로 밀었다. 점검 21개 스위트가
전부 통과하고, 설치본에 대고 돌린 브라우저 점검 39개와 설명서 점검 32개도 전부
통과한다.

---

## 1. 지금 컴퓨터 상태

| 항목 | 상태 |
|---|---|
| 설치본 (8765) | **v0.6.0 으로 새로 설치됨 · 켜져 있음.** `projects/` 17개 그대로 |
| 개발 서버 (8766) | **꺼져 있다.** 설치할 때 껐고 다시 켜지 않았다 |
| 마지막 커밋 | `0855efe` (아래 3절에 이 문서 커밋이 하나 더 붙는다) |
| GitHub | `origin/main` = `0855efe`, 태그 `v0.6.0` 올라감 |
| GitHub 릴리스 | **v0.6.0 공개됨.** 설치 파일 124.2MB 첨부 |
| 소개 사이트 | **새로 배포됨.** <https://moviefit-studio.vercel.app> |

### 개발 서버를 다시 켜는 법

```
python -m app --port 8766
```

⚠ **설치 파일을 다시 설치하려면 8765 와 8766 을 모두 꺼야 한다.** 켜져 있는지
확인하는 표시가 포트와 무관해서, 개발 서버만 떠 있어도 설치가 막힌다
(`memory/installed-app-needs-restart.md`).

### 점검 돌리는 법

```
MOVIEFIT_TEST_URL=http://127.0.0.1:8766 python tests/looks_test.py
```

`MOVIEFIT_TEST_PROJECTS` 는 **지운다**. `PYTHONIOENCODING=utf-8` 을 함께 준다.

---

## 2. 이번 세션에 한 일

### 2-1. 밀려 있던 커밋 2개를 올렸다

`dbe3c76` → `132cc26` 까지 push 했다.

### 2-2. ★ 사용설명서에 없던 기능을 찾아 채웠다

배포 절차대로만 했으면 **화면 효과 16종이 설명서에 한 줄도 없는 채로** 공개될
뻔했다. 게다가 옛 문구들이 거짓말이 되어 있었다:

| 어디 | 뭐라고 적혀 있었나 | 왜 틀렸나 |
|---|---|---|
| 설명서 1장 | "…**색을 보정하는** 편집 기능은 없습니다" | 따뜻하게·차갑게·빈티지·흑백이 이미 있다 |
| 소개 사이트 | "컷 편집·전환·**필터**·스티커 등 … 만들지 않는 것" | 필터가 여섯 종 있다 |
| 소개 사이트 | "자동 점검 **123개** 항목" | 21개 묶음으로 늘었다 |

**점검은 이 넷을 절대 못 잡는다.** `tests/guide_test.py` 조차 "설명서에 적힌 것이
코드와 맞는가"만 묻고, **"코드에 있는 것이 설명서에 있는가"는 묻지 않는다.**
없는 장에는 아무 검사도 안 걸린다. 교훈은
`memory/release-needs-the-manual-too.md` 에 남겼다.

**새 9장 「화면 효과 넣기」** 를 썼다 — 효과를 놓고 구간을 정하는 법, 16종 표,
세기 3단계, 값 슬라이더(진하기 20~200% 등), 자막이 효과 위에 그려진다는 것,
만드는 시간이 몇 배 걸린다는 것.

**옛 9~19장은 10~20장으로 밀었다.** 손으로 하지 않고 스크립트로 큰 수부터 내려오며
바꾸고, 두 가지를 검사했다 — 소절 번호(`### N-M`)가 속한 장과 어긋난 곳 **0건**,
`N장` 상호참조 4곳(배경음악 13→14, 사진 9→10, 노래 10→11, 나레이션 12→13) 자동 갱신.

설명서 HTML 둘은 `python tools/build_guide.py` 가 `docs/USER_GUIDE.md` 하나에서
만든다. 마크다운만 고치고 생성기를 돌렸다.

### 2-3. 버전을 0.6.0 으로 올리고 릴리스 노트를 썼다

`app/__init__.py` 한 곳만 고치면 설명서 두 곳과 설치 파일 이름까지 따라온다.
`docs/release_notes_v0.6.0.md` 를 v0.3.0 것을 본으로 썼다.

### 2-4. 설치 파일을 새로 만들고 **실제로 설치해 확인했다**

`python tools/build_installer.py` → `build/dist/MovieFitStudio-Setup.exe` (124.2MB).

설치 전에 **payload 를 그 안의 파이썬으로 직접 돌려** 개발 환경과 무관하게 도는지
확인했다(내려받은 사람과 같은 조건):

    파이썬 3.11.9 · pillow 12.3.0 · 프로그램 0.6.0 · 효과 16종 · numpy 2.4.6 · ffmpeg 있음

`pillow` 는 이번에 새로 필요해진 부품인데 `requirements.txt` 에 들어 있어
빌드가 payload 에 자동으로 넣었다. **앞 세션처럼 손으로 넣을 필요가 없다.**

설치 뒤 **파일 복사 ≠ 반영**이므로 실제로 새 코드가 도는지 확인했다:

| 확인한 것 | 결과 |
|---|---|
| 설치본이 보고하는 버전 | **0.6.0** |
| 폭죽 '터지는 크기' 기본값 | **22** (옛 코드는 38 — 새 코드가 확실히 돈다) |
| 효과 종류 · 세기 | 16종 · 약하게/보통/많이 |
| 설명서 | 20장 · 9장 「화면 효과 넣기」 있음 |
| `projects/` | 설치 전후 **17개 그대로** |
| 브라우저로 실제 눌러 보기 | `effects_ui_test.py` **39개 전부 통과** (8765 겨냥) |
| 설명서 점검 | `guide_test.py` **32개 전부 통과** (소개 사이트와 프로그램 안 본문이 16,441자로 동일) |

### 2-5. GitHub 릴리스와 소개 사이트 배포

- 태그 `v0.6.0` push, 릴리스 생성, 설치 파일 첨부(130,249,672 바이트)
- `releases/latest` 가 v0.6.0 을 가리키는 것과 내려받기 주소가 200 을 주는 것 확인
- 소개 사이트 배포 후 **사이트가 주는 파일이 로컬과 글자 하나까지 같은 것** 확인

---

## 3. 점검 현황

**21개 스위트 전부 통과**(종료코드 0, 실패 0). 개발 서버 8766 겨냥.

> ⚠ 이번에는 **스위트별 개수를 세지 않았다.** 집계 스크립트가 요약 문구 형식
> 두 가지 중 하나만 읽어서, 통과 여부(종료코드)만 확실하고 개수는 `guide_test.py`
> 32개만 확인됐다. 개수가 필요하면 다시 돌려야 한다. 요약 문구는 두 가지다:
> `통과 N개 · 실패 M개` (대부분) / `결과: N개 통과 · M개 실패` (guide_test).

따로 도는 것: `longvideo_test.py`(10~40분) — **이번에 돌리지 않았다.**

---

## 4. 이번에 겪은 함정 (다음에 또 만난다)

### 4-1. 개발 서버가 설치를 막는다

설치가 **종료코드 1** 로 죽었다. "초기화 실패"처럼 보이지만 실제 원인은 개발
서버 8766 이 켜져 있어서였다. `/VERYSILENT /SUPPRESSMSGBOXES` 는 안내창을 Cancel 로
처리해 원인을 감춘다. **`/LOG=<경로>` 를 붙여 다시 돌리면 로그 끝에 이유가 그대로
적혀 있다.**

### 4-2. Vercel `--cwd` 가 "Not authorized" 로 죽는다

`vercel deploy --prod --cwd landing` 이 권한 오류로 죽었다. 로그인은 멀쩡했다
(`vercel whoami` 됨, `vercel project ls` 에 프로젝트 보임). **폴더 안에서 그냥
돌리면 된다**: `Push-Location landing; vercel deploy --prod; Pop-Location`.

### 4-3. 배포 확인할 때 글자 수가 안 맞아 보인다

사이트가 주는 파일은 `\r\n`, 파이썬이 로컬 파일을 글자 모드로 읽으면 `\r` 이
떨어진다. 그래서 **줄 수만큼**(이번엔 627자) 차이가 나 배포가 안 된 것처럼 보인다.
양쪽을 `.replace('\r\n','\n')` 로 맞춰 비교하라.

---

## 5. 다음에 할 일

1. **사용자가 사이트를 지인에게 전달해 써 보게 한다** ← 지금 여기
   - 소개: <https://moviefit-studio.vercel.app>
   - 설명서: <https://moviefit-studio.vercel.app/guide>
   - 내려받기 단추는 `releases/latest` 를 가리키므로 손댈 필요 없다
2. 피드백이 오면 그것부터 고친다
3. 남은 것: 4K·30분 영상 시험 · ElevenLabs 승인 대기 · 소재 칸 통합 ·
   마이크 녹음(F-45) · 내 목소리 나레이션(F-46) · EXIF 회전·`.heic` · 강제정렬 정확도

---

## 6. 다음 배포 때 지킬 순서 (6-3절을 고쳐 쓴 것)

    0) 지난 릴리스 이후 생긴 기능마다 **설명서에 장이 있는가** 를 먼저 본다
       git log <지난태그>..HEAD 로 훑고 docs/USER_GUIDE.md 에서 기능 이름을 찾아본다
    0-1) 설명서·소개 사이트의 "안 되는 것" 목록에 이제 되는 것이 남아 있지 않은가
    0-2) 소개 사이트에 박아 둔 숫자(점검 개수·날짜)가 지금 값인가
    1) app/__init__.py 의 __version__ 을 올린다
    2) python tools/build_guide.py  (설명서를 고쳤다면 반드시)
    3) docs/release_notes_vX.Y.Z.md 를 쓴다
    4) 점검을 돌린다 (개발 서버 재기동 후 — 파이썬을 고쳤으면 필수)
    5) python tools/build_installer.py
    6) **8765 와 8766 을 모두 끄고** 설치 파일로 설치해, 새 코드가 도는지 확인한다
       옛 코드와 값이 다른 자리를 하나 골라 본다 (지금은 폭죽 기본값 22)
    7) git push · git tag · gh release create ... --notes-file docs/release_notes_...
    8) Push-Location landing; vercel deploy --prod; Pop-Location
    9) 사이트가 주는 파일과 로컬 파일을 **줄바꿈 맞춰** 비교한다

---

## 7. 새 창에서 쓸 시작 프롬프트

```
capcut-studio-kit 프로젝트를 이어서 진행합니다.

먼저 다음을 읽어 주세요:
1) CLAUDE.md
2) memory/ 폴더 전체
3) handover_20260815_1655.md   ← 이 문서

지금 상태: v0.6.0 을 공개 배포했습니다. 설치본(8765)도 0.6.0 이고, 소개 사이트와
GitHub 릴리스 모두 새것입니다. 개발 서버(8766)는 꺼져 있습니다.

점검을 돌릴 때는 MOVIEFIT_TEST_URL=http://127.0.0.1:8766 을 주고
MOVIEFIT_TEST_PROJECTS 는 지워 주세요.
```
