# 인수인계 — MovieFit Studio (효과 16종 완성 · 가림 상한 도입)

> **v1.1** | 최종 업데이트: **2026-08-15 13:35** | 작성자: Claude Code

## 변경 이력

| 버전 | 일시 | 주요 변경 |
|------|------|---------|
| v1.1 | 2026-08-15 13:35 | 사용자가 데모를 보고 **다음 작업 3건**을 지시. 8절 신설(그 3건의 상세). 전체 점검 792개 통과 확인 |
| v1.0 | 2026-08-15 13:10 | 초기 작성. 5단계까지 마쳐 **승인된 다섯 단계가 전부 끝난** 시점 |

> **새 창에서 맨 먼저 읽을 곳: 8절.** 사용자가 지시한 작업 3건이 거기 있다.

> 앞 문서 `handover_20260815_1057.md`(v1.1)는 **효과 13종과 값 조절 슬라이더까지**를 다룬다.
> 그 뒤의 일은 이 문서가 맞다.

---

## 0. 한 문단 요약

**승인된 다섯 단계가 전부 끝났다.** 효과가 13종에서 **16종**이 되었고(물방울 맺힘 ·
비눗방울 · 작은 폭죽), 그보다 중요하게 **사용자 지적을 받아 13종의 세기 값을 전부 다시
잡았다** — "효과들이 화면을 너무 가려 시청자가 영상에 집중할 수 없다". 점검은
**792개가 전부 통과**한다(715 → 792). **아직 커밋하지 않았고 push 도 안 했다.**
사용자가 데모 두 편을 보고 판단한 뒤 한꺼번에 올리기로 되어 있다.

---

## 1. 지금 컴퓨터 상태

| 항목 | 상태 |
|---|---|
| 설치본 (8765) | **반영 안 됨** |
| 개발 서버 (8766) | 새 코드가 올라가 있다 (13:0x 재기동) |
| 마지막 커밋 | `093d9a8` — 효과 13종·값 조절까지 |
| 커밋 안 된 변경 | **아래 3절** |
| GitHub push | `origin/main` 보다 **3개 앞서 있다** (그 위에 이번 것이 더 쌓인다) |

### 서버를 다시 띄우는 법 (파이썬을 고쳤으면 **반드시**)

```powershell
Get-NetTCPConnection -LocalPort 8766 -State Listen -ErrorAction SilentlyContinue |
  ForEach-Object { Stop-Process -Id $_.OwningProcess -Force }
```
```
python -m app --port 8766
```

### 점검 돌리는 법

```
MOVIEFIT_TEST_URL=http://127.0.0.1:8766 python tests/artfx_test.py
```
`MOVIEFIT_TEST_PROJECTS` 는 **지운다**.

---

## 2. 이번 세션에 한 일 (셋)

### 2-1. 앞 작업을 커밋했다 (`018ffa3`, `093d9a8`)

효과 13종·값 조절 슬라이더. 그리고 그 커밋에 담긴 인수인계 문서가 "이번 작업은
커밋하지 않았다"라고 적고 있어 곧바로 바로잡았다(`093d9a8`).

### 2-2. **세기 사다리를 전부 다시 잡았다** — 이 문서에서 가장 중요한 절

사용자 지적: *"지금 효과들이 화면을 너무 가리고 있다. 영상 화면을 너무 가리게 되면
시청자들이 영상 화면에 집중할 수 없어."*

**원인이 코드에 남아 있었다.** 세기 3단계를 정할 때 지킨 기준은 "단계 사이가 1.3배
넘게 벌어질 것"(가짜 선택지 방지) **하나뿐**이었다. 그것은 **아래에서 미는 힘만** 준다.
위에서 누르는 기준이 없으니 '많이'가 계속 세졌고, 점검 715개는 전부 통과하고 있었다.

실측(결이 있는 화면):

| 효과 | 세기 | 전 → 후 |
|---|---|---|
| 주변 어둡게 | **약하게** | 남은 밝기 65% → **87%** (가장 약한 단계가 이미 과했다) |
| 주변 어둡게 | 많이 | 38% → **66%** |
| 가장자리 어둡게 | 많이 | 44% → **69%** |
| 비 | 많이 | 남은 결 **193%** → 139% (원본보다 두 배 어수선했다) |
| 빈티지 | 많이 | 세피아 100% → **50%** |

세운 상한 (화면 전체에 거는 효과):

| 세기 | 평균 밀림 | 남은 밝기 | 남은 결 |
|---|---:|---:|---:|
| 약하게 | ≤ 4 | ≥ 90% | 92~112% |
| 보통 | ≤ 9 | ≥ 78% | 88~125% |
| 많이 | ≤ 18 | ≥ 62% | 80~140% |

**단계 간격 1.3배는 그대로 지킨다.** 값을 그냥 낮추면 안 되고 **비율을 유지한 채
사다리 전체를 내려야** 한다(색감 계열은 전 단계에 0.7배).

### 2-3. 5단계 — 물방울 맺힘 · 비눗방울 · 작은 폭죽

**인수인계 문서의 계획이 틀렸다.** 앞 문서는 `-i` 를 더 붙이고 `-filter_complex` 로
옮겨야 하며 소리·자막이 살아남는지 다시 재야 한다고 적었다. 실측해 보니 FFmpeg 의
`movie=` 소스 필터가 필터그래프 **안에서** 파일을 열어 준다 — **구조를 한 군데도
안 건드렸다.**

| 효과 | 파이썬이 그리는 것 | FFmpeg 이 하는 일 |
|---|---|---|
| 물방울 맺힘 | 굴절 지도 2장 + 반짝임 1장 | `displace` 로 화면을 휘게 한다 |
| 비눗방울 | 고리 그림 1장 (세로 두 겹) | `overlay` 의 **자리를 시간식으로** 올린다 |
| 작은 폭죽 | 장면 30개를 이어 붙인 1장 | `crop` 으로 칸을 골라 얹는다 (만화책 넘기기) |

---

## 3. 커밋하지 않은 변경

```
 M app/core/effects.py         art 갈래 신설 · 16종 · 세기 값 전면 재조정
 M app/core/ffmpeg.py          build_filter 에 그림 폴더(folder=workdir) 전달
 M docs/DESIGN_effects_and_zoom.md   14·15절 신설 (v1.7)
 M requirements.txt            pillow·numpy 추가 (BOM 유지함)
 M tests/looks_test.py         5절 "화면을 가리지 않는가" 22개 신설 · 잣대 하나 고침
 M memory/ffmpeg-effect-layer-traps.md      함정 ③-e 신설
?? app/core/fxart.py           **새 모듈** — 그림을 그린다
?? tests/artfx_test.py         **새 점검 55개**
?? memory/effects-must-not-obscure-the-picture.md   **새 교훈**
?? memory/movie-filter-avoids-restructuring.md      **새 교훈**
?? handover_20260815_1310.md   이 문서
```

> 데모 `효과_가림비교_데모.mp4`·`효과_5단계_데모.mp4` 는 `.gitignore` 의 `/*.mp4` 가
> 막고 있어 커밋에 안 들어간다. **일부러 그렇게 두었다.**

---

## 4. 잡은 결함 넷 — 전부 "오류 없이 틀린 결과"

### ① 굴절 지도의 자가 눌려 **온 화면이 밀렸다**

방울 10개짜리가 화면의 **71%** 를 건드렸고 세기를 올려도 71% 그대로였다. 그림 파일의
밝기 0~255 가 필터 안에서 16~235 로 눌려, '안 밂'인 128 이 126 이 된 탓이다.

    지도를 통째로 128 로 채우고 재기 — 다른 값이 0이어야 정상
      그대로 넘김                                        663,681개  ❌
      format=yuv420p / yuvj420p / gbrp / scale=out_range=full  전부 ❌
      lutyuv=y='(val-minval)*255/(maxval-minval)'               0개  ✅

**확인 방법에 한 번 속았다.** 지도의 Y 를 `-pix_fmt gray` 로 뽑아 보니 128 이 나와
"범위 문제가 아니다"라고 결론 냈는데, 그 확인 경로가 범위를 되돌리는 변환을 거쳤다.
지도 값을 126~130 으로 바꿔 가며 **밀림이 0이 되는 값**을 직접 찾아서야 알았다.

### ② 비눗방울이 위가 아니라 **아래로 가라앉았다** (초당 72픽셀)

화면의 한 줄 r 에 보이는 것은 그림의 (r − y) 줄이라, y 가 커지면 그림이 아래로 흐른다.
**이것도 처음 검증에서는 "위로 간다"고 나왔다** — 밝아진 자리의 무게중심으로 쟀는데
화면 가득한 무늬는 무게중심이 늘 가운데다. 비 점검이 쓰는 **무늬 맞춰보기**로 바꾸자
방향과 속도가 정확히 나왔다.

### ③ 점검이 **틀린 잣대**를 쓰고 있었다

주변 어둡게의 "세 단계가 1.3배 벌어진다"가 **남은 밝기끼리** 나누고 있었다(63/47).
그것은 '어두워지는 정도'가 아니다 — 효과가 셀 때만 우연히 1.3을 넘는다. **어두워진
양**으로 고쳤다.

### ④ 값을 낮추다 **반대쪽 절벽**에 두 번 빠졌다

- 비·눈의 '약하게'가 아무 일도 안 하게 되었다(평균 밀림 0.00). `density` 는 **절벽처럼**
  동작한다 — 0.715 에서는 점이 남고 0.724 에서는 하나도 안 남는다
- 5단계의 물방울·폭죽이 **화면에서 안 보였다.** 수치는 상한에 한참 못 미쳐 통과했다.
  **데모를 눈으로 보고서야** 드러났다

---

## 5. 점검 현황 — 792개 (21개 스위트) · 전부 통과

| 스위트 | 개수 | 무엇 |
|---|---:|---|
| `tests/looks_test.py` | **107** | 색감·강조·값 조절 + **가림 상한 22개(신설)** |
| `tests/artfx_test.py` | **55** | **새로 만듦.** 물방울·비눗방울·폭죽 |
| `tests/weather_test.py` | 71 | 비·눈 |
| `tests/effects_test.py` · `effects_ui_test.py` | 39·39 | 효과 띠·화면 |
| 나머지 16개 | 481 | 기존 |

따로 도는 것: `longvideo_test.py`(17개, 10~40분) — **이번에 돌리지 않았다.**

점검이 일하는지 **일부러 망가뜨려 확인했다**: 옛 과한 값으로 되돌리자 정확히 3개가
실패했다(둘레 밝기 33%, 화면 밝기 38%, 남은 결 194%).

---

## 6. 다음에 할 일

1. **사용자가 데모 두 편을 본다** ← 지금 여기
   - `효과_가림비교_데모.mp4` (20초) — 왼쪽 예전 값 / 오른쪽 고친 값
   - `효과_5단계_데모.mp4` (24초) — 새 효과 3종
2. 값을 더 손볼 것이 있으면 고친다
3. **커밋하고 GitHub 에 push** (사용자가 "검토 후 한꺼번에 올리자"고 했다)
4. 설치본(8765)에 반영하고 배포
5. 남은 것: 4K·30분 영상 시험 · ElevenLabs 승인 대기 · 소재 칸 통합 ·
   마이크 녹음(F-45) · 내 목소리 나레이션(F-46) · EXIF 회전·`.heic` · 강제정렬 정확도

---

## 8. ★ 다음 작업 — 사용자가 데모를 보고 지시한 4건 (2026-08-15 13:35)

사용자가 `효과_가림비교_데모.mp4` 와 `효과_5단계_데모.mp4` 를 본 뒤 지시했다.
**원문 그대로 옮긴다:**

> 1. 고친 가림에 투명 정도를 사용자가 설정하도록 해줘
> 2. 물방물 맺힘, 물방울 원을 좀 작게
> 3. 비눗방울 원도 조금만 더 작계
> 4. 폭죽도 아주 작게

### 8-1. 「고친 가림에 투명 정도를 사용자가 설정하도록」

**뜻**: 세기 3단계(약하게·보통·많이)만으로는 사용자가 원하는 만큼을 못 고른다.
14절에서 낮춘 효과들에 **진하기(투명도) 슬라이더**를 붙여 직접 정하게 한다.

붙일 대상 — 14절에서 값을 낮춘 것들이다:
`spotlight`(주변 어둡게) · `vignette`(가장자리 어둡게) · `rain`(비) · `snow`(눈) ·
`warm` · `cool` · `vivid` · `vintage` · `mono`

**만드는 법은 이미 다 있다.** 등록표(`KINDS`)의 `params` 에 한 줄 적으면 화면이
슬라이더를 저절로 그린다. `app.js` 는 손댈 필요가 **없다**(13-1절).

```python
"params": {"opacity": _slider("진하기", 100, 20, 200, 5)}
```

**설계 판단 두 가지 (권고안, 사용자에게 확인받을 것):**

1. **기본값은 지금 값 그대로(100%)** 로 두고 슬라이더가 그것을 배로 곱하게 한다.
   그래야 아무것도 안 만진 사용자는 14절의 순한 값을 그대로 받는다.
2. **최대 200%** 로 두면 원하는 사람은 옛날의 진한 느낌까지 갈 수 있다.
   `color_adjust` 와 같은 논리다 — **사용자가 직접 정한 것은 상한을 걸지 않는다.**
   다만 `memory/effects-must-not-obscure-the-picture.md` 의 규칙상
   **제품이 사용자 값을 더 부풀리면 안 된다**(세기 배수를 1.15 로 낮춘 이유).

**점검에 반드시 반영할 것 (안 하면 거짓 통과한다):**
- `tests/looks_test.py` 5절의 가림 상한은 **기본값(진하기 100%)에서** 재야 한다.
  슬라이더를 붙였다고 상한 점검을 빼면 14절 작업이 통째로 무의미해진다.
- 슬라이더가 **실제로 화면을 바꾸는지** 재라 (이름만 있는 손잡이 금지).
  진하기 50%·100%·200% 의 평균 밀림이 1.3배 넘게 벌어져야 한다.

### 8-2. 「물방울 원을 좀 작게」

| 지금 | 어디 |
|---|---|
| `base = min(width, height) / 17.0 * scale` | `app/core/fxart.py` `draw_water_drops()` |
| 개수 `low 9 · medium 19 · high 34` | `app/core/effects.py` `KINDS["water_drops"]` |
| 크기 슬라이더 `방울 크기` 기본 100 (40~180) | 같은 곳 `params` |

**권고**: 나누는 수를 `17 → 21` 정도로 (방울 지름이 약 0.8배). 방울이 작아지면
덮는 면적이 줄므로 **개수를 1.3배쯤 올려야** 세기 사다리가 유지된다
(`12 / 25 / 44` 근처에서 재 볼 것).

> ⚠ **너무 줄이면 안 보인다.** 처음에 `/26` 으로 잡았다가 화면에서 거의 안 보여
> 다시 키웠다(15-5절). `/21` 은 그 사이 값이다. **반드시 데모를 다시 만들어 눈으로
> 확인**하고, `tests/artfx_test.py` 의 "'약하게'가 아무 일도 안 하지 않는다"(평균 밀림
> 0.3 이상)가 통과하는지 본다.

### 8-3. 「비눗방울 원도 조금만 더 작게」

| 지금 | 어디 |
|---|---|
| `base = min(width, height) / 42.0 * scale` | `app/core/fxart.py` `draw_bubbles()` |
| `swell` = `low 0.85 · medium 1.00 · high 1.25` | `app/core/effects.py` `KINDS["bubbles"]` |

**권고**: `42 → 50` (지름 약 0.84배). "조금만"이라고 했으므로 물방울보다 덜 줄인다.
비눗방울은 지금 화면의 9.8%(많이)를 덮어 셋 중 존재감이 가장 크므로, 개수는
올리지 말고 그대로 두는 편이 낫다.

### 8-4. 「폭죽도 아주 작게」

| 지금 | 어디 |
|---|---|
| 크기 슬라이더 `터지는 크기` 기본 **38** (15~70) | `app/core/effects.py` `KINDS["fireworks"]` |
| 알갱이 굵기 `grain = max(2.6, cell / 28.0)` | `app/core/fxart.py` `draw_fireworks()` |

**권고**: 기본값을 `38 → 22` 로 내리고 하한도 `15 → 10` 으로 넓힌다.

> ⚠ **여기서 앞의 실수를 되풀이하지 말 것.** 폭죽이 안 보였던 원인은 칸 크기가
> 아니라 **알갱이 굵기**였다(칸/46 → 칸/28 로 고쳐 밝은 화소 4,428 → 9,207).
> 칸을 작게 하면 `cell/28` 도 같이 작아지므로 **알갱이가 다시 점으로 사라진다.**
> 칸을 줄일 때는 나누는 수도 함께 줄여라 (`cell / 20` 근처). 판정은
> `tests/artfx_test.py` 의 "'약하게'가 아무 일도 안 하지 않는다" 와 **데모 눈 확인**.

### 8-5. 네 건 모두에 공통으로 지킬 것

1. **파이썬을 고쳤으면 서버를 다시 띄운다** (1절). 안 하면 옛 코드를 검사한다
2. 값을 바꿨으면 `tests/artfx_test.py` · `tests/looks_test.py` 를 돌린다.
   상한(위)과 바닥(아래)을 **둘 다** 본다 — 한쪽만 보면 반대쪽 절벽에 빠진다(4절 ④)
3. **데모를 다시 만들어 눈으로 확인한다.** 수치가 통과해도 안 보일 수 있다(15-5절).
   데모 만드는 스크립트는 이번 세션의 scratchpad 에 있었으나 **저장소에는 없다** —
   필요하면 다시 만들어야 한다 (`docs/DESIGN_effects_and_zoom.md` 15절 참고)
4. 다 끝나면 **커밋 + push**. 사용자가 "검토 후 한꺼번에 올리자"고 했고 이번 지시가
   그 검토의 결과다. 지금 `origin/main` 보다 3개 앞서 있다

---

## 7. 새 창에서 쓸 시작 프롬프트

```
capcut-studio-kit 프로젝트를 이어서 진행합니다.

먼저 다음을 읽어 주세요:
1) CLAUDE.md
2) memory/ 폴더 전체
3) handover_20260815_1310.md   ← 이 문서
4) docs/DESIGN_effects_and_zoom.md   (14·15절이 최신 현황)

지금 상태: 화면 효과 16종이 완성되어 개발 서버(8766)에 올라가 있고,
점검 792개가 전부 통과합니다. 승인된 5단계가 전부 끝났습니다.
아직 커밋하지 않았고 push 도 안 했습니다.

점검을 돌릴 때는 MOVIEFIT_TEST_URL=http://127.0.0.1:8766 을 주고
MOVIEFIT_TEST_PROJECTS 는 지워 주세요.

할 일: 인수인계 문서 8절의 4건을 해 주세요.
  1) 고친 가림에 투명 정도를 사용자가 설정하도록
  2) 물방울 맺힘 — 물방울 원을 좀 작게
  3) 비눗방울 원도 조금만 더 작게
  4) 폭죽도 아주 작게

값을 바꾼 뒤에는 점검(상한과 바닥을 모두)을 돌리고,
데모 영상을 다시 만들어 눈으로 확인한 결과까지 보여 주세요.
```
