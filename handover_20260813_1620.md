# 인수인계 — MovieFit Studio (v0.3.0 배포 직전)

> **v3.0** | 최종 업데이트: **2026-08-13 16:55** | 작성자: Claude Code

## 변경 이력

| 버전 | 일시 | 주요 변경 |
|------|------|---------|
| v1.0 | 2026-08-13 16:20 | 초기 작성. 설명서 자동 생성·점검 강화·설치 결함 고침까지 마치고 배포만 남은 시점 |
| v2.0 | 2026-08-13 16:45 | **오진 정정** — "설치가 조용히 실패한다"는 판정이 틀렸다(내 검사 코드의 인코딩 문제였다). **최신 설치 파일로 설치를 완료**했고 파일이 저장소와 완전히 같음을 해시로 확인. 프로그램도 다시 켰다. 남은 일은 **배포와 Phase 6** 뿐이다 |
| v3.0 | 2026-08-13 16:55 | **v0.3.0 배포 완료** — 태그·GitHub 릴리스·Vercel 운영 배포를 모두 마치고 각각 확인했다. **설치 중 안내 창을 눈으로 확인**(문구 전문을 3절에 적어 둠). **Vercel 배포 방법이 v1.0 에 잘못 적혀 있던 것을 바로잡았다** — git push 로 자동 배포되지 않는다. 남은 일은 **Phase 6** 뿐이다 |

---

## ⚠ v1.0 의 오진 정정 (반드시 읽을 것)

v1.0 에서 **"프로그램이 켜져 있으면 설치가 조용히 실패한다"** 고 적었고 커밋 `8887d7d` 의
메시지에도 그렇게 썼다. **그 판정은 틀렸다.**

- 근거로 삼은 것: PowerShell 로 설치본 `app.js` 를 읽어 한국어 문장을 찾았더니 없었고,
  파일 수정 시각도 그대로였다.
- **실제 원인**: 윈도우 PowerShell 5.1 의 `Get-Content` 는 BOM 없는 파일을 **cp949** 로
  읽는다. UTF-8 한글이 깨져서 못 찾은 것이다. **영어는 찾고 한국어만 못 찾았다.**
  파일 시각도 근거가 아니다 — **Inno Setup 은 원본 파일의 시각을 그대로 복사한다.**
- 해시로 다시 재니 저장소와 설치본이 **완전히 같았다.** 설치는 정상이었다.

`AppMutex` 장치 자체는 **실제로 동작하는 것을 확인했다** — 프로그램을 켜 둔 채 설치를
시도하니 중단되었고(`EAbort`, 종료 코드 1) 파일을 하나도 건드리지 않았다.
다만 **그 장치가 없었다면 정말 조용히 실패했을지는 시험하지 않았다.** 표준적인 안전장치라
남겨 두지만, **"그런 결함이 있었다"고 말해서는 안 된다.**

자세한 내용과 재발 방지: `memory/powershell-reads-korean-as-cp949.md`

---

## 0. 한 문단 요약

**v0.3.0 설치 파일까지 다 만들어 두었고, 남은 일은 "설치 → 확인 → 배포" 셋뿐이다.**
이번 세션에서 사용설명서를 원본 하나에서 자동 생성하게 바꾸고, 프로그램 안에서 열 수 있게 했다.
독립 검증에서 기능 결함은 0건이었으나 **점검 스크립트의 구멍 8개**를 찾아 메웠고, 사용자가 보내 준
화면에서 **오류 문구가 영어 원문으로 나오는 결함**과 **프로그램이 켜져 있으면 설치가 조용히
실패하는 결함**을 찾아 고쳤다. 자동 점검은 231개 → **248개**로 늘었다.

---

## 1. 지금 컴퓨터 상태 (새 창에서 제일 먼저 확인할 것)

| 항목 | 상태 |
|---|---|
| 개발 서버 | **8766 포트에 떠 있음** (버전 0.3.0, 새 코드) |
| 설치본 | **8765 포트에 떠 있음. 최신 빌드로 설치 완료됨** (해시로 저장소와 동일함을 확인) |
| 최신 설치 파일 | `build/dist/MovieFitStudio-Setup.exe` (120.6MB, **16:03:46**에 만든 것) |
| 작업 트리 | 깨끗함 |
| 태그·릴리스 | **아직 v0.3.0 없음** (v0.2.1이 최신) ← **남은 일은 이것뿐** |

> **⚠ 가장 중요한 주의**: 설치본과 개발본이 **둘 다 떠 있다.** 점검 스크립트를 그냥 돌리면
> **개발본이 아니라 설치본을 검사하고서 "통과"라고 말한다.** 반드시 이렇게 겨냥할 것:
>
> ```
> $env:MOVIEFIT_TEST_URL="http://127.0.0.1:8766"
> ```
>
> 그리고 **겨냥한 서버가 정말 새 코드인지 먼저 확인**할 것 (버전 번호만으로는 부족하다):
> ```
> python -c "import urllib.request as u; print('link-guide' in u.urlopen('http://127.0.0.1:8766/').read().decode())"
> ```
> 자세한 내용: `memory/test-must-target-the-right-server.md`

---

## 2. 이번 세션에서 한 일 (커밋 4개)

### `59deb46` 사용설명서를 원본 하나에서 자동 생성한다

설명서가 **세 곳**에 필요해졌다: 마크다운 원본, 소개 사이트, 프로그램 안.
손으로 복사하면 반드시 어긋나므로 생성기를 만들었다.

- **`tools/build_guide.py`** (신규) — `docs/USER_GUIDE.md` 하나에서 `landing/guide.html` 과
  `app/static/guide.html` 을 만든다. **바깥 부품(pip 설치)을 쓰지 않는다.**
  `--check` 로 어긋남을 잡는다.
- **`app/static/guide.html`** (자동 생성물, 직접 고치지 말 것)
- **`app/static/guide.css`** (사람이 고치는 파일) — 배치 규칙만. 색은 `style.css` 의 `:root`
  변수를 쓴다. 색 값을 여기 베끼면 나중에 화면 색을 바꿀 때 여기만 옛 색으로 남는다.
- **진입점 2개** — 첫 화면 아래 `[사용설명서]`(`#link-guide`), 작업 화면 오른쪽 위
  `[?]`(`#link-guide-top`). 둘 다 `target="_blank"` 로 **새 탭**에서 열어 하던 작업을 잃지 않는다.
- **`docs/USER_GUIDE.md` v1.0 → v2.0** — 새 8장 「화면비 바꾸기」를 통째로 추가, 7장에 위치
  화살표, 3장에 `[저장됨]`·`Ctrl`+`S`·설명서 여는 길. 9~15장은 번호가 밀렸다.
- 버전을 **0.3.0** 으로 올렸다 (`app/__init__.py`).

### `81a8b30` 독립 검증이 지적한 점검의 구멍을 메운다

기능 결함은 0건이었지만, 검증자가 **"통과하는데도 실제로는 망가져 있을 수 있는 경우" 8가지**를
찾아냈다. 핵심은 **동어반복 검증**이다.

| 구멍 | 무엇이 문제였나 | 어떻게 메웠나 |
|---|---|---|
| `--check` | 생성기가 만든 것을 **같은 생성기로** 다시 만들어 비교한다. 한 장을 통째로 삼켜도 언제나 통과 | 원본 마크다운의 문단을 HTML 본문과 **전수 대조** |
| 단추 이름 | 설명서 안에 낱말이 있는지만 봄. **화면의 이름이 바뀌어도 초록불** | `index.html` 의 실제 글자와 대조 |
| 1%·5% | 글자만 봄. 실제 값을 2%로 바꿔도 통과 | `app.js` 의 `NUDGE_STEP` 상수와 대조 |
| 404×720 | `phase4` 는 **서버에 정답을 물어보고** 비교 → 서버가 틀리면 기대값도 같이 틀림 | 숫자를 점검 파일에 **못박음** |
| 색 검사 | `#RRGGBB` 만 찾아 `#fff`·`rgb()`·색 이름을 놓침 | 정규식 확장 (단 `white-space` 같은 속성명은 제외) |
| 클릭 | `id`·`href` 정규식만 봄. 가려져 있어도 통과 | 브라우저로 **실제로 눌러** 새 탭이 열리는지 확인 |

**그리고 일부러 망가뜨려 점검이 정말 작동하는지 확인했다** — 단추 이름과 이동 폭을 바꾸자
정확히 그 두 항목이 실패했고, 되돌리니 다시 전부 통과했다. 이 확인이 없으면 "점검을 늘렸다"는
말은 아무 뜻이 없다.

### `ff10ebe` 서버에 닿지 못할 때 영어 원문 대신 안내를 준다

사용자가 설치본 첫 화면 사진을 보내 왔고 아래에 빨간 **"Failed to fetch"** 가 떠 있었다.

원인: `api()`(`app.js` 30행)가 **HTTP 오류(404·500)만** 한국어로 바꾸고 있었다. 서버에 아예
닿지 못하면 `fetch()` 자체가 영어 오류를 던지는데 그것이 손대지 않은 채 화면까지 올라왔다.

이 상황은 드물지 않다 — **서비스 워커가 화면 파일을 캐시**하므로 서버가 꺼져 있거나 켜지는
중이어도 화면은 그려지는데, API 요청만 실패한다. 사용자 눈에는 "화면은 멀쩡한데 빨간 영어
오류만 뜨는" 상태가 된다.

고침 문구: `"프로그램과 연결이 끊겼습니다. 검은 명령 창이 켜져 있는지 확인한 뒤 F5로 새로고침해 주세요."`
점검: `ui_test.py` 에 요청을 일부러 끊고(`route.abort`) 화면 글을 읽는 항목 2개.
**이 시험은 브라우저 오류를 일부러 만들므로 "자바스크립트 오류 없음" 검사보다 뒤에 두어야 한다.**

### `8887d7d` 프로그램이 켜져 있으면 설치가 조용히 실패하던 것을 막는다

**가장 중요한 발견.** 프로그램을 켜 둔 채 새 버전을 설치하면 파일이 잠겨 덮어쓰기가 실패하는데,
설치 프로그램은 **아무 말 없이 "완료"라고 끝난다.** 실제로 이 컴퓨터에서 벌어졌다 —
종료 코드 0인데 `app/static/app.js` 의 수정 시각이 이전 설치 때 그대로였다.

- `app/__main__.py` — 서버가 뜰 때 윈도우에 표시(뮤텍스)를 남긴다. `RUNNING_MARK = "MovieFitStudioRunning"`
- `tools/build_installer.py` — 설치 스크립트에 `AppMutex` 를 넣어 먼저 확인하고 멈춘다.
  `[Messages]` 로 안내를 바꿨다: **브라우저 탭만 닫아서는 안 꺼지고 검은 명령 창을 닫아야 한다**
  는 점을 명시. 기본 문구는 "응용 프로그램이 실행 중입니다"라고만 해서 알 수 없었다.

> **곁들여 알아낸 것**: 설치할 때 쓰던 `/S` 는 **NSIS용 옵션**이고 이 설치 파일은 **Inno Setup**
> 이라 통하지 않는다. **`/VERYSILENT`** 를 써야 한다. (이것이 두 번째 설치가 안 먹은 원인일 수도 있다)

---

## 3. 지금 바로 이어서 할 일 (순서대로)

### ① 설치 — **이미 끝났다. 다시 할 필요 없다.**

최신 설치 파일로 설치를 마쳤고, 설치본 파일이 저장소와 **완전히 같음을 해시로 확인**했다.
프로그램도 다시 켜서 8765에서 돌고 있다 (설명서 열림, 화면비 장 확인).

**"실행 중이면 알려 주기"도 확인했다** — 프로그램을 켜 둔 채 설치를 시도하니 중단되었고
(`EAbort`, 종료 코드 1) 파일을 하나도 건드리지 않았다.

**안내 창도 눈으로 확인했다 (v3.0 — 16:38).** 프로그램을 켠 채 설치 파일을 GUI로 실행하니
한국어 안내 창이 떴다. 문구 전문:

> MovieFit Studio 가 지금 실행 중입니다. / 먼저 프로그램을 닫아 주세요. /
> 브라우저 탭만 닫아서는 꺼지지 않습니다. 제목이 "MovieFit Studio" 인 검은 명령 창을 닫아야
> 완전히 종료됩니다. / 닫으신 뒤 [확인]을 누르면 설치를 계속합니다.

단추도 한국어(`확인`·`취소`)다.

> **이 창은 화면 전체를 찍어서는 안 잡힌다.** 다른 창 뒤에 가려지기 때문이다. 창 자체의
> 그림을 뽑는 `PrintWindow` 로 찍어야 한다. 그리고 화면 배율 때문에 창이 알려 주는 크기
> (471×248)보다 실제 그림이 크므로, 넉넉한 크기(1100×620)로 잡아야 잘리지 않는다.
> 방법: `memory/capture-a-dialog-with-printwindow.md`

> **파일 내용을 확인할 때 PowerShell 로 읽지 말 것.** 한글이 cp949로 읽혀 조용히 실패한다.
> 파이썬으로 `encoding="utf-8"` 을 밝혀 읽거나, 아예 해시를 비교할 것.
> (`memory/powershell-reads-korean-as-cp949.md`)

설치본을 겨냥한 점검을 돌려 두면 좋다:
```powershell
$env:MOVIEFIT_TEST_URL="http://127.0.0.1:8765"
python tests/guide_test.py ; python tests/smoke_test.py
```

### ② 배포 — **이미 끝났다 (v3.0 — 16:50). 다시 할 필요 없다.**

| 한 일 | 확인한 방법 |
|---|---|
| `git push origin main` | `main...origin/main` 이 나란해짐 (커밋 10개 올라감) |
| `v0.3.0` 태그 (주석 달린 태그) | `git ls-remote --tags origin` 에 보임 |
| GitHub 릴리스 | `gh api .../releases/latest` 의 `tag_name` = `v0.3.0`, 첨부 120.6MB `uploaded` |
| Vercel 운영 배포 | 사이트가 주는 `/guide` 가 21,477바이트로 로컬 파일과 같고 8장(화면비)이 보임 |

릴리스 주소: https://github.com/parkjikoon-hub/moviefit-studio/releases/tag/v0.3.0

> **⚠ v1.0 이 여기서 틀렸다.** "소개 사이트는 `git push` 하면 자동 배포된다"고 적혀 있었으나
> **그렇지 않다.** 깃 연동이 걸려 있지 않고, `landing/` 폴더 하나가 Vercel 프로젝트로
> 연결되어 있다(`landing/.vercel/project.json`). 명령줄로 직접 올려야 한다:
>
> ```powershell
> vercel deploy --prod --cwd landing
> ```
>
> `--cwd` 를 앞에 두면 **미리보기에만** 올라간다. 자세한 내용과 확인법:
> `memory/vercel-landing-is-cli-deployed.md`

첫 페이지의 내려받기 단추는 `releases/latest/download/...` 를 가리키므로 **버전을 손으로
고칠 필요가 없다.** 릴리스를 만들면 자동으로 새것을 가리킨다 (확인함).

### ③ Phase 6 착수 (사용자와 이미 합의됨) ← **여기부터 하면 된다**

`docs/ROADMAP.md` 의 **Phase 6** 에 7단계와 수용 기준이 적혀 있다. 설계 근거는
`docs/RESEARCH_image_and_music_video.md` (660줄, FFmpeg 실측 포함).

사용자가 정한 것:
- 시작 화면 **카드 4개** `[영상으로] [사진으로] [음원으로] [대본으로]`
- 사진 **전환 효과는 이번 범위에서 제외** (딱딱 바뀜)
- 이미지는 **가사 자막이 바뀔 때** 함께 넘어간다
- 가사 시각은 **두드려 맞추기**로 직접 찍는다 (이미 구현되어 있음)
- 강제정렬은 **실험 없이 바로 구현** (품질은 미지수 — 단계 6 수용 기준에서 드러난다)
- 정규화 중간 파일은 프로젝트 폴더 `cache/` 에 남긴다 (내가 정한 것)

**단계 1부터 순서대로.** 1~5는 반드시 순서대로이고, 6·7은 독립적이다.

---

## 4. v0.3.0 에 들어간 것 (릴리스 노트용)

**새 기능**
- **화면비 바꾸기** — 가로 16:9 / 세로 9:16 / 정사각 1:1 / 원본 그대로. 서로 오갈 수 있다.
  잘라내기(가장자리를 잘라 꽉 채움) / 여백 채우기(흐린 배경). 미리보기에 밝은 틀이 생기고
  **틀을 마우스로 끌어** 어디를 남길지 정한다. 자막이 틀 밖으로 나가면 넘치는 픽셀과 해결법을 알린다.
  영상 내보내기 3종 모두에 적용. (1280×720 → 세로 9:16 잘라내기 = **404×720**, 실측 확인)
- **자막 위치 화살표** — 1%씩, `Shift`와 함께 5%씩
- **프로그램 안 사용설명서** — 첫 화면 `[사용설명서]`, 작업 화면 `[?]`. 새 탭, 인터넷 없이 열림
- **`[저장됨]` 클릭·`Ctrl`+`S`** 로 즉시 저장

**고친 것**
- 화면비를 바꿨을 때 미리보기 자막 위치가 결과물과 달랐던 문제 (최대 438픽셀)
- 화면비를 고른 뒤 자막을 마우스로 끌 수 없게 되던 문제
- 서버에 닿지 못할 때 영어 원문("Failed to fetch")이 그대로 뜨던 문제
- 프로그램이 켜져 있으면 설치가 조용히 실패하던 문제

---

## 5. 점검 현황 — 248개

| 점검 | 개수 | 무엇을 보나 |
|---|---|---|
| `tests/guide_test.py` | 32 | 설명서가 실제로 열리는가, 세 곳이 어긋나지 않는가, **설명서가 실제 화면과 맞는가** |
| `tests/ui_test.py` | 47 | **브라우저를 띄워 실제로 눌러** 확인. 화면비·화살표·저장·설명서·연결 끊김 안내 |
| `tests/smoke_test.py` | 46 | 서버 기본 기능 전반 |
| `tests/phase1_test.py` | 26 | 영상 → 자막 |
| `tests/phase2_test.py` | 33 | 나레이션 (인터넷 필요) |
| `tests/phase3_test.py` | 18 | 편집 완성도 |
| `tests/phase4_test.py` | 46 | 화면비 — `app.js` 파일을 node로 실행해 서버 계산과 240가지 대조 |

전부 `MOVIEFIT_TEST_URL` 환경변수로 겨냥할 서버를 정한다. 기본값은 8765(설치본)다.

---

## 6. 반드시 읽어야 할 교훈 (`memory/`)

새 세션은 **`memory/` 폴더 전체를 먼저 읽는다.** 특히:

- **`test-must-target-the-right-server.md`** — 지금 설치본·개발본이 둘 다 떠 있으므로 최우선
- **`generated-files-need-a-different-kind-of-test.md`** (이번 세션 신규) — 생성기를 만들면
  점검이 조용히 무력해진다. 동어반복 검증의 여러 얼굴
- `preview-must-use-the-output-frame.md` — Phase 6에서 **같은 결함이 재발할 위험이 있다**
  (사진·음원 프로젝트는 `sourceDimensions()` 가 null이 된다)
- `design-change-has-copies-elsewhere.md` — 같은 것이 여러 곳에 사본으로 있는 문제
- `verify-ui-by-actually-clicking.md` — 브라우저 자동화 사용법과 걸림돌

---

## 7. 새 창에서 쓸 시작 프롬프트

```
capcut-studio-kit 프로젝트를 이어서 진행합니다.

먼저 다음을 읽어 주세요:
1) CLAUDE.md
2) memory/ 폴더 전체 (특히 test-must-target-the-right-server.md 와
   generated-files-need-a-different-kind-of-test.md)
3) handover_20260813_1620.md  ← 이 문서
4) docs/ROADMAP.md 의 Phase 6

지금 상태: v0.3.0 설치 파일까지 다 만들어 두었고 배포만 남았습니다.
설치본(8765)과 개발본(8766)이 둘 다 떠 있으니 점검할 때 어느 쪽을 겨냥하는지
반드시 확인해 주세요.

할 일:
① 설치본을 끄고 최신 설치 파일로 설치한 뒤, 덮어쓰기가 실제로 됐는지 확인하고
   다시 켜 주세요. (사용자 허락은 이미 받았지만 끄기 전에 한 번 알려 주세요)
② 프로그램을 켜 둔 채 설치 파일을 실행했을 때 한국어 안내 창이 뜨는지
   눈으로 확인해 주세요. 이번에 새로 넣은 기능입니다.
③ 문제가 없으면 v0.3.0 태그를 달고 GitHub 릴리스와 Vercel 배포를 해 주세요.
④ 그다음 Phase 6 단계 1(사진이 들어올 통로)에 착수해 주세요.
```

---

## 8. 아직 안 된 것 (정직하게)

- ~~v0.3.0 배포 안 됨~~ → **끝났다** (v3.0 — 태그·릴리스·Vercel 모두 확인 완료)
- ~~"실행 중이면 알려 주기"의 안내 창을 눈으로 확인 안 함~~ → **봤다** (v3.0 — 3절 ① 참고)
- **AppMutex 가 없었다면 정말 조용히 실패했을지는 시험하지 않았다** (위 오진 정정 참고)
- **소개 사이트 첫 페이지(`landing/index.html`)에는 v0.3.0 의 새 기능이 안 적혀 있다.**
  설명서(`/guide`)만 새것이다. 첫 페이지에 화면비 기능을 소개할지는 정하지 않았다
- **「내 목소리로 나레이션」·마이크 녹음은 화면에만 있고 동작하지 않는다**
  (`app.js` 1366행 — 누르면 "엔진 검토를 마치는 대로 연결됩니다" 안내만). Phase 4 미착수
- **Phase 6은 계획만** 세운 상태
- `USER_GUIDE.md` 만 보고 처음 쓰는 사람이 끝까지 갈 수 있는지 **시험하지 못했다**
