# 인수인계 — MovieFit Studio

> **v1.0** | 최종 업데이트: **2026-08-12 11:11** | 작성자: Claude Code

## 변경 이력

| 버전 | 일시 | 주요 변경 |
|------|------|---------|
| v1.0 | 2026-08-12 11:11 | 초기 작성. Phase 0~3 완료, GitHub 공개, Vercel 배포까지 마친 시점 |

---

## 1. 이 프로젝트가 무엇인가

**MovieFit Studio** — 영상 자막을 자동으로 만들고, 대본으로 AI 나레이션까지 만드는 윈도우용 무료 도구.
`run.bat`을 더블클릭하면 브라우저가 열리는 "내 컴퓨터 안에서만 도는 웹앱" 방식이다.

- 작업 폴더: `c:\Users\INTEL\Antigravity\capcut-studio-kit`
  (폴더 이름은 옛 이름 그대로다. 제품 이름만 MovieFit Studio로 바뀌었다)
- GitHub (공개): <https://github.com/parkjikoon-hub/moviefit-studio>
- 소개 사이트 (Vercel): <https://moviefit-studio.vercel.app>
- 라이선스: MIT

문서 우선순위: `docs/PRD.md` > `docs/TECH_SPEC.md` > `docs/UI_SPEC.md` > `docs/ROADMAP.md`

---

## 2. 지금 어디까지 되어 있는가 (v1.0 기준 — 2026-08-12 11:11)

### 완료된 단계

| 단계 | 내용 | 검증 |
|------|------|------|
| Phase 0 | 서버 뼈대, 프로젝트 저장, PWA, 실행 스크립트 | ✅ 독립 검증 |
| Phase 1 | 영상 → 자막 자동 생성, 편집기, SRT·번인 내보내기 | ✅ 독립 검증 + 결함 5건 수정 |
| Phase 2 | 대본 → 나레이션 + 자막 타이밍 자동 산출 | ✅ 독립 검증 + 결함 3건 수정 |
| Phase 3 | 사전, 자막 가져오기, 스타일, 자유 위치 배치 | ✅ 실측 검증 18항목 |

자동 점검 **총 123개 항목 통과**:
`tests/smoke_test.py` 46 · `tests/phase1_test.py` 26 · `tests/phase2_test.py` 33 · `tests/phase3_test.py` 18

### 실제로 되는 것

- 영상·오디오(mp3 포함)를 넣으면 로컬 음성인식으로 자막 자동 생성, 진행률 표시
- 목록형 편집기 + 타임라인 막대 + 소리 파형 + 읽기속도 경고 + 자막 검사
- 되돌리기/다시하기, 찾기·바꾸기, 전체 시간 밀기, 무음 자동 분할, 두드려 맞추기, A-B 반복
- 대본 → 문장별 나레이션 + **자막 타이밍 자동 산출** (핵심 차별점 D1)
- 문장별 목소리·속도·쉼 개별 지정, 한 문장만 다시 만들기
- 자막 스타일 전체 + **마우스로 끌어 자유 배치** (영상에도 오차 0.2%로 반영)
- 내보내기: SRT / VTT / 10초 미리보기 / 자막 새긴 mp4 / 나레이션 오디오 / 나레이션 입힌 영상
- 자동 백업 (저장할 때마다 최근 10개 보관, 되돌리기 API 있음)

### 남은 것

- **Phase 4**: 내 목소리 나레이션(F-46), 마이크 녹음(F-45), 단어 단위 타임스탬프 재분할(F-13)
- **Phase 5**: 사용자 설명서(`USER_GUIDE.md`), 30분 이상 긴 영상 성능 점검
- **F-54/F-55**: 10초 미리보기는 완료. 투명 배경 자막 영상(webm) 내보내기는 미착수

---

## 3. 반드시 알아야 할 것 (모르면 시간을 크게 잃는다)

`memory/` 폴더에 교훈이 파일 하나에 하나씩 정리되어 있다. **새 세션을 시작하면 먼저 읽을 것.**

| 파일 | 요약 |
|------|------|
| `bat-file-must-be-crlf.md` | `.bat` 파일은 줄바꿈이 CRLF여야 한다. LF면 cmd가 각 줄 앞글자를 잘라먹는다 |
| `windows-console-encoding.md` | 윈도우 명령창은 cp949라 `—` 같은 문자에서 프로그램이 죽는다. 진입점마다 UTF-8 설정 필요 |
| `ffmpeg-filter-path-escaping.md` | FFmpeg 필터의 경로는 역슬래시 **2개**. 폴더명에 `,;[]'`가 있으면 그래도 깨진다 → 상대경로+cwd로 해결 |
| `whisper-first-run-is-slow.md` | 음성인식 첫 실행은 486MB를 받느라 6분. 미리 안내하지 않으면 사용자가 창을 닫는다 |
| `tts-silence-padding.md` | edge-tts는 문장 앞뒤에 무음을 붙인다. **"정확히 쟀다"와 "옳은 것을 쟀다"는 다르다** |

### 검증 방식에 대한 교훈

이 프로젝트에서 발견된 결함 대부분은 **자체 테스트가 놓친 것**이었다. 이유는 두 가지다.

1. **동어반복 검증** — "자막 길이 == 음성 길이"를 비교했는데, 자막 길이를 음성 길이로 계산했으니
   언제나 통과한다. 실제 오디오 파일을 따로 재어 대조해야 잡힌다.
2. **결과물을 안 봄** — 자막이 영상에 제대로 새겨졌는지는 프레임을 뽑아 **눈으로 보거나**
   픽셀을 측정해야 안다. 코드가 맞게 생겼다는 것으로는 부족하다.

그래서 각 Phase 종료 시 **별도 컨텍스트의 검증 서브에이전트**를 띄워
"내 설명을 믿지 말고 직접 실행해 확인하라"고 지시했다. 이 방식이 실제로 결함 8건을 잡았다.
**Phase 4를 끝낼 때도 같은 방식을 쓸 것.**

### 아직 확인하지 못한 것 (정직한 고지)

브라우저를 자동으로 조작할 수단이 없어 **마우스·키보드로만 쓰는 5가지는 코드 검토로만 판정**했다:
자막 나누기·합치기, 타임라인 막대 끌기, 읽기 속도 색상, 찾기·바꾸기, Ctrl+Z 되돌리기.
실사용 중 이상이 보이면 여기부터 의심할 것.

또한 **진짜 사람 목소리가 든 긴 영상으로는 아직 시험하지 않았다.** 지금까지는 10초짜리 합성 영상과
4~5초짜리 TTS 음성 파일로만 확인했다.

---

## 4. 파일 구조 (핵심만)

```
app/
├─ server.py             FastAPI 앱, 라우터 등록, woff2 MIME 등록
├─ config.py             경로·포트·허용 확장자
├─ core/
│   ├─ jobs.py           오래 걸리는 작업 + 진행률 + 취소 (앞 3인자는 위치 전용 `/`)
│   ├─ stt.py            faster-whisper 음성인식
│   ├─ subtitles.py      자막 분할·줄바꿈·SRT/VTT 읽고 쓰기 (쓰기는 직접 구현)
│   ├─ narration.py      대본 → 나레이션 → 자막 타이밍 확정 (D1의 본체)
│   ├─ audio_mix.py      문장 이어붙이기·영상 믹싱·덕킹·무음 잘라내기
│   ├─ ffmpeg.py         자막 번인·미리보기·ASS 생성·진행률 파싱
│   ├─ audio_analysis.py 파형 추출·무음 감지
│   ├─ style_map.py      스타일 단일 출처 (CSS ↔ ASS 매핑)
│   ├─ projects.py       프로젝트 JSON 저장·자동 백업
│   ├─ fonts.py          글꼴 목록
│   ├─ ffprobe.py        길이 실측
│   ├─ filedialog.py     윈도우 파일 선택 창 (kind: media | subtitle)
│   └─ tts/              base.py(인터페이스) + edge.py(기본 엔진)
├─ api/                  projects · media · system · styles · tts · audio · jobs · render · narration
└─ static/               index.html · style.css · app.js · fonts/ · icons/

tests/    smoke_test · phase1_test · phase2_test · phase3_test · sample/
tools/    check_env · launch · make_icons · make_sample · fetch_font
landing/  Vercel 소개 페이지 (별도 배포)
memory/   교훈 저장소
docs/     PRD · TECH_SPEC · UI_SPEC · ROADMAP · DEV_KIT · RESEARCH_voice_cloning
```

---

## 5. 실행 방법

```
run.bat                      사용자용 — 더블클릭하면 서버 기동 + 브라우저 열림
python -m app                개발용 서버 (http://127.0.0.1:8765)
python tools/check_env.py    환경 점검
python tests/smoke_test.py   기본 동작 46항목  (서버를 켠 상태에서)
python tests/phase1_test.py  자막 생성·내보내기 26항목
python tests/phase2_test.py  나레이션·동기화 33항목
python tests/phase3_test.py  스타일·위치·사전 18항목
```

배포:
```
cd landing && vercel deploy --prod --yes     소개 사이트 갱신
git push origin main                          GitHub 반영
```

---

## 6. 내 목소리 나레이션 (Phase 4) — 이미 조사 완료

`docs/RESEARCH_voice_cloning.md`에 상세 비교가 있다. 결론만:

**음성 복제 엔진을 새로 붙이지 말고, 만들어진 음성의 "음색만 바꾸는 후처리"를 넣는다.**

```
대본 → edge-tts (한국어 발음 담당) → mp3 → OpenVoice v2 음색 변환 → 내 목소리 mp3
```

- 한국어 발음이 약한 복제 엔진들의 문제를 통째로 피한다
- OpenVoice v2는 코드·가중치 모두 MIT (사용자가 유튜브 수익을 낼 수 있어야 하므로 결정적)
- XTTS-v2는 **상업적 사용 금지**라 탈락 (제작사도 폐업해 라이선스를 살 수 없음)
- 내려받는 용량 약 0.4GB → **선택 설치(`requirements-voice.txt`)로 분리할 것**
- 한계: 음색만 옮기고 말투·억양은 못 옮긴다 → UI에 반드시 명시
- **아직 실측 안 함**: CPU에서의 변환 속도. 붙이기 전에 `tests/sample/`로 재고 그 수치를 UI에 넣을 것

마이크 녹음(F-45)이 목소리 등록의 전제다. 브라우저 MediaRecorder로 구현.

---

## 7. 새 창에서 시작할 때 쓸 프롬프트

```
c:\Users\INTEL\Antigravity\capcut-studio-kit 에서 MovieFit Studio 개발을 이어서 합니다.

먼저 이것부터 읽어 주세요:
1) handover_20260812_1111.md  (이 문서 — 전체 상황)
2) memory/ 폴더의 모든 파일   (같은 실수를 반복하지 않기 위해)
3) docs/ROADMAP.md            (Phase별 완료 상태와 검증 근거)

Phase 0~3은 완료되어 GitHub와 Vercel에 배포되어 있습니다.
다음으로 [여기에 하고 싶은 작업을 적으세요]를 진행해 주세요.

작업 규칙:
- 기능 하나를 완성할 때마다 서버를 실제로 띄우고 tests/ 의 점검 스크립트를 돌려서
  통과를 확인한 뒤에만 완료라고 말해 주세요.
- Phase를 끝낼 때는 별도 컨텍스트의 검증 서브에이전트를 띄워
  "내 설명을 믿지 말고 직접 실행해 확인하라"고 지시해 주세요.
  지금까지 이 방식으로 결함 8건을 잡았습니다.
```

권장 다음 작업 순서:

1. **실제 영상으로 손수 써 보기** — 사람 목소리가 든 몇 분짜리 영상. 지금까지 확인하지 못한
   부분(마우스 조작 5가지, 긴 영상 성능)이 여기서 드러난다. 이게 가장 값싼 검증이다.
2. Phase 5의 `USER_GUIDE.md` — 배포를 시작했으니 쓰는 사람에게 설명서가 먼저 필요하다.
3. Phase 4 (내 목소리 나레이션) — 용량이 크고 검증이 까다로우니 위 둘 다음에.

---

## 8. 사용자에 대해 기억할 것

- 공학박사이나 **소프트웨어 비개발자**다. 모든 대화는 한국어로, 전문 용어에는 쉬운 설명을 붙인다.
- 실행할 일이 있으면 "1) … 2) …"처럼 따라 할 수 있는 순서로 알려 준다.
- **관성적으로 동의하지 말 것.** 지적을 받으면 프로젝트 목적에 비추어 독립적으로 검토한 뒤
  근거와 함께 답한다. 문제가 있으면 대안과 함께 솔직히 반론한다.
- 이 프로그램을 **필요한 사람들에게 배포할 계획**이다. 그래서 라이선스·상표·정직한 기능 표기가 중요하다.
