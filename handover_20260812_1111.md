# 인수인계 — MovieFit Studio

> **v4.1** | 최종 업데이트: **2026-08-13 12:35** | 작성자: Claude Code
>
> **이 문서만 읽고 새 창에서 바로 이어서 작업할 수 있다.**
> 기능 4건(F-A~F-D)은 **완료**되었다 (점검 208개 통과, 커밋 `788c4ce`).
> 다음 작업은 4절의 **①-1·①-2 (설명서 반영 → v0.3.0 재배포)** 다.
> **시작하자마자 사용자에게 물어야 할 것이 하나 있다** — 4절 ①-1의 ⛔ 상자를 볼 것.

## 변경 이력

| 버전 | 일시 | 주요 변경 |
|------|------|---------|
| v1.0 | 2026-08-12 11:11 | 초기 작성. Phase 0~3 완료, GitHub 공개, Vercel 배포까지 마친 시점 |
| v2.0 | 2026-08-12 16:24 | 설치 파일(exe) 배포 방식 추가, 화면 디자인 전면 개편, 발견된 결함 다수 수정. 남은 작업 목록 갱신 |
| v3.0 | 2026-08-12 17:12 | **v0.2.0 재배포 완료** — 개발본과 설치본의 불일치 해소. 시작 화면 테두리, 앱 아이콘, 소개 사이트를 모두 크림 체계로 통일. 남은 작업에서 ①번(재배포) 삭제 |
| v3.1 | 2026-08-12 17:55 | **사용설명서 작성·배포**(`docs/USER_GUIDE.md` + 소개 사이트 `/guide`). 소개 사이트의 한글 줄바꿈·양쪽 정렬·상자 테두리 개선, 스타일을 `landing/style.css`로 분리 |
| v3.2 | 2026-08-13 07:50 | **v0.2.1 배포** — `manifest.webmanifest`(앱 설치 설정)의 색이 개편 전 값이던 것을 고침. 서비스 워커 저장 이름 v2로. 색 누락 지점이 3곳 → 4곳으로 늘어 memory 갱신 |
| v3.3 | 2026-08-13 08:10 | 프로그램 안에 설명서로 가는 길이 없다는 것을 남은 작업 ①-2로 기록(사용자 결정: 기능 4건과 함께 처리). 바탕화면 아이콘이 무엇을 가리키는지 확인 결과 추가 |
| v3.4 | 2026-08-13 10:05 | **세션 종료 정리.** 아이콘 캐시 교훈을 5절 목록에 추가. 바탕화면 아이콘 색 문제 해결 확인(사용자 확인 완료). 새 창 인수인계용으로 마감 |
| v4.1 | 2026-08-13 12:35 | 새 창 인수인계용 마감. ①-1에 **시작 전 사용자에게 물어야 할 결정**(설명서를 세 곳에 두는 방법)을 ⛔ 상자로 명시. 8절 시작 프롬프트 갱신 |
| v4.0 | 2026-08-13 12:25 | **기능 4건 완료** (F-A 화면비 전환 / F-B 위치 화살표 / F-C 롱폼·숏폼 / F-D 저장됨 클릭). 새 모듈 `app/core/framing.py`. **브라우저 자동화(Playwright) 도입** — 화면 기능을 코드 검토가 아니라 실제 클릭으로 검증. 독립 검증에서 결함 2건(미리보기 좌표 어긋남 438px, 자막 끌기가 막힘)을 찾아 고침. 점검 **208개 통과**. 남은 작업이 ①-1·①-2(설명서·재배포)로 바뀜 |

---

## 1. 이 프로젝트가 무엇인가

**MovieFit Studio** — 영상 자막을 자동으로 만들고, 대본으로 AI 나레이션까지 만드는 윈도우용 무료 도구.

- 작업 폴더: `c:\Users\INTEL\Antigravity\capcut-studio-kit`
  (폴더 이름은 옛 이름 그대로다. 제품 이름만 MovieFit Studio로 바뀌었다)
- GitHub (공개): <https://github.com/parkjikoon-hub/moviefit-studio>
- 소개 사이트 (Vercel): <https://moviefit-studio.vercel.app>
- 설치 파일 (최신): <https://github.com/parkjikoon-hub/moviefit-studio/releases/tag/v0.2.0>
- 라이선스: MIT

문서 우선순위: `docs/PRD.md` > `docs/TECH_SPEC.md` > `docs/UI_SPEC.md` > `docs/ROADMAP.md`

---

## 2. 지금 어디까지 되어 있는가 (v3.0 기준 — 2026-08-12 17:12)

| 단계 | 내용 | 검증 |
|------|------|------|
| Phase 0 | 서버 뼈대, 프로젝트 저장, PWA, 실행 스크립트 | ✅ 독립 검증 |
| Phase 1 | 영상 → 자막 자동 생성, 편집기, SRT·번인 내보내기 | ✅ 독립 검증 + 결함 5건 수정 |
| Phase 2 | 대본 → 나레이션 + 자막 타이밍 자동 산출 | ✅ 독립 검증 + 결함 3건 수정 |
| Phase 3 | 사전, 자막 가져오기, 스타일, 자유 위치 배치 | ✅ 실측 검증 18항목 |
| 배포 v0.1.0 | 설치 파일(exe) 하나로 끝나는 배포 | ✅ 실제 설치 후 4항목 확인 |
| 디자인 | 크림색 체계로 화면 전면 개편 | ⚠️ 사용자 눈으로 확인. 자동 점검 없음 |
| 배포 v0.2.0 | 크림 화면·크림 아이콘을 담아 재배포 | ✅ 실제 설치 후 46항목 통과 |
| **배포 v0.2.1** | **앱 설치 설정 색 보정 + 사용설명서** | ✅ **실제 설치 후 46항목 통과** |

### v2.0에 있던 경고는 해소되었다

v2.0 문서는 "개발본과 설치본이 다르다"고 경고했다. **v0.2.0으로 재배포하여 같아졌다.**
지금 <https://moviefit-studio.vercel.app> 에서 내려받으면 크림 화면이 들어 있는 설치 파일을 받는다.

---

## 3. 이번 세션(v2.0 → v3.0)에서 한 일

### 3-1. 시작 화면이 개편에서 빠져 있었다 (사용자 지적)

사용자가 첫 화면 사진과 함께 "박스 테두리 진하고 굵게 처리할 것"이라고 지시했다.
확인해 보니 작업 화면만 개편되고 **시작 화면은 옛 규칙 그대로**였다.

- `.drop-zone`(영상 넣기 상자)과 `[대본으로 시작하기]`가 `--line`(#2E2A26)을 쓰고 있었다.
  거의 검정이라 상자로 읽히지 않았다 → **진한 크림 2px 실선**(`--line-outer`)으로.
- 최근 프로젝트 항목과 경로 입력칸은 한 단계 낮춘 **크림 1px**(`--line-cream`)로.

### 3-2. 아이콘이 아직 청록이었다

아이콘은 CSS가 아니라 `tools/make_icons.py`가 그려서 PNG로 굽는다. 색 상수가 그 스크립트
안에 따로 박혀 있어 CSS를 고쳐도 바뀌지 않았다. **바탕화면·설치 파일·브라우저 탭에 모두
쓰이는, 사용자가 가장 먼저 보는 그림**이었다. 8종을 크림으로 다시 그렸다.

### 3-3. 소개 사이트 본문도 아직 청록이었다

"저장소 전체에서 옛 색 코드(`2DD4BF`)를 찾아 0건인지 확인한다"는 점검을 실제로 돌려
발견했다. 사이트에서는 청록을 보고 내려받았는데 프로그램을 켜면 크림이 나오는 상태였다.

- `landing/index.html`의 색 변수를 `app/static/style.css`의 `:root`와 같은 값으로 맞췄다.
- 모서리 둥글기 14px → 3px, 마지막 내려받기 상자는 진한 크림 2px 테두리.
- `landing/icons/`는 앱과 **별도 사본**이라 따로 복사해야 했다.

교훈: `memory/design-change-has-copies-elsewhere.md`

### 3-4. v0.2.0 만들어 배포

- 버전을 0.2.0으로 올렸다. `tools/build_installer.py`가 이제 `app/__init__.py`의 버전을
  **직접 읽는다** — 두 곳에 숫자를 적어 두면 어긋나기 때문이다.
- 설치 파일 **120.5MB** (내용물 480.1MB). 소개 사이트 표기를 123MB → 121MB로 고쳤다.
- 소개 사이트의 내려받기 주소는 `releases/latest/download/...` 이므로 v0.2.0을 최신으로
  올리자 사이트를 고치지 않고도 새 파일을 가리키게 되었다 (실제로 눌러 확인함).

### 3-5. 점검 스크립트의 오판정 1건 수정

설치본을 상대로 `smoke_test.py`를 돌리면 "디스크에 project.json 없음"으로 실패했다.
**프로그램 결함이 아니었다.** 설치본은 자기 설치 폴더에 저장하는데 점검 스크립트가 개발
폴더만 들여다본 것이다. 두 곳을 모두 살피게 고쳤다. (`tests/`는 설치 파일에 들어가지 않으므로
이미 만든 설치 파일에는 영향이 없다.)

### 3-6. 사용설명서를 쓰고 소개 사이트에 붙였다 (v3.1)

- `docs/USER_GUIDE.md` — 설치부터 내보내기까지 14장. **화면에 실제로 있는 항목만** 적기
  위해 `app/static/index.html`에서 버튼·라벨 165개와 단축키 표를 뽑아 대조했다.
- `landing/guide.html` — 같은 내용의 웹 페이지. 주소는 `/guide` (vercel.json의
  `cleanUrls`가 `.html`을 떼어 준다). 소개 페이지의 메뉴·바닥글에서 연결된다.
- **두 파일은 같은 내용을 따로 갖고 있다. 한쪽을 고치면 다른 쪽도 고쳐야 한다.**
- 소개 사이트의 `<style>` 블록을 `landing/style.css`로 분리했다. 두 페이지가 같은 파일을
  쓰므로 색이 어긋날 여지를 없앴다.

### 3-7. 한글 줄바꿈 문제 (사용자 지적)

사용자가 "임의로 줄바꿈하지 말고 너비에 맞게 하라"고 지적했다. `<br>` 같은 강제 줄바꿈은
없었고, 원인은 **`max-width`를 `ch` 단위로 준 것**이었다. `ch`는 영문 `0` 한 글자 너비라
한글에서는 의도한 폭의 **절반**이 된다(한글 한 글자 = 영문 `0` 두 배). `62ch`로 준 문단이
화면 절반에서 꺾이고 있었다. 픽셀로 바꾸거나 제한을 없앴다.

함께 요청받아 반영한 것: 상자 안 글의 **좌우 양쪽 정렬**(한글에서 양쪽 정렬을 하려면
`word-break`를 `normal`로 되돌려야 한다. `keep-all`이면 좁은 상자에서 글자 사이가 크게
벌어진다), 상자 테두리를 잔선(`--border`)보다 진한 `--border-box`(#8E8578)로 분리.

### 3-8. 앱 설치 설정의 색이 또 남아 있었다 (v3.2)

사용자가 설치본 화면을 보며 "주소가 IP 번호로 뜬다, PWA 아이콘을 고쳐야 하지 않느냐"고
물었다. 두 가지를 나눠 답해야 했다.

- **주소가 `127.0.0.1:8765`인 것은 정상이고 고치면 안 된다.** 이 프로그램은 내 컴퓨터
  안에서 도는 서버다. 바탕화면 아이콘이 소개 사이트(vercel)를 열면 자막을 만들 수 없다.
  `localhost:8765`로도 접속되는 것은 확인했으나(IPv6 폴백 지연 위험), 바꾸지 않았다.
- **그러나 지적 덕분에 진짜 결함이 나왔다.** `app/static/manifest.webmanifest`의
  `background_color`·`theme_color`가 개편 전 `#17181C` 그대로였다. 아이콘 **그림**은
  크림으로 바꿨지만 이 파일의 **색 값**은 그림이 아니라 눈에 띄지 않았다.
  `[앱으로 설치]`로 설치하면 시작 화면과 창 색이 옛 회색으로 나오는 상태였다.

아이콘 그림 자체는 픽셀을 세어 확인했다 — 4종 + 파비콘 모두 **청록 픽셀 0개**.
서비스 워커의 저장 이름도 `v1` → `v2`로 올렸다(이름이 바뀌어야 옛 저장본이 지워진다).

**중요**: 서비스 워커의 fetch 처리는 **네트워크 우선**이라, 서버가 켜져 있으면 옛 파일이
붙들리지 않는다. 화면이 안 바뀌어 보이면 서비스 워커보다 브라우저 캐시를 먼저 의심할 것.

### 3-9. 이번 세션의 검증 기록 (증거)

| 확인 항목 | 방법 | 결과 |
|---|---|---|
| 설치 파일 생성 | `python tools/build_installer.py` | 120.5MB, 종료 코드 0 |
| 내용물이 최신인가 | 개발 폴더와 payload의 SHA256 대조 7개 파일 | 전부 일치 |
| `._pth` 함정 3가지 | 파일 바이트 검사 | BOM 없음 / `..` 있음 / `Lib` 있음 |
| 임베디드 파이썬 동작 | payload의 python.exe로 import | tkinter·app 0.2.0·글꼴 57개 |
| 실제 설치 | `/VERYSILENT` 설치 | 종료 코드 0, 기존 projects 보존 |
| 설치본 기동 | 바탕화면 아이콘과 같은 `MovieFitStudio.bat` 실행 | health ok, 버전 0.2.0 |
| 설치본 기능 | `python tests/smoke_test.py` | **46개 통과 · 0개 실패** |
| 설치본 화면 | HTTP로 style.css·icon-192.png 받아 대조 | 크림 2px 있음, 청록 0건, 아이콘 일치 |
| 릴리스 | `gh release view v0.2.0` | Latest, exe 첨부됨 |
| 내려받기 단추 | latest/download 주소에 HEAD 요청 | 120.5MB 새 파일로 연결됨 |
| 소개 사이트 | 실제 사이트 HTML·아이콘 받아 대조 | 크림 반영, 청록 0건, 121MB |

---

## 4. 다음에 할 일 (우선순위 순)

### ① 사용자가 요청한 기능 4건 — ✅ **완료 (2026-08-13, 커밋 b852f2d)**

| 번호 | 기능 | 무엇이 되었나 |
|------|------|---------|
| F-A | **세로↔가로 전환** | `[원본 그대로] [가로 16:9] [세로 9:16] [정사각 1:1]` 네 단추. **서로 오갈 수 있다.** 옛 `[📱…]` 단추는 남았지만 이제 **자막 모양만** 세로에 맞추는 역할로 바뀌었다 (화면비는 위 단추가 담당) |
| F-B | **자막 위치 화살표** | 위/가운데/아래 아래에 십자 화살표. 한 번에 **1%**, `Shift`와 함께 누르면 **5%**. 프리셋을 쓰던 중이어도 지금 보이는 자리에서 이어서 움직인다 |
| F-C | **롱폼·숏폼** | ⓐ 미리보기 틀이 고른 화면비로 바뀐다 ⓑ 내보낼 때 **실제로 잘라 저장**한다. 사용자 확정 범위대로 **잘라내기·여백 채우기 둘 다**, **자를 위치는 미리보기 틀을 끌어서**, **영상 내보내기 3종 모두**(자막 영상·10초 미리보기·나레이션 영상) |
| F-D | **"저장됨" 클릭** | 상태 글자를 단추로 바꿨다. 누르면 2초를 기다리지 않고 **즉시 저장**하고, 마우스를 올리면 마지막 저장 시각이 보인다. `Ctrl`+`S`도 같은 경로를 쓴다 |

**핵심 파일**: 새 모듈 `app/core/framing.py` 가 화면비 계산의 단일 출처다.
화면(`app.js`)은 마우스를 끄는 동안 즉시 다시 그려야 해서 **같은 계산을 갖고 있다.**
두 곳이 어긋나면 "미리보기와 결과물이 다르다"는 최악의 결함이 되므로,
`tests/phase4_test.py` 2절이 **`app.js` 파일을 그대로 node로 실행해** 240가지 경우를 대조한다.
`framing.py` 를 고치면 `app.js` 의 `resolveFraming()` 도 함께 고쳐야 한다.

**저장 형식**: `project.json` 에 `output` 키가 생겼다.
`{"aspect": "source|16:9|9:16|1:1", "fit": "crop|pad", "focus_x": 0~100, "focus_y": 0~100, "pad_blur": true}`
기본값이 `source`(원본 그대로)라 **옛 프로젝트도 그대로 열리고 동작이 바뀌지 않는다.**

**검증 결과** (개발 서버를 실제로 띄우고 실행):

| 점검 | 결과 | 무엇을 확인했나 |
|---|---|---|
| `tests/phase4_test.py` | **46개 통과** | 내보낸 mp4 크기를 ffprobe로 실측. 원본에서 직접 잘라 만든 기준 그림과 대조해 **좌우가 뒤집히지 않았는지** 확인. 자막이 잘려 나가지 않았는지 **자막 없는 같은 영상과 밝은 픽셀을 비교**해 확인 |
| `tests/ui_test.py` | **39개 통과** | **진짜 브라우저(Playwright)로 단추를 눌러** 확인 |
| smoke / phase1 / 2 / 3 | 46 / 26 / 33 / 18 통과 | 회귀 없음 |

**독립 검증에서 결함 2건이 나와 고쳤다** (별도 컨텍스트 검증 에이전트가 발견):

1. **미리보기의 자막이 실제 결과와 다른 자리에 보였다.** 자막 위치는 "틀의 몇 %"인데
   미리보기가 잘라낸 틀이 아니라 **원본 전체**를 기준으로 그리고 있었다.
   실측 438픽셀 어긋남. 가장자리 자막이 결과물에서 30% 잘려 나갔다.
   → `outputFrameRect()` 하나를 만들어 **그리는 쪽과 마우스로 끄는 쪽이 모두** 거치게 고침.
2. **화면비를 고르면 자막을 마우스로 끌 수 없게 되었다.** 새로 얹은 화면비 안내 틀이
   자막 위에 덮여 클릭을 가로챘다. 오류도 안 나고 표시도 없었다.
   → 겹침 순서를 자막(z-index 2) > 화면비 틀(z-index 1) 로 못 박음.

함께 넣은 것: **자막이 틀 밖으로 나가면 경고**가 뜬다(몇 픽셀 넘치는지와 해결 방법까지).
내보내기 대화상자에서도 다시 알린다. 조용히 잘리는 일이 없도록 하기 위함이다.

이 넷을 마쳤으므로 **설치 파일을 다시 만들어 v0.3.0으로 올려야 한다** (아래 7절의 명령 참고).
다만 아래 ①-2를 먼저 넣고 한 번에 배포하는 것이 낫다.

### ①-1 새 기능 4건이 사용설명서에 아직 없다 — **재배포 전에 반드시**

`docs/USER_GUIDE.md` 와 `landing/guide.html` 은 기능 4건이 생기기 전에 쓰였다.
**화면비 단추·위치 화살표·저장됨 클릭이 설명서에 한 줄도 없다.**
두 파일은 같은 내용을 따로 갖고 있으므로 **양쪽을 다 고쳐야 한다** (아래 ①-2와 함께 처리할 것).
`docs/UI_SPEC.md` 의 우측 패널 설명에도 [화면비] 묶음이 빠져 있다.

> ### ⛔ 시작하기 전에 사용자에게 물어야 할 것 (이것 때문에 진행이 멈춰 있다)
>
> ①-2를 하면 같은 설명서가 **세 곳**에 생긴다:
> `docs/USER_GUIDE.md`(원본 글) · `landing/guide.html`(소개 사이트) · `app/static/guide.html`(프로그램 안).
> **손으로 복사하면 반드시 어긋난다.** 어느 방식으로 갈지 사용자에게 물어라:
>
> | 안 | 방법 | 장단점 |
> |---|---|---|
> | **가 (추천)** | `docs/USER_GUIDE.md` 를 **원본**으로 삼고, `tools/build_guide.py` 를 새로 만들어 두 HTML을 **자동 생성** | 어긋날 수 없다. 파이썬 마크다운 변환기를 하나 넣어야 한다(가벼운 것) |
> | 나 | `landing/guide.html` 을 원본으로 삼고 프로그램 안에는 **그 파일을 복사만** 한다 | 만들기 쉽다. 마크다운 원본과 HTML이 여전히 두 벌이라 어긋날 수 있다 |
> | 다 | 프로그램 안에서는 **소개 사이트를 브라우저로 연다** (파일을 담지 않는다) | 가장 간단하다. 그러나 **인터넷이 없으면 안 열린다** — 이 프로그램이 내세우는 "내 컴퓨터 안에서만 처리"와 어긋난다 |
>
> 사용자는 비개발자이므로 장단점을 한 줄씩 붙여 묻고, **(추천)** 을 표시할 것.

### ①-2 프로그램 안에 설명서로 가는 길이 없다 — **①-1과 함께 넣을 것**

첫 화면 아래에는 `버전 · 파이썬`과 `[앱으로 설치]`뿐이다. **프로그램만 쓰는 사람은
사용설명서가 있다는 사실 자체를 알 수 없다.** 사용자가 2026-08-13에 이 점을 지적했고,
"설치 파일을 또 만들기보다 기능 4건을 마친 뒤 한 번에 넣자"고 결정했다.

**함께 넣을 것 (2026-08-13 추가)**: `favicon.ico`에 128·256픽셀을 담은 새 아이콘.
배포된 v0.2.1 설치 파일에는 64픽셀까지만 들어 있어 바탕화면 큰 아이콘 보기에서 흐릿하다.
사용자 컴퓨터에는 파일을 직접 복사해 반영해 두었으나, **새로 내려받는 사람은 다음 설치
파일부터 적용된다.** 저장소에는 이미 반영되어 있으므로 다음 빌드가 자동으로 담는다.

권장 방식(사용자에게 추천했던 안): **설명서를 설치 파일에 통째로 담는다.**
`landing/guide.html`을 앱이 내보내는 화면으로 옮겨(`app/static/guide.html`) 인터넷 없이도
열리게 한다. 이 프로그램이 내세우는 "내 컴퓨터 안에서만 처리"와도 맞는다.
주의: 그러면 같은 내용이 세 곳(`docs/USER_GUIDE.md`, `landing/guide.html`,
`app/static/guide.html`)에 생긴다. **어느 하나를 원본으로 정하고 나머지를 만들어 내는
방법을 먼저 정할 것.** 그냥 복사하면 반드시 어긋난다.

### ② 기존 계획 (Phase 4·5)

- **Phase 4**: 내 목소리 나레이션(F-46), 마이크 녹음(F-45), 단어 단위 타임스탬프 재분할(F-13)
  → `docs/RESEARCH_voice_cloning.md`에 조사 완료. edge-tts로 만든 음성에 OpenVoice v2로
  **음색만 입히는 후처리** 방식. 0.4GB 선택 설치로 분리할 것
- **Phase 5**: 30분 이상 긴 영상 성능 점검, 단축키 전수 확인, 오류 메시지 한국어화.
  사용자 설명서는 v3.1에서 작성했으나 **"처음 쓰는 사람이 이것만 보고 끝까지 갈 수 있는가"는
  아직 시험하지 못했다** (ROADMAP Phase 5 참고)
- **F-55**: 투명 배경 자막 영상(webm) 내보내기 (10초 미리보기 F-54는 완료)

---

## 5. 반드시 알아야 할 것 (모르면 시간을 크게 잃는다)

`memory/` 폴더에 교훈이 파일 하나에 하나씩 정리되어 있다. **새 세션을 시작하면 먼저 읽을 것.**

| 파일 | 요약 |
|------|------|
| `do-not-narrow-the-request.md` | **기술적으로 어렵다고 요청받은 기능을 임의로 없애면 안 된다.** 제약을 알리고 묻는 것이 내 일이다 |
| `preview-must-use-the-output-frame.md` | **화면비를 바꾸면 "화면의 몇 %"의 기준 틀이 통째로 바뀐다.** 미리보기가 옛 기준으로 그리면 자막이 실제 결과와 438픽셀 어긋난다. 그리는 쪽과 끄는 쪽을 **짝으로** 고쳐야 한다. 미리보기 위에 얹은 틀이 자막 끌기를 **소리 없이 막는다**(겹침 순서) |
| `test-must-target-the-right-server.md` | **설치본과 개발본이 같은 8765 포트를 쓴다.** 그냥 점검하면 방금 고친 코드가 아니라 예전에 설치한 프로그램을 검사하고 "통과"라고 말한다. 오류가 안 나고 **통과 개수가 늘어나는 방향으로 틀린다** |
| `verify-ui-by-actually-clicking.md` | **화면 기능은 이제 코드 검토로 판정하지 않아도 된다.** Playwright로 실제로 눌러 본다. 화면과 서버에 나뉜 같은 계산은 `app.js`를 node로 실행해 대조한다. 파이썬과 JS의 **0.5 반올림 규칙이 다르다**는 함정 포함 |
| `design-change-has-copies-elsewhere.md` | **색을 바꾸는 일은 CSS 한 파일로 끝나지 않는다.** 놓치기 쉬운 네 곳 — 자주 안 보는 화면 / 그림을 굽는 스크립트(`make_icons.py`) / 다른 폴더의 사본(`landing/`) / CSS가 아닌 설정 파일(`manifest.webmanifest`). **강조색 하나만 찾으면 바탕색이 남은 것을 놓친다** |
| `windows-icon-cache-is-per-size.md` | **"파일을 바꿨다"와 "화면이 바뀐다"는 다른 문제다.** 윈도우는 아이콘을 크기별 15개 파일에 따로 저장한다. `ie4uinit -show`로는 부족하고 `iconcache*.db`를 지우고 탐색기를 다시 켜야 한다 |
| `fresh-install-must-be-tested.md` | **개발 컴퓨터에서 도는 것은 내려받은 사람 컴퓨터에서 도는 것을 보장하지 않는다.** 빈 venv + 새 clone으로 첫 실행을 반드시 시험할 것 |
| `embedded-python-bundling.md` | 설치 파일을 만들 때 `python311._pth` 함정 3가지 (`Lib` 누락 / `..` 누락 / BOM 금지) |
| `bat-file-must-be-crlf.md` | `.bat` 파일은 줄바꿈이 CRLF여야 한다. LF면 cmd가 각 줄 앞글자를 잘라먹는다 |
| `windows-console-encoding.md` | 윈도우 명령창은 cp949다. **내가 출력하는 글자는 `reconfigure`로, 남이 읽는 파일은 BOM으로** 지킨다 |
| `ffmpeg-filter-path-escaping.md` | FFmpeg 필터 경로는 상대경로+cwd로 우회한다 |
| `whisper-first-run-is-slow.md` | 음성인식 첫 실행은 486MB를 받느라 6분. 미리 안내하지 않으면 창을 닫는다 |
| `tts-silence-padding.md` | edge-tts는 앞뒤에 무음을 붙인다. **"정확히 쟀다"와 "옳은 것을 쟀다"는 다르다** |

### 검증 방식에 대한 교훈

이 프로젝트에서 발견된 결함 대부분은 **자체 테스트가 놓친 것**이었다. 이유는 세 가지다.

1. **동어반복 검증** — 자막 길이를 음성 길이로 계산해 놓고 둘을 비교하면 언제나 통과한다.
2. **결과물을 안 봄** — 자막이 영상에 제대로 새겨졌는지는 프레임을 뽑아 **눈으로 봐야** 안다.
3. **내 컴퓨터에서는 실행되지 않는 경로** — 부품이 이미 깔려 있으면 설치 단계가 통째로
   건너뛰어진다. **테스트는 "있는 것"만 확인할 수 있다.**

각 Phase 종료 시 **별도 컨텍스트의 검증 서브에이전트**를 띄워 "내 설명을 믿지 말고 직접
실행해 확인하라"고 지시할 것. 이 방식이 실제로 결함을 여러 건 잡았다.

### 아직 확인하지 못한 것 (정직한 고지)

- **디자인에는 자동 점검이 없다.** 색 코드가 CSS에 들어 있는지는 확인할 수 있지만,
  화면이 보기 좋은지는 사용자가 눈으로 봐야 안다.
- **다른 사람 컴퓨터에서 설치 파일을 시험하지 못했다.** 설치본은 자체 파이썬·FFmpeg을
  쓰므로 영향이 없어야 하지만 확인된 사실은 아니다.
- **진짜 사람 목소리가 든 긴 영상으로 아직 시험하지 않았다.**
- 마우스·키보드로만 쓰는 일부 동작(자막 나누기·합치기, 타임라인 막대 끌기, 찾기·바꾸기,
  `Ctrl`+`Z`)은 코드 검토로만 판정했다. **이제 `tests/ui_test.py` 방식으로 실제로 눌러
  확인할 수 있으므로, 이 항목들도 브라우저 점검으로 옮기면 좋다** (아직 안 옮겼다).
- **여백 채우기의 흐린 배경은 미리보기와 결과물이 완전히 같지는 않다.** 미리보기는 CSS의
  `blur(18px)`, 결과물은 FFmpeg의 `gblur=sigma=22`다. 위치와 크기는 같지만 흐린 정도가
  픽셀 단위로 일치하지는 않는다.
- **새 기능 4건을 설치 파일로 만들어 시험하지 않았다.** 개발 서버에서만 확인했다.
- 소개 사이트에 적힌 **"자동 점검 123개 항목"** 이라는 숫자는 검증하지 못했다.

---

## 6. 파일 구조 (핵심만)

```
app/
├─ server.py             FastAPI 앱, 라우터 등록
├─ __init__.py           __version__ — 버전의 단일 출처 (빌드 스크립트가 여기서 읽는다)
├─ config.py             경로·포트·허용 확장자
├─ core/
│   ├─ jobs.py           오래 걸리는 작업 + 진행률 + 취소
│   ├─ stt.py            faster-whisper 음성인식
│   ├─ subtitles.py      자막 분할·줄바꿈(손으로 넣은 줄바꿈을 지킨다)·SRT/VTT
│   ├─ narration.py      대본 → 나레이션 → 자막 타이밍 확정 (D1의 본체)
│   ├─ audio_mix.py      문장 이어붙이기·영상 믹싱·덕킹·무음 잘라내기
│   ├─ ffmpeg.py         자막 번인·미리보기·ASS 생성·진행률 파싱
│   ├─ style_map.py      스타일 단일 출처 (CSS ↔ ASS 매핑)
│   ├─ framing.py        출력 화면비 단일 출처 (롱폼·숏폼). app.js 에 같은 계산이 있다
│   ├─ fonts.py          글꼴 목록 — 설치된 글꼴의 문자표를 직접 확인한다
│   └─ tts/              base.py(인터페이스) + edge.py(기본 엔진)
├─ api/                  projects · media · system · styles · tts · audio · jobs · render · narration
└─ static/               index.html · style.css(:root 가 색의 단일 출처) · app.js · fonts/ · icons/

tests/    smoke_test · phase1_test · phase2_test · phase3_test · phase4_test(화면비) ·
          ui_test(브라우저를 띄워 실제로 눌러 보는 점검) · sample/
tools/    check_env · launch · make_icons(아이콘 색이 여기 박혀 있다) · make_sample ·
          fetch_font · build_installer
landing/  Vercel 소개 페이지 (별도 배포. icons/ 는 앱과 별도 사본이다)
memory/   교훈 저장소
build/    설치 파일 빌드 결과 (.gitignore 됨)
docs/     PRD · TECH_SPEC · UI_SPEC · ROADMAP · DEV_KIT · RESEARCH_voice_cloning
```

---

## 7. 실행 명령

```
run.bat                       사용자용 — 더블클릭하면 서버 기동 + 브라우저 열림
python -m app                 개발용 서버 (http://127.0.0.1:8765)
python tools/check_env.py     환경 점검
python tools/make_icons.py    아이콘 다시 그리기 (색은 스크립트 안 상수)
python tools/build_installer.py             설치 파일 만들기 (20분)
python tools/build_installer.py --compile-only   내용물은 그대로 두고 압축만 (10분)
python tests/smoke_test.py    기본 동작   (서버를 켠 상태에서)
python tests/phase1_test.py   자막 생성·내보내기
python tests/phase2_test.py   나레이션·동기화
python tests/phase3_test.py   스타일·위치·사전
python tests/phase4_test.py   화면비(롱폼·숏폼)·위치 화살표·저장됨 클릭
python tests/ui_test.py       브라우저를 띄워 실제로 눌러 보는 점검
```

### 설치본이 켜져 있을 때 점검하는 법 (중요 — 안 지키면 조용히 틀린다)

설치본과 개발본은 **같은 8765 포트**를 쓴다. 설치본이 켜진 채로 점검을 돌리면
**방금 고친 코드가 아니라 예전에 설치한 프로그램을 검사하고 "전부 통과"라고 말한다.**
오류가 전혀 나지 않으므로 알아채기 어렵다. (`memory/test-must-target-the-right-server.md`)

```
python -m app --port 8766                          개발 서버를 다른 포트로 띄운다
set MOVIEFIT_TEST_URL=http://127.0.0.1:8766        점검이 그쪽을 겨냥하게 한다
python tests/phase4_test.py
```

겨냥한 서버가 정말 내 코드인지 확인하는 법 (버전 번호로는 알 수 없다):

```
python -c "import urllib.request as u; print('<button id=\"save-state\"' in u.urlopen('http://127.0.0.1:8766/').read().decode())"
```

브라우저 점검(`ui_test.py`)에 필요한 개발 전용 도구 — **`requirements.txt`에는 넣지 않았다**
(프로그램 실행에 필요한 것이 아니고 설치 파일 크기에 영향을 주면 안 되므로):

```
python -m pip install playwright
python -m playwright install chromium
```

### 새 버전을 배포하는 절차 (이번에 실제로 밟은 순서)

```
1. app/__init__.py 의 __version__ 을 올린다        (빌드가 여기서 읽는다)
2. python tools/build_installer.py                 설치 파일 만들기
3. 개발 서버를 끈다                                 (설치본과 같은 8765 포트를 쓴다)
4. build/dist/MovieFitStudio-Setup.exe /VERYSILENT  실제로 설치해 본다
5. 설치 폴더의 MovieFitStudio.bat 실행 후
   python tests/smoke_test.py                      설치본을 상대로 점검
6. git push origin main
7. gh release create v0.X.0 --title "..." --notes-file <파일> build/dist/MovieFitStudio-Setup.exe
8. cd landing && vercel deploy --prod --yes         파일 크기 표기가 바뀌었으면
```

**주의 1**: 소개 사이트의 내려받기 주소는 `releases/latest/download/MovieFitStudio-Setup.exe`다.
**새 릴리스에 같은 이름으로 올리면 사이트는 고치지 않아도 된다.**

**주의 2**: 개발 서버(`python -m app`)가 켜져 있으면 설치본이 같은 주소를 쓰지 못해
바탕화면 아이콘을 눌러도 뜨지 않는다. 둘 중 하나만 켜거나, **개발 서버를 `--port 8766`
으로 띄운다** (그러면 사용자 프로그램을 끄지 않아도 된다. 6절 아래 설명 참고).

**주의 2-0 — 이 문서를 쓴 시점의 상태 (2026-08-13 12:35)**: 사용자 컴퓨터에서
**설치본 v0.2.1이 8765**에, **개발 서버가 8766**에 떠 있었다. 8766 쪽은 이전 세션이
띄운 것이라 **새 창에서는 살아 있지 않을 수 있다.** 먼저 확인하고 없으면 다시 띄울 것:

```
python -c "import urllib.request as u; print(u.urlopen('http://127.0.0.1:8766/api/health',timeout=2).read())"
```

**주의 2-1 — 자주 나오는 질문**: 바탕화면 아이콘은 **사이트가 아니라 프로그램**을 가리킨다
(`...\Programs\MovieFit Studio\MovieFitStudio.bat`, 확인 방법은 `WScript.Shell`로 `.lnk`의
TargetPath 읽기). 눌렀을 때 주소창에 `127.0.0.1:8765`가 뜨는 것이 **정상**이다.
소개 사이트(`moviefit-studio.vercel.app`)는 내려받기·설명용 별개 페이지이며, 아이콘이
그쪽을 열면 자막을 만들 수 없다. 사용자가 두 번 물어본 적 있으니 헷갈리지 말 것.

**주의 3**: 압축이 도는 동안 `build/payload/`를 손으로 실행하지 말 것. 서버가 남기는
빈 폴더와 로그가 설치 파일에 섞여 들어간다.

**주의 4**: git 커밋 메시지에 큰따옴표가 들어가면 PowerShell에서 잘린다.
파일에 적고 `git commit -F <파일>`로 넘길 것.

---

## 8. 새 창에서 시작할 때 쓸 프롬프트

```
c:\Users\INTEL\Antigravity\capcut-studio-kit 에서 MovieFit Studio 개발을 이어서 합니다.

먼저 이것부터 읽어 주세요:
1) handover_20260812_1111.md  (이 문서 — 전체 상황, v4.1)
2) memory/ 폴더의 모든 파일   (같은 실수를 반복하지 않기 위해)
3) docs/ROADMAP.md            (Phase별 완료 상태와 검증 근거)

다음으로 4절 ①-1과 ①-2를 진행해 주세요.
기능 4건(F-A~F-D)은 이미 완료되었습니다. 그 기능들이 사용설명서에 빠져 있고,
프로그램 안에서 설명서로 가는 길이 없습니다. 이 둘을 넣고 v0.3.0으로 재배포해 주세요.

시작하기 전에, 4절 ①-1의 ⛔ 상자에 있는 질문(설명서를 세 곳에 두는 방법 가·나·다)을
저에게 먼저 물어봐 주세요. 그 답이 있어야 나머지가 정해집니다.

작업 규칙:
- 기능 하나를 완성할 때마다 서버를 실제로 띄우고 tests/ 의 점검 스크립트를 돌려서
  통과를 확인한 뒤에만 완료라고 말해 주세요.
  (설치본이 8765를 쓰고 있으면 python -m app --port 8766 으로 띄우고
   MOVIEFIT_TEST_URL 을 8766으로 맞춰야 합니다. 안 그러면 구버전을 검사합니다.)
- 제가 지시한 범위를 임의로 좁히거나 기능을 없애지 마세요. 기술적으로 어렵다면
  그 사실을 알리고 어떻게 할지 물어봐 주세요.
- Phase를 끝낼 때는 별도 컨텍스트의 검증 서브에이전트를 띄워
  "내 설명을 믿지 말고 직접 실행해 확인하라"고 지시해 주세요.
```

권장 다음 작업 순서:

1. **①-1 설명서에 새 기능 4건 반영 + ①-2 프로그램 안에서 설명서 열기** — 두 가지를
   함께 처리해야 한다. 같은 내용이 `docs/USER_GUIDE.md` 와 `landing/guide.html` 두 곳에
   있고, ①-2를 하면 `app/static/guide.html` 까지 **세 곳**이 된다.
   **어느 하나를 원본으로 정하고 나머지를 만들어 내는 방법을 먼저 정할 것.** 손으로
   복사하면 반드시 어긋난다.
2. **v0.3.0 재배포** (7절 절차). 이때 `favicon.ico` 의 256픽셀 아이콘도 함께 나간다.
3. **실제 영상으로 손수 써 보기** — 사람 목소리가 든 몇 분짜리 영상.
   이때 `docs/USER_GUIDE.md`를 그대로 따라가며 설명이 맞는지 함께 확인하면 좋다.
4. Phase 4 (내 목소리 나레이션) — 용량이 크고 검증이 까다로우니 뒤로.

---

## 9. 사용자에 대해 기억할 것

- 공학박사이나 **소프트웨어 비개발자**다. 모든 대화는 한국어로, 전문 용어에는 쉬운 설명을 붙인다.
- 실행할 일이 있으면 "1) … 2) …"처럼 따라 할 수 있는 순서로 알려 준다.
- **관성적으로 동의하지 말 것.** 지적을 받으면 프로젝트 목적에 비추어 독립적으로 검토한 뒤
  근거와 함께 답한다. 문제가 있으면 대안과 함께 솔직히 반론한다.
- **그러나 지시를 임의로 바꾸지 말 것.** "스포이드를 고쳐 달라"는 요청에 기능을 삭제하는
  것으로 답했다가 강하게 지적받은 적이 있다. 기술적 제약이 있으면 **그 사실을 알리고
  어떻게 할지 물어야지, 혼자 범위를 줄이면 안 된다.**
- 화면을 직접 보며 세밀하게 지적한다. **작업 중에도 사진과 함께 지시를 보낸다.**
  간격·정렬·테두리·색감처럼 눈에 보이는 것을 중요하게 여기므로, 디자인 작업은 한 번에
  끝내려 하지 말고 자주 보여 주고 고칠 것.
- **눈에 보이는 불일치는 반드시 발견된다.** 청록 아이콘도, 얇은 테두리도 사용자가 먼저 찾았다.
- 이 프로그램을 **필요한 사람들에게 배포할 계획**이다. 그래서 라이선스·상표·정직한 기능
  표기가 중요하고, **설치가 쉬워야 한다**는 요구가 계속 나온다.
