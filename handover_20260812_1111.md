# 인수인계 — MovieFit Studio

> **v2.0** | 최종 업데이트: **2026-08-12 16:24** | 작성자: Claude Code

## 변경 이력

| 버전 | 일시 | 주요 변경 |
|------|------|---------|
| v1.0 | 2026-08-12 11:11 | 초기 작성. Phase 0~3 완료, GitHub 공개, Vercel 배포까지 마친 시점 |
| v2.0 | 2026-08-12 16:24 | 설치 파일(exe) 배포 방식 추가, 화면 디자인 전면 개편, 발견된 결함 다수 수정. 남은 작업 목록 갱신 |

---

## 1. 이 프로젝트가 무엇인가

**MovieFit Studio** — 영상 자막을 자동으로 만들고, 대본으로 AI 나레이션까지 만드는 윈도우용 무료 도구.

- 작업 폴더: `c:\Users\INTEL\Antigravity\capcut-studio-kit`
  (폴더 이름은 옛 이름 그대로다. 제품 이름만 MovieFit Studio로 바뀌었다)
- GitHub (공개): <https://github.com/parkjikoon-hub/moviefit-studio>
- 소개 사이트 (Vercel): <https://moviefit-studio.vercel.app>
- 설치 파일 (Releases): <https://github.com/parkjikoon-hub/moviefit-studio/releases/tag/v0.1.0>
- 라이선스: MIT

문서 우선순위: `docs/PRD.md` > `docs/TECH_SPEC.md` > `docs/UI_SPEC.md` > `docs/ROADMAP.md`

---

## 2. 지금 어디까지 되어 있는가 (v2.0 기준 — 2026-08-12 16:24)

### 완료된 단계

| 단계 | 내용 | 검증 |
|------|------|------|
| Phase 0 | 서버 뼈대, 프로젝트 저장, PWA, 실행 스크립트 | ✅ 독립 검증 |
| Phase 1 | 영상 → 자막 자동 생성, 편집기, SRT·번인 내보내기 | ✅ 독립 검증 + 결함 5건 수정 |
| Phase 2 | 대본 → 나레이션 + 자막 타이밍 자동 산출 | ✅ 독립 검증 + 결함 3건 수정 |
| Phase 3 | 사전, 자막 가져오기, 스타일, 자유 위치 배치 | ✅ 실측 검증 18항목 |
| 배포 | **설치 파일(exe) 하나로 끝나는 배포** | ✅ 실제 설치 후 4항목 확인 |
| 디자인 | **크림색 체계로 화면 전면 개편** | ⚠️ 사용자 눈으로 확인함. 자동 점검은 없음 |

### ⚠️ 가장 먼저 알아야 할 것 — 개발본과 설치본이 다르다

- **개발본**(작업 폴더)에는 디자인 개편이 전부 들어가 있다.
- **설치본**(`%LOCALAPPDATA%\Programs\MovieFit Studio`)과 **GitHub Releases의 설치 파일**은
  **디자인 개편 전 상태**다. 2026-08-12 13:00쯤 만든 것이다.
- 따라서 **설치 파일을 다시 만들어 배포하는 것이 다음 작업의 첫 항목**이다.
  (`python tools/build_installer.py` — 내용물 만들기 10분 + 압축 10분)

---

## 3. 이번 세션(v1.0 → v2.0)에서 한 일

### 3-1. 설치 파일 하나로 끝나는 배포 (완료)

"GitHub 다운로드·설치를 어려워하는 사람이 많다"는 요구에서 출발했다.

**먼저 검토하고 접은 것**: 소개 사이트에서 바로 쓰는 웹앱화. Vercel 무료 요금제는
요청 본문 4.5MB·함수 실행 60초·프로그램 크기 250MB 제한이 있는데, 영상은 수백 MB이고
Whisper 모델만 486MB이며 자막 번인은 몇 분이 걸린다. 무엇보다 PRD의 차별점 D5
(영상이 외부로 나가지 않는다)를 포기하게 된다. 그래서 설치 간소화로 방향을 잡았다.

- `tools/build_installer.py` 신규. 임베디드 파이썬 + 부품 + tkinter + FFmpeg + 폰트 + 앱을
  모아 Inno Setup으로 묶는다. 결과 **122.7MB** (내용물 489.6MB).
- 설치 위치는 사용자 폴더(`{localappdata}\Programs`), 권한은 `lowest`.
  관리자 권한 경고가 뜨지 않고 `projects/` 쓰기도 막히지 않는다. **앱 코드는 고치지 않았다.**
- 설치는 **2단계**(내려받기 → 실행). 소개 사이트도 그에 맞게 고쳐 배포했다.
- 교훈은 `memory/embedded-python-bundling.md`에 있다 (`._pth` 함정 3가지).

### 3-2. 새로 내려받은 사람만 겪던 결함 3건 (완료)

빈 가상환경에 새로 clone 해서 처음으로 돌려 보고 발견했다.

1. **첫 설치가 통째로 실패** — `requirements.txt`에 한글 주석이 있는데 BOM이 없어
   pip이 cp949로 읽다 죽었다. UTF-8 BOM으로 저장해 해결.
2. **자막 글꼴이 달라짐** — 폰트를 `.gitignore`로 뺐는데 `run.bat`이 받아 주지 않았다.
   `tools/launch.py`에 `ensure_fonts()` 추가.
3. **목소리 견본 mp3 7개가 저장소 맨 위에 공개되어 있었다** — 제거하고 `.gitignore` 보강.

교훈: `memory/fresh-install-must-be-tested.md`

### 3-3. 화면 디자인 전면 개편 (완료)

사용자가 참고 사이트(pipeletsystem.com)의 색감을 지정했다.

- **초록(청록) 계열 완전 제거**. 강조색을 **크림 한 가지**로 통일.
- 바탕을 따뜻한 검정(`#0D0C0B`)으로.
- 테두리 4단계: 바깥 상자는 **진한 크림 2px**, 안쪽 단추·입력칸은 한 단계 낮춘 1px.
- 모서리 둥글기 10~18px → **2~4px**.
- 단추는 양각이 기본, **고르면 안이 크림으로 채워지며 음각으로 유지**.
  한 번 실행하고 끝나는 단추는 눌린 모습을 0.3초만 남긴다.
- 패널 안의 묶음 17개를 테두리 상자(`.grp`)로 감쌌다 (HTML 구조 변경).
- 상단 막대를 왼쪽·가운데·오른쪽 3구역으로. 모드 단추가 늘 화면 한가운데 온다.

### 3-4. 색 고르기를 직접 만듦 (완료)

**주의 — 이 부분에서 지시를 어긴 적이 있다.** 사용자가 "스포이드가 색감창 앞으로
나오게 하라"고 했는데, 브라우저 기본 색 창의 한계를 이유로 **기능을 임의로 삭제**했다.
사용자가 강하게 지적했고, 지시대로 다시 만들었다.
**앞으로도 범위를 임의로 좁히지 말 것.**

- 색감표(가로=선명함, 세로=밝기) + 색상 띠 + 대표 색 8개 + 색상 코드 입력
- **스포이드는 `EyeDropper`를 우리 화면에서 직접 부른다.** 브라우저 색 창을 거치지
  않으므로 창이 뒤로 숨지 않는다. 미지원 브라우저에서는 그 단추만 끈다.

### 3-5. 그 밖에 고친 것

| 항목 | 내용 |
|------|------|
| 글꼴 목록 | **7개 → 57개.** 이름을 미리 적어 두고 대조하던 방식을 버리고, 설치된 글꼴 파일의 문자표에서 '가'(U+AC00)를 그릴 수 있는지 직접 확인한다 (`app/core/fonts.py`). 첫 조회 0.34초 |
| 두 번 실행 | 아무 설명 없이 창이 닫히던 것을 고쳤다. 이미 실행 중이면 알리고 브라우저만 연다 (`tools/launch.py`의 `port_state()`) |
| 구간 반복 | `A-B 반복` 같은 전문 용어를 없애고 `구간 반복 → 끝 지점 찍기 → 반복 끄기`로 |
| 타임라인 붉은 선 | `− + 전체` 단추를 덮던 문제. 원인은 **내가 넣은 공통 최소 너비(96px)** 가 좁은 자리의 작은 단추에도 걸린 것 |
| 줄바꿈 안내 | `Shift`+`Enter`로 직접 줄을 나눌 수 있다는 설명 추가. 기능은 원래 있었으나 화면에 안내가 없었다 |
| 프리셋 저장 줄 | 이름 칸과 `[저장]`이 겹치던 것 수정, 높이 34px로 통일 |

---

## 4. 다음에 할 일 (우선순위 순)

### ① 설치 파일 다시 만들어 배포 — **가장 먼저**

디자인 개편이 설치본에 반영되어 있지 않다.

```
python tools/build_installer.py          내용물 만들기 + 압축 (20분쯤)
gh release upload v0.1.0 build/dist/MovieFitStudio-Setup.exe --clobber
```
또는 새 버전(v0.2.0)으로 릴리스를 만든다. 소개 사이트의 내려받기 주소는
`releases/latest/download/MovieFitStudio-Setup.exe` 이므로 **최신 릴리스에 같은 이름으로
올리면 사이트는 고치지 않아도 된다.**

### ② 사용자가 요청한 기능 4건 (미착수)

| 번호 | 기능 | 지금 상태 |
|------|------|---------|
| F-A | **세로↔가로 전환** | `[📱 세로 영상용으로 맞추기]`가 **한 방향으로만** 간다. 되돌리려면 `Ctrl`+`Z` 뿐. `app/static/app.js`의 `applyShortsPreset()` 참조. **`[가로 16:9] [세로 9:16] [정사각 1:1]`로 나눠 서로 오갈 수 있게** 만들 것 |
| F-B | **자막 위치 ↑↓←→ 단추** | 지금은 가로 %·세로 % 숫자 입력과 마우스 끌기뿐. 백엔드에 `POSITION_PRESET_Y`(top/middle/bottom)가 이미 있으니 연결만 하면 된다 |
| F-C | **롱폼·숏폼** | 사용자가 **둘 다** 선택했다. ⓐ 미리보기 틀을 16:9 / 9:16 / 1:1로 바꿔 보기 ⓑ 영상을 실제로 세로로 잘라 저장. ⓑ는 PRD의 "만들지 않는 것"(영상 편집)에 걸치므로 범위를 명확히 하고 진행할 것 |
| F-D | **"저장됨" 클릭 동작** | 지금은 클릭 안 되는 상태 표시 글자다. 사용자가 눌러 보려 했다. 눌렀을 때 즉시 저장 + 되돌림점을 만들지, 아예 눌리지 않게 보일지 **사용자에게 물어볼 것** |

### ③ 기존 계획 (Phase 4·5)

- **Phase 4**: 내 목소리 나레이션(F-46), 마이크 녹음(F-45), 단어 단위 타임스탬프 재분할(F-13)
  → `docs/RESEARCH_voice_cloning.md`에 조사 완료. edge-tts로 만든 음성에 OpenVoice v2로
  **음색만 입히는 후처리** 방식. 0.4GB 선택 설치로 분리할 것
- **Phase 5**: 사용자 설명서(`USER_GUIDE.md`), 30분 이상 긴 영상 성능 점검
- **F-55**: 투명 배경 자막 영상(webm) 내보내기 (10초 미리보기 F-54는 완료)

---

## 5. 반드시 알아야 할 것 (모르면 시간을 크게 잃는다)

`memory/` 폴더에 교훈이 파일 하나에 하나씩 정리되어 있다. **새 세션을 시작하면 먼저 읽을 것.**

| 파일 | 요약 |
|------|------|
| `bat-file-must-be-crlf.md` | `.bat` 파일은 줄바꿈이 CRLF여야 한다. LF면 cmd가 각 줄 앞글자를 잘라먹는다 |
| `windows-console-encoding.md` | 윈도우 명령창은 cp949다. **내가 출력하는 글자는 `reconfigure`로, 남이 읽는 파일은 BOM으로** 지킨다 |
| `ffmpeg-filter-path-escaping.md` | FFmpeg 필터 경로는 상대경로+cwd로 우회한다 |
| `whisper-first-run-is-slow.md` | 음성인식 첫 실행은 486MB를 받느라 6분. 미리 안내하지 않으면 창을 닫는다 |
| `tts-silence-padding.md` | edge-tts는 앞뒤에 무음을 붙인다. **"정확히 쟀다"와 "옳은 것을 쟀다"는 다르다** |
| `fresh-install-must-be-tested.md` | **개발 컴퓨터에서 도는 것은 내려받은 사람 컴퓨터에서 도는 것을 보장하지 않는다.** 빈 venv + 새 clone으로 첫 실행을 반드시 시험할 것 |
| `embedded-python-bundling.md` | 설치 파일을 만들 때 `python311._pth` 함정 3가지 (`Lib` 누락 / `..` 누락 / BOM 금지) |

### 검증 방식에 대한 교훈

이 프로젝트에서 발견된 결함 대부분은 **자체 테스트가 놓친 것**이었다. 이유는 세 가지다.

1. **동어반복 검증** — 자막 길이를 음성 길이로 계산해 놓고 둘을 비교하면 언제나 통과한다.
2. **결과물을 안 봄** — 자막이 영상에 제대로 새겨졌는지는 프레임을 뽑아 **눈으로 봐야** 안다.
3. **내 컴퓨터에서는 실행되지 않는 경로** — 부품이 이미 깔려 있으면 설치 단계가 통째로
   건너뛰어진다. **테스트는 "있는 것"만 확인할 수 있다.**

각 Phase 종료 시 **별도 컨텍스트의 검증 서브에이전트**를 띄워 "내 설명을 믿지 말고 직접
실행해 확인하라"고 지시할 것. 이 방식이 실제로 결함을 여러 건 잡았다.

### 아직 확인하지 못한 것 (정직한 고지)

- **디자인 개편에는 자동 점검이 없다.** 사용자가 눈으로 확인했을 뿐이다.
  `tests/` 스크립트는 화면 모양을 보지 못한다.
- **다른 사람 컴퓨터에서 설치 파일을 시험하지 못했다.** 설치본은 자체 파이썬·FFmpeg을
  쓰므로 영향이 없어야 하지만 확인된 사실은 아니다.
- **진짜 사람 목소리가 든 긴 영상으로 아직 시험하지 않았다.**
- 마우스·키보드로만 쓰는 일부 동작(자막 나누기·합치기, 타임라인 막대 끌기, 찾기·바꾸기,
  `Ctrl`+`Z`)은 코드 검토로만 판정했다.
- 소개 사이트에 적힌 **"자동 점검 123개 항목"** 이라는 숫자는 검증하지 못했다.

---

## 6. 파일 구조 (핵심만)

```
app/
├─ server.py             FastAPI 앱, 라우터 등록
├─ config.py             경로·포트·허용 확장자
├─ core/
│   ├─ jobs.py           오래 걸리는 작업 + 진행률 + 취소
│   ├─ stt.py            faster-whisper 음성인식
│   ├─ subtitles.py      자막 분할·줄바꿈(손으로 넣은 줄바꿈을 지킨다)·SRT/VTT
│   ├─ narration.py      대본 → 나레이션 → 자막 타이밍 확정 (D1의 본체)
│   ├─ audio_mix.py      문장 이어붙이기·영상 믹싱·덕킹·무음 잘라내기
│   ├─ ffmpeg.py         자막 번인·미리보기·ASS 생성·진행률 파싱
│   ├─ style_map.py      스타일 단일 출처 (CSS ↔ ASS 매핑)
│   ├─ fonts.py          글꼴 목록 — 설치된 글꼴의 문자표를 직접 확인한다
│   └─ tts/              base.py(인터페이스) + edge.py(기본 엔진)
├─ api/                  projects · media · system · styles · tts · audio · jobs · render · narration
└─ static/               index.html · style.css · app.js · fonts/ · icons/

tests/    smoke_test · phase1_test · phase2_test · phase3_test · sample/
tools/    check_env · launch · make_icons · make_sample · fetch_font · build_installer
landing/  Vercel 소개 페이지 (별도 배포)
memory/   교훈 저장소
build/    설치 파일 빌드 결과 (.gitignore 됨)
docs/     PRD · TECH_SPEC · UI_SPEC · ROADMAP · DEV_KIT · RESEARCH_voice_cloning
```

---

## 7. 실행 명령

```
run.bat                       사용자용 — 더블클릭하면 서버 기동 + 브라우저 열림
python -m app                 개발용 서버 (http://127.0.0.1:8765)
python tools/check_env.py     환경 점검
python tools/build_installer.py             설치 파일 만들기 (20분)
python tools/build_installer.py --compile-only   내용물은 그대로 두고 압축만 (10분)
python tests/smoke_test.py    기본 동작   (서버를 켠 상태에서)
python tests/phase1_test.py   자막 생성·내보내기
python tests/phase2_test.py   나레이션·동기화
python tests/phase3_test.py   스타일·위치·사전
```

배포:
```
cd landing && vercel deploy --prod --yes                       소개 사이트 갱신
gh release upload v0.1.0 build/dist/MovieFitStudio-Setup.exe --clobber   설치 파일 갱신
git push origin main                                            GitHub 반영
```

**주의**: 개발 서버(`python -m app`)가 켜져 있으면 설치본이 같은 주소를 쓰지 못해
바탕화면 아이콘을 눌러도 뜨지 않는다. 둘 중 하나만 켤 것.

---

## 8. 새 창에서 시작할 때 쓸 프롬프트

```
c:\Users\INTEL\Antigravity\capcut-studio-kit 에서 MovieFit Studio 개발을 이어서 합니다.

먼저 이것부터 읽어 주세요:
1) handover_20260812_1111.md  (이 문서 — 전체 상황, v2.0)
2) memory/ 폴더의 모든 파일   (같은 실수를 반복하지 않기 위해)
3) docs/ROADMAP.md            (Phase별 완료 상태와 검증 근거)

다음으로 [여기에 하고 싶은 작업을 적으세요]를 진행해 주세요.

작업 규칙:
- 기능 하나를 완성할 때마다 서버를 실제로 띄우고 tests/ 의 점검 스크립트를 돌려서
  통과를 확인한 뒤에만 완료라고 말해 주세요.
- 제가 지시한 범위를 임의로 좁히거나 기능을 없애지 마세요. 기술적으로 어렵다면
  그 사실을 알리고 어떻게 할지 물어봐 주세요.
- Phase를 끝낼 때는 별도 컨텍스트의 검증 서브에이전트를 띄워
  "내 설명을 믿지 말고 직접 실행해 확인하라"고 지시해 주세요.
```

권장 다음 작업 순서:

1. **설치 파일 다시 만들어 배포** — 지금 배포된 것은 디자인 개편 전이다. 20분이면 끝난다.
2. **기능 4건**(F-A ~ F-D) — 사용자가 실제로 써 보며 요청한 것들이라 값이 크다.
3. **실제 영상으로 손수 써 보기** — 사람 목소리가 든 몇 분짜리 영상.
4. **사용자 설명서**(`USER_GUIDE.md`) — 배포를 시작했으니 쓰는 사람에게 필요하다.
5. Phase 4 (내 목소리 나레이션) — 용량이 크고 검증이 까다로우니 뒤로.

---

## 9. 사용자에 대해 기억할 것

- 공학박사이나 **소프트웨어 비개발자**다. 모든 대화는 한국어로, 전문 용어에는 쉬운 설명을 붙인다.
- 실행할 일이 있으면 "1) … 2) …"처럼 따라 할 수 있는 순서로 알려 준다.
- **관성적으로 동의하지 말 것.** 지적을 받으면 프로젝트 목적에 비추어 독립적으로 검토한 뒤
  근거와 함께 답한다. 문제가 있으면 대안과 함께 솔직히 반론한다.
- **그러나 지시를 임의로 바꾸지 말 것.** 이번 세션에서 "스포이드를 고쳐 달라"는 요청에
  기능을 삭제하는 것으로 답했다가 강하게 지적받았다. 기술적 제약이 있으면
  **그 사실을 알리고 어떻게 할지 물어야지, 혼자 범위를 줄이면 안 된다.**
- 화면을 직접 보며 세밀하게 지적한다. 간격·정렬·테두리·색감처럼 눈에 보이는 것을
  중요하게 여기므로, 디자인 작업은 한 번에 끝내려 하지 말고 자주 보여 주고 고칠 것.
- 이 프로그램을 **필요한 사람들에게 배포할 계획**이다. 그래서 라이선스·상표·정직한 기능
  표기가 중요하고, **설치가 쉬워야 한다**는 요구가 계속 나온다.
